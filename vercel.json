{
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter athelon-app run build",
  "outputDirectory": "apps/athelon-app/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
