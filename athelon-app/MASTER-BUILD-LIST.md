# MASTER BUILD LIST — Athelon MRO Platform
**Created:** 2026-02-28
**Source:** DOM/QCM simulation findings + Corridor competitive analysis + gap audit

---

## Priority Tiers
- **P0 — Critical**: Blocks basic MRO workflow (found during simulation)
- **P1 — High**: Competitive gap with Corridor, required for sales
- **P2 — Medium**: Nice to have, improves UX/completeness
- **P3 — Low**: Future / external dependency

---

## P0 — CRITICAL (Simulation Blockers)

| # | Feature | Issue Found | Backend | Frontend | Est |
|---|---------|-------------|---------|----------|-----|
| 1 | **Dual Sign-off (Tech + Inspector)** | Schema only has `signingTechnicianId` — no separate inspector vs RTS signer fields. Part 145 requires tech sign-off AND independent inspector sign-off | FIX schema: add `inspectorTechnicianId`, `inspectorSignedAt`, `rtsTechnicianId`, `rtsSignedAt` | Update WO detail / task card UI | 2h |
| 2 | **PDF Generation — Invoices** | All PDF buttons are TODO stubs. Cannot send invoices to customers | Wire `@react-pdf/renderer` to invoice data | Invoice detail page PDF button | 3h |
| 3 | **PDF Generation — Quotes** | Quote PDF is a stub | Wire `@react-pdf/renderer` to quote data | Quote detail page PDF button | 2h |
| 4 | **PDF Generation — RTS / 8610-2** | Return-to-service document generation is a stub. Required for aircraft release | Build FAA Form 8610-2 template + RTS certificate | WO release page | 4h |
| 5 | **PDF Generation — Work Order Pack** | No WO printout capability — shops need paper trail | Build WO summary + task card list PDF | WO detail page | 3h |
| 6 | **Email / Notifications System** | Zero email capability. Cannot send invoices, quotes, or alerts | Wire email provider (Resend/SendGrid) or stub with Convex action | Settings page + send buttons on invoices/quotes | 4h |
| 7 | **Pricing Rules — Auto-Application** | Pricing module exists but always returns $0. Labor rates not applied to quotes/invoices | Fix `pricing:computeRate` to actually look up org pricing rules and apply | Verify pricing flows into quote/invoice creation | 2h |
| 8 | **Tax Calculation** | Tax config page exists but `computeTaxForInvoice` always returns 0 | Fix tax computation to use org tax rates from `taxRates` table | Verify tax auto-populates on invoices | 1h |
| 9 | **Document Upload / Attachments** | Backend has 6 functions but no real file upload wired. Cannot attach photos, manuals, etc. | Wire Convex file storage for uploads | Add upload dropzone to WO detail, task cards, discrepancies | 4h |
| 10 | **Customer Portal (ServiceEdge equivalent)** | `customerPortal.ts` has 7 functions but no frontend pages | Backend exists | Build `/portal` routes: WO status, aircraft list, invoice history, quote approval | 6h |

---

## P1 — HIGH (Corridor Competitive Gaps)

| # | Feature | Corridor Has | Backend | Frontend | Est |
|---|---------|-------------|---------|----------|-----|
| 11 | **Barcoding / QR Scanning** | System-wide barcode scanning for parts, tools, WOs | Add barcode fields to parts/tools schema, generate QR codes | Camera scanner component + QR code display on parts/WO pages | 6h |
| 12 | **Reports & Analytics Dashboard** | PowerBI integration, job performance, delivery metrics | Build report queries: WO turnaround time, labor utilization, revenue by customer, parts usage | `/reports` page with recharts dashboards + CSV export | 8h |
| 13 | **Scheduling — Drag-Drop Gantt** | Full planning & scheduling module | Already have `scheduling.ts` + `capacity.ts` — need drag-drop mutations | Replace static Gantt with draggable bars, save schedule changes | 6h |
| 14 | **Scheduling — Auto-Schedule** | Resource-constrained scheduling | Build constraint solver: bay capacity × tech availability × priority | "Auto-Schedule" button on scheduling page | 4h |
| 15 | **Role Management Admin UI** | Robust security and access controls | `roles.ts` has 5 functions | Build `/settings/users` role assignment UI — assign/revoke roles, permission matrix | 4h |
| 16 | **Training & Qualifications Mgmt** | Dedicated module — manage/enforce personnel qualifications | `training.ts` has 8 functions | Build `/personnel/training` with full CRUD: add training records, set requirements, track expiry, compliance dashboard | 4h |
| 17 | **Multi-Location Cross-Site UI** | Enterprise multi-facility support | `shopLocations.ts` exists | Build location switcher in header, cross-site parts transfer UI, location-scoped dashboards | 4h |
| 18 | **Warranty Management** | Dedicated warranty adjudication module | `warranty.ts` has 8 functions | Build warranty claims list, create/submit/approve flow, link to WO/invoice | 4h |
| 19 | **Tool Crib Management** | Dedicated tool management, calibration tracking | `toolCrib.ts` has 8 functions | Build `/parts/tools` with full CRUD: check-in/out, calibration due list, tool usage history | 4h |
| 20 | **AD/SB Fleet Compliance Dashboard** | Regulatory compliance module with CAMP Connect | `adCompliance.ts` has 6 functions | Enhance `/compliance/ad-sb`: fleet-wide AD matrix, due-date tracking, bulk import ADs | 4h |

---

## P2 — MEDIUM (UX & Completeness)

| # | Feature | Issue | Backend | Frontend | Est |
|---|---------|-------|---------|----------|-----|
| 21 | **Overtime / Shift Management** | Time clock lacks overtime rules, shift definitions | Add shift schedules, overtime thresholds to schema | Time clock settings + overtime flagging on time approval | 3h |
| 22 | **Discrepancy Disposition Workflow** | All 11 discrepancies in sim stayed "open" — no UI to disposition | Backend exists (`discrepancies.ts`) | Add disposition dialog: corrective action, parts used, sign-off, MEL deferral | 3h |
| 23 | **Work Order Close Readiness** | `getCloseReadiness` query exists but no UI shows it | Backend exists | Add close-readiness checklist panel on WO detail page | 2h |
| 24 | **Squawk Board Enhancements** | Squawk page exists but doesn't show severity/priority badges | Backend has severity/priority fields | Add color-coded severity badges, filter by priority, link to WO | 2h |
| 25 | **Search — Global Search** | `globalSearch` query exists but no search bar in UI | Backend exists | Add command-palette search (⌘K) with results across aircraft, WOs, parts, customers | 3h |
| 26 | **Dashboard KPIs** | Dashboard exists but limited real-time metrics | Add dashboard aggregate queries | KPI cards: active WOs, AOG count, overdue invoices, parts on order, utilization % | 3h |
| 27 | **Kanban — Drag-Drop** | Kanban board is visual only, no drag | Backend `updateWorkOrderStatus` exists | Add drag-drop between status columns on kanban page | 3h |
| 28 | **Handoff Notes** | Schema supports handoff notes but no UI | Backend `addHandoffNote` exists | Add shift handoff panel on task card detail | 2h |
| 29 | **Fleet Calendar Enhancements** | Calendar page exists but sparse | `fleetCalendar.ts` has 1 function | Add maintenance due dates, AD due dates, inspection intervals to calendar | 3h |
| 30 | **Conformity Inspection** | `conformityInspections.ts` has 4 functions, no frontend | Backend exists | Build conformity inspection page linked from receiving | 2h |

---

## P3 — LOW (External Dependencies / Future)

| # | Feature | Dependency | Est |
|---|---------|-----------|-----|
| 31 | **CAMP Connect Integration** | External API access required (CAMP/Hearst partnership) | Blocked |
| 32 | **QuickBooks Integration** | External OAuth + QuickBooks API | 8h when unblocked |
| 33 | **Multi-Currency** | `currency.ts` has 6 functions — needs UX design for currency switching | 4h |
| 34 | **ILS Parts Marketplace** | External API (ILS Bridge) | Blocked |
| 35 | **AI Predictive Maintenance** | `predictions.ts` backend exists — needs ML model or heuristic engine | 8h |
| 36 | **eCommerce / Parts Sales** | Payment provider integration required | Blocked |
| 37 | **Vertex Tax Integration** | External API | 4h when unblocked |
| 38 | **UPS WorldShip Integration** | External API | Blocked |
| 39 | **FBO Line Sales** | Different market segment — not building | N/A |
| 40 | **Native Mobile App** | PWA covers this adequately for now | Future |

---

## Build Order (Recommended)

### Sprint 1: P0 Critical Path (8 items, ~25h)
1. Dual sign-off schema fix (#1)
2. PDF generation — invoices, quotes, RTS, WO pack (#2-5)
3. Tax calculation fix (#8)
4. Pricing rules fix (#7)
5. Document upload (#9)

### Sprint 2: P0 + P1 High Priority (6 items, ~26h)
6. Email/notifications (#6)
7. Customer portal (#10)
8. Reports & analytics (#12)
9. Barcoding/QR (#11)
10. Role management UI (#15)

### Sprint 3: P1 Corridor Parity (6 items, ~24h)
11. Scheduling drag-drop + auto-schedule (#13-14)
12. Training & qualifications UI (#16)
13. Multi-location UI (#17)
14. Warranty management UI (#18)
15. Tool crib UI (#19)
16. AD fleet compliance dashboard (#20)

### Sprint 4: P2 Polish (10 items, ~26h)
17-26. Items #21-30

### Sprint 5: P3 Integrations (as dependencies unblock)
27-40. Items #31-40

---

## Metrics
- **Total features**: 40
- **P0 Critical**: 10 (~25h)
- **P1 High**: 10 (~48h)  
- **P2 Medium**: 10 (~26h)
- **P3 Low/Blocked**: 10 (varies)
- **Estimated total buildable now**: 30 features, ~99h
- **Current completion**: ~82% → Target after Sprint 3: ~95%

---

## Autonomous Improvements

### Cycle 2 — 2026-02-28 (Workflow Fidelity)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-004 | Wire task card compliance items to Convex backend — replace `useState(INITIAL_COMPLIANCE_ITEMS)` with `useQuery(api.taskCompliance.getComplianceItemsForTask)` + `addComplianceItem` / `updateComplianceStatus` mutations. Under a Part 145 audit, compliance changes must persist — currently all edits evaporate on page reload. | P0 | 1.5h | `tasks/[cardId]/page.tsx` | ✅ Done |
| AI-005 | Wire vendor services on task card page to Convex backend — replace `useState(INITIAL_VENDOR_SERVICES)` with `useQuery(api.taskCardVendorServices.getVendorServicesForTask)` + `addVendorServiceToTask` / `updateVendorServiceStatus` mutations. Vendor service assignments currently evaporate on page reload. | P1 | 1h | `tasks/[cardId]/page.tsx` | ✅ Done |
| AI-006 | Wire `Log Squawk` button in `DiscrepancyList` — button renders but has no `onClick`. Add a `LogSquawkDialog` component and call `api.discrepancies.openDiscrepancy`. Pass required props (`orgId`, `workOrderId`, `techId`) from the WO detail page. | P1 | 1h | `_components/DiscrepancyList.tsx`, `work-orders/[id]/page.tsx` | ✅ Done |

### Cycle 1 — 2026-02-28 (Regulatory Compliance)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-001 | Replace hardcoded `demoTaskCompliance` in `WOComplianceTab` with real Convex data from `api.taskCompliance.getComplianceItemsForWorkOrder`. A DOM inspector walking in sees fake task compliance data today. | P0 | 1h | `WOComplianceTab.tsx` | ✅ Done |
| AI-002 | Replace hardcoded `demoRtsChecklist` and `rtsCompleted = false` in `ReturnToServiceSection` with real data from `api.returnToService.getCloseReadinessReport`. RTS section always shows "not complete" even when RTS is signed. | P0 | 1h | `WOComplianceTab.tsx` | ✅ Done |
| AI-003 | Make the Compliance tab badge on WO detail page dynamic — compute actual non-compliance from real AD + task compliance data instead of hardcoded amber dot. | P1 | 0.5h | `work-orders/[id]/page.tsx` | ✅ Done |
| BUG-001 | `dashboard/page.tsx` line 90: `wo.discrepancyCount` → `wo.openDiscrepancyCount` (pre-existing TS error) | P0 | 5m | `dashboard/page.tsx` | ✅ Done |
| BUG-002 | `work-orders/page.tsx` line 440: `wo.workOrderNumber` on `WorkOrderRow` type → `wo.number` (pre-existing TS error) | P0 | 5m | `work-orders/page.tsx` | ✅ Done |

### Cycle 4 — 2026-02-28 (UI/UX Quality)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-010 | Fix `squawks/page.tsx` dark-mode color system — replace all hardcoded Tailwind color literals (`text-gray-900`, `text-gray-500`, `bg-blue-50`, `bg-red-50`, `bg-amber-50`, `text-blue-600`, `text-red-600`, `text-amber-600`) with CSS variable tokens matching app design system. Replace hand-rolled loading spinner with `Skeleton` components. Fix "New Squawk" disabled button — replace with a navigable link to work orders (squawks require WO context). | P1 | 30m | `squawks/page.tsx` | ✅ Done |
| AI-011 | Fix `dashboard/page.tsx` — make date subtitle dynamic (was hardcoded "Mon, Feb 23, 2026"). Replace duplicate static `stats` grid (4 fake-data cards) with a `LiveSecondaryKPIs` component that queries real parts-awaiting-inspection count (`api.parts.listParts` with `pending_inspection` filter) and expiring cert count (`api.technicians.listWithExpiringCerts`). The old `stats` row duplicated the live KPI cards above it with fabricated numbers. | P1 | 1h | `dashboard/page.tsx` | ✅ Done |
| AI-012 | Fix `dashboard/page.tsx` — replace hardcoded `activeWorkOrders` demo array with live Convex data from `api.workOrders.getWorkOrdersWithScheduleRisk`. Dashboard "Active Work Orders" card was showing N192AK, N76LS, N416AB with fake task/squawk counts — never reflected real WOs. Add `LiveActiveWorkOrders` component that fetches live, sorts AOG-first, caps at 5. | P1 | 1h | `dashboard/page.tsx` | ✅ Done |

### Cycle 5 — 2026-02-28 (Data Integrity)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-013 | Fix `CloseReadinessPanel.tsx` stub close handler — `handleClose()` was calling `toast.success("Work order closed successfully")` without any Convex mutation. WO never actually closed in the backend (silent data integrity failure). Replaced stub with `navigate(\`/work-orders/${workOrderId}/rts\`)` which routes to the full RTS authorization flow (signatureAuthEventId, RTS statement, aircraft hours). | P0 | 30m | `components/CloseReadinessPanel.tsx` | ✅ Done |
| AI-014 | Fix `CloseReadinessPanel.tsx` fragile string-matching checklist — CHECKLIST_ITEMS used `b.toLowerCase().includes("task card")` etc. to detect blockers. The status blocker `"Work order status is 'X'"` matched nothing and silently showed green. Replaced with direct backend blocker display — actual blocker messages verbatim with XCircle icons. No string guessing. | P1 | 20m | `components/CloseReadinessPanel.tsx` | ✅ Done |
| AI-015 | Fix MEL deferral in `DiscrepancyDispositionDialog.tsx` — previously showed `toast.error("MEL deferral requires pre-signing auth")` and did nothing. No "full deferral workflow" page exists. MEL deferrals are a 14 CFR 91.213/MMEL regulatory requirement. Added `signatureAuthEventId` input + `melDeferralDate` field to MEL path. Wired to `api.discrepancies.deferDiscrepancy`. Added optional `workOrderId` prop so signature page link can be constructed. Updated `squawks/page.tsx` to pass `workOrderId` to dialog. | P0 | 1h | `components/DiscrepancyDispositionDialog.tsx`, `app/(app)/squawks/page.tsx` | ✅ Done |

### Cycle 3 — 2026-02-28 (Parts Traceability)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-007 | Replace hand-rolled toast state (`useState` + `setTimeout`) in `parts/page.tsx` with `toast` from `sonner`. The page currently uses a custom `<div>` overlay instead of the app-standard `sonner` toast — a direct violation of coding rules. Also removes the `showToast()` wrapper function. | P1 | 30m | `parts/page.tsx` | ✅ Done |
| AI-008 | Upgrade `parts/new/page.tsx` to capture FAA 8130-3 / cert fields at receive time. Current form only collects partNumber, partName, qty, supplier — zero traceability data. A Part 145 receiving clerk MUST record cert type, cert number, serial number, condition, and receiving date. Upgrade form to use `api.parts.receivePart` (not `createPart`) when cert data is present, with collapsible sections for life-limited and shelf-life data. | P0 | 2h | `parts/new/page.tsx` | ✅ Done |
| AI-009 | Add Inspect action to `parts/requests/page.tsx` (Parts Queue). Parts in `pending_inspection` have no action button on this page — the clerk sees the part but can't act on it without navigating elsewhere. Add inline `InspectDialog` + Inspect button using `api.gapFixes.completeReceivingInspection`. | P1 | 1h | `parts/requests/page.tsx` | ✅ Done |

### Cycle 7 — 2026-02-28 (Testing Gaps)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-019 | Add E2E interaction tests for `DiscrepancyDispositionDialog` — verifies MEL deferral path (AI-015 fix) renders amber 14 CFR 91.213 warning, all 5 MEL fields (item number, category, deferral date, auth event ID), and that Submit button is disabled until required fields are filled. Also tests Corrected path requires corrective action text. Tests are data-resilient: `test.skip()` if no squawks exist in org. 9 tests total. | P1 | 1h | `e2e/wave5-discrepancy-disposition.spec.ts` (new) | ✅ Done |
| AI-020 | Add E2E interaction tests for Parts Receive form `/parts/new` — verifies 8130-3 cert section toggle exists and is collapsed by default (AI-008 fix), expanding it reveals Approval Number, Form Tracking Number, Approving Authority, Applicant Name, Authorized Signatory fields. Also tests partial cert data triggers validation error. Life-limited and shelf-life section toggles also tested. 11 tests total. | P1 | 1h | `e2e/wave5-parts-receive.spec.ts` (new) | ✅ Done |
| AI-021 | Add E2E interaction tests for billing/inventory pages — tests labor-kits "New Labor Kit" dialog (AI-016 fix): form opens, has Name + ATA Chapter fields, Save disabled until name filled, Cancel closes. Tests inventory-count reconcile uses shadcn AlertDialog NOT window.confirm (AI-017 fix): verifies `browserDialogTriggered = false` when clicking Reconcile. Documents remaining `confirm()` regression risk on delete/complete-count for next cycle. 8 tests total. | P1 | 1h | `e2e/wave5-billing-integrity.spec.ts` (new) | ✅ Done |

### Cycle 6 — 2026-02-28 (Error Handling)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-016 | Fix `billing/labor-kits/page.tsx` — `handleSave()`, `duplicateKit()`, `toggleKit()`, and `deleteKit()` all fire Convex mutations with zero error handling. No try/catch, no toast feedback, no submitting state (allows double-submit). Delete uses bare browser `confirm()`. Add: `import { toast } from "sonner"`, wrap all mutations in try/catch with `toast.success`/`toast.error`, add `isSubmitting` state to dialog save button, add Loader2 spinner. Also fix UI design to match app design system (header uses `text-2xl font-bold` instead of smaller standard). | P1 | 45m | `billing/labor-kits/page.tsx` | ✅ Done |
| AI-017 | Fix `parts/inventory-count/page.tsx` — `handleCreate()`, `handleStart()`, `completeCount()`, `reconcileCount()`, `deleteCount()`, and inline `recordItem()` in `onBlur` handlers all have zero error handling. `reconcileCount` is a **destructive irreversible action** (permanently adjusts stock quantities) gated only by a bare `confirm()` dialog. Add: `import { toast } from "sonner"`, wrap all mutations in try/catch with `toast.success`/`toast.error`, replace `confirm()` for reconcile with `AlertDialog` (shadcn/ui). Add loading states to action buttons. Also installed `alert-dialog` shadcn component (was missing). | P0 | 1h | `parts/inventory-count/page.tsx`, `components/ui/alert-dialog.tsx` (new) | ✅ Done |
| AI-018 | Fix `billing/credit-memos/page.tsx` — `handleIssue()` has a `catch` block that only calls `console.error()`. Error is silently swallowed, user sees no feedback when issuing a credit memo fails. Add `toast.success("Credit memo issued")` on success and `toast.error(...)` in catch. Add `issuingId` state to show loading indicator and prevent double-issue on the Issue button. | P1 | 20m | `billing/credit-memos/page.tsx` | ✅ Done |

### Cycle 10 — 2026-02-28 (Workflow Fidelity — 2nd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-028 | Wire `onStartClick` + `isStarting` in `tasks/[cardId]/page.tsx` to `api.gapFixes.startStep` — `TaskStepRow` has had a "Start" button since GAP-18 but the props were never passed from the parent page. Without this wire-up, the Start button never renders and techs can't mark steps as "in_progress". They go straight from pending to signed with no intermediate "actively working" state — no visibility for the DOM into what work is happening. Added `startStepMutation = useMutation(api.gapFixes.startStep)`, `startingStepId` state, `handleStartStep()` function, and passed `onStartClick={handleStartStep}` + `isStarting={startingStepId === step._id}` to every `TaskStepRow`. | P1 | 30m | `tasks/[cardId]/page.tsx` | ✅ Done |
| AI-029 | Fix silent handoff note failure in `tasks/[cardId]/page.tsx` — `catch` block was empty (`// Error will show in console; could add toast later`). When `addHandoffNote` fails (network error, Convex down, auth expiry), the note is silently dropped — the form resets, the tech thinks the note was saved, and the handoff communication is lost at 2 AM mid-job. Changed `catch` to `catch (err)` with `toast.error(err.message \|\| "Failed to add handoff note — please try again")`. | P1 | 5m | `tasks/[cardId]/page.tsx` | ✅ Done |
| AI-030 | Fix `WorkOrderHeader.tsx` "Sign Off & Close" button — linked to `/work-orders/${id}/signature` (PIN re-auth page that generates an event ID) instead of `/work-orders/${id}/rts` (the actual RTS authorization page). Same bug as AI-026 (fixed for QCM review) but present in the WO detail header used on every work order. When `canClose` was true and the button was clickable, it routed to the wrong page entirely. Changed `/signature` → `/rts`. | P0 | 5m | `_components/WorkOrderHeader.tsx` | ✅ Done |

### Cycle 9 — 2026-02-28 (Regulatory Compliance — 2nd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-025 | Fix `work-orders/[id]/release/page.tsx` — release form allows aircraft customer handback with zero verification that RTS was authorized. Under 14 CFR Part 145, an aircraft CANNOT be returned to a customer without a signed RTS record. Add `useQuery(api.returnToService.getCloseReadinessReport)` to the release page; show a red blocking card and disable the Release button when `!report.isAlreadySigned`. | P0 | 45m | `work-orders/[id]/release/page.tsx` | ✅ Done |
| AI-026 | Fix `compliance/qcm-review/page.tsx` `CloseReadinessPanel` — "Sign Off & Close" button links to `/work-orders/${selectedWoId}/signature` (the re-authentication page where you **get** an auth event) instead of `/work-orders/${selectedWoId}/rts` (the page where you **do** the actual RTS sign-off). Sending an IA to the signature page when they intend to complete RTS is a workflow error that wastes time and causes confusion. | P0 | 10m | `compliance/qcm-review/page.tsx` | ✅ Done |
| AI-027 | Fix `compliance/page.tsx` — groups aircraft into "Aircraft with Active Work Orders" vs "Other Fleet Aircraft" based on `openWorkOrderCount > 0`. Having an open work order is orthogonal to AD compliance status — a compliant aircraft can have an open WO, and a non-compliant one might not. Remove the open-WO grouping; show all fleet aircraft in a single list sorted by actual compliance status: non-compliant first, then due-soon, then pending review, then compliant. Rename section header to "Fleet AD Compliance Status". | P1 | 30m | `compliance/page.tsx` | ✅ Done |

### Cycle 8 — 2026-02-28 (Performance)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-022 | Eliminate duplicate Convex subscription in `WOStatusChart` — chart fires `api.workOrders.listWorkOrders` (200 items) independently, but the dashboard parent already holds all WO data via `getWorkOrdersWithScheduleRisk`. Add an optional `workOrders` prop; when supplied, skip the internal query. Pass `workOrdersWithRisk` from `dashboard/page.tsx`. Eliminates one redundant backend subscription on every dashboard load. | P1 | 30m | `dashboard/_components/WOStatusChart.tsx`, `dashboard/page.tsx` | ✅ Done |
| AI-023 | Replace O(6n) counts `useMemo` in `work-orders/page.tsx` — `counts` runs `filterWorkOrders` 6 separate times on the full WO array (once per tab). Replace with a single-pass counter loop: one iteration, 6 buckets incremented, same output. | P2 | 15m | `work-orders/page.tsx` | ✅ Done |
| AI-024 | Wire `TechUtilizationChart` to real data — chart renders 5 hardcoded fake techs/hours with a "Sample data" disclaimer. Wire to `api.timeClock.listTimeEntries` (this-week filter) + `api.technicians.list` to show actual hours logged per technician in the current ISO week. Add Skeleton loading state. Show "No time entries this week" empty state. | P1 | 1h | `dashboard/_components/TechUtilizationChart.tsx` | ✅ Done |

### Cycle 11 — 2026-02-28 (Parts Traceability — 3rd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-031 | Fix `rotables/page.tsx` — "Condemn" quick-action button fires immediately with zero confirmation. Condemning an aircraft component is irreversible: it writes the part off as airworthy-unfit and removes it from service permanently. A $20k–$200k part should not be written off with a single accidental click. Replace with `AlertDialog` (shadcn/ui) confirmation: "Condemn P/N XXXXXXX S/N XXXXXXX? This action cannot be undone." with a red confirm button. | P0 | 20m | `parts/rotables/page.tsx` | ✅ Done |
| AI-032 | Fix `loaners/page.tsx` — "Loan Out" dialog uses a raw text `Input` for Customer ID — the clerk must type a Convex document ID (e.g. `jd7abc123...`) which is completely unusable. Replace with `useQuery(api.customers.listCustomers)` dropdown so the clerk selects by customer name. Add loading state and "No customers" empty state. | P1 | 30m | `parts/loaners/page.tsx` | ✅ Done |
| AI-033 | Fix `parts/page.tsx` — part inventory cards have `cursor-pointer` class but clicking does nothing (no detail page exists, no `onClick` handler). Added `PartDetailSheet` slide-in panel showing: P/N, S/N, name, condition, location, supplier, received date, life-limit hours/cycles remaining (with colour-coded warnings), shelf-life expiry (with EXPIRED badge), 8130-3 on-file indicator, inspection result/notes, quarantine reason, and reservation status. Wired `onClick={() => setDetailPart(part)}` to every card. | P1 | 45m | `parts/page.tsx` | ✅ Done |

### Cycle 12 — 2026-02-28 (UI/UX Quality — 2nd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-034 | Fix `fleet/[tail]/logbook/page.tsx` Back button — routes to `/fleet` (fleet list) instead of `/fleet/${tailNumber}` (the aircraft detail page). A tech opens the logbook for N192AK, reads the records, hits Back, and lands on the fleet list instead of N192AK's detail tab. Navigation regression — user loses context. Change `to="/fleet"` → `to={\`/fleet/${encodeURIComponent(tailNumber)}\`}`. | P1 | 5m | `fleet/[tail]/logbook/page.tsx` | ✅ Done |
| AI-035 | Fix `fleet/[tail]/logbook/page.tsx` date range filter — uses raw HTML `<input type="date">` styled manually with `bg-background border border-border/60 rounded px-2 py-1 text-xs` instead of shadcn `Input` component. Raw date inputs have no dark-mode theming support — the text is invisible against dark backgrounds on many browsers. Replace both raw date inputs with shadcn `<Input type="date">` (same as used in reports/page.tsx). Also updated the "Clear" link from raw `<button>` to shadcn `<Button variant="ghost">`. | P1 | 10m | `fleet/[tail]/logbook/page.tsx` | ✅ Done |
| AI-036 | Fix `personnel/page.tsx` `ShiftEditor.handleSave()` — catch block contains `// Silently ignore — toast system can be added later`. That comment has been stale for months; `sonner` is installed and used throughout the app. When a shift save fails (network error, auth expiry, Convex down), the technician's Save button silently re-enables with no explanation. Add `import { toast } from "sonner"` to personnel page and replace the silent catch with `toast.error(err instanceof Error ? err.message : "Failed to save shift — please try again")`. | P1 | 10m | `personnel/page.tsx` | ✅ Done |

### Cycle 13 — 2026-02-28 (Data Integrity — 2nd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-037 | Fix `billing/time-approval/page.tsx` — Pending, Approved, and Rejected tabs all displayed raw Convex document IDs (`k7abc123def456…`) in the Technician and Work Order columns. A supervisor approving time entries had no way to know *whose* time they were approving or *which work order* it was against without a separate lookup. Added `useQuery(api.technicians.list)` + `useQuery(api.workOrders.listActive, { limit: 500 })` to the page, built `techMap` (id→legalName) and `woMap` (id→workOrderNumber), and replaced raw ID cells with `resolveTech()` / `resolveWO()` helpers. "Approved By" column in the Approved tab also resolved. Fallback: truncated ID if tech/WO not found (closed WOs outside listActive's coverage). | P0 | 30m | `billing/time-approval/page.tsx` | ✅ Done |
| AI-038 | Fix `work-orders/new/page.tsx` — submit button `disabled` check omitted `!workOrderNumber.trim()`. The Create Work Order button was enabled even with an empty Work Order Number field; the HTML `required` attribute provided the only gate. A browser form-submission bypass (or race condition) could create a WO with a blank `workOrderNumber`, violating INV-14 (unique non-empty WO number per org). Added `!workOrderNumber.trim()` to the disabled expression so the button is grayed out whenever the number field is blank. | P1 | 5m | `work-orders/new/page.tsx` | ✅ Done |
| AI-039 | Fix `billing/invoices/[id]/page.tsx` payment dialog — no guard prevented recording a payment larger than the invoice's outstanding balance. A billing clerk could record $50,000 against a $500 invoice with zero friction. Added: (1) client-side overpayment check in `handlePayment()` with clear error message; (2) `max={invoice.balance}` on the Amount input; (3) a persistent Outstanding Balance summary row in the dialog header; (4) "Pay Full Balance" convenience button that auto-fills the exact balance amount — eliminates manual entry errors on full payments. | P1 | 30m | `billing/invoices/[id]/page.tsx` | ✅ Done |

### Cycle 16 — 2026-02-28 (Performance — 2nd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-046 | Fix `billing/invoices/page.tsx` O(5n) inline render computations — `counts` ran 5 separate `.filter()` passes on the full invoices array on every render (checkbox toggle, search keystroke, tab switch). Replaced with a single-pass `useMemo` counter loop (O(n), only recomputes when `invoices` changes). Also memoized `allFilteredIds`, `{ allSelected, someSelected }`, and `{ selectedInvoices, selectedDraftIds }` with `useMemo`. Converted `toggleSelectAll` and `toggleSelect` to `useCallback`. Net result: selection interactions and search no longer trigger invoice re-aggregation. | P2 | 20m | `billing/invoices/page.tsx` | ✅ Done |
| AI-047 | Fix `work-orders/kanban/page.tsx` — `WoCard` had no `React.memo` wrapper and `onDragStart` was a new function ref on every render. During drag-and-drop operations, `dragOverColumn` and `draggedWoId` state changes (which happen on every `dragover` event) caused ALL kanban cards to re-render even though no card data changed. Wrapped `WoCard` in `memo()` and converted `handleDragStart` to `useCallback([])`. Cards now skip re-render unless their `wo` data or `onDragStart` reference changes. | P2 | 15m | `work-orders/kanban/page.tsx` | ✅ Done |
| AI-048 | Fix `reports/page.tsx` inline `reduce` aggregations — `totalRevenue` and `totalWOs` were bare `Array.reduce` calls in the render body, running on every keystroke in the date-range inputs (even while `revenueData` and `throughputData` were already properly `useMemo`-ized). Wrapped both in `useMemo` with their respective data arrays as deps. Aggregations now only recompute when the chart data changes, not on every date character typed. | P2 | 10m | `reports/page.tsx` | ✅ Done |

### Cycle 15 — 2026-02-28 (Testing — 2nd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-043 | Add E2E tests for RTS regulatory gate on aircraft release page (AI-025 fix). Verifies: release page loads, RTS blocking card renders with "Return-to-Service Not Authorized" when RTS unsigned, 14 CFR Part 145 reference present in card, "Go to RTS Authorization" link routes to `/rts` NOT `/signature` (also covers AI-026/AI-030 routing regression), Release button is disabled without RTS signed, form requires aircraft total time, QCM review page loads clean. 9 tests total; data-resilient (`test.skip()` when no WOs exist). | P0 | 1h | `e2e/wave6-rts-release-gate.spec.ts` (new) | ✅ Done |
| AI-044 | Add E2E tests for invoice payment overpayment guard (AI-039 fix). Verifies: invoices list loads, Record Payment dialog opens, dialog shows "Outstanding Balance" summary row, amount input has `max` attribute set to invoice balance, "Pay Full Balance" button auto-fills exact balance amount, payment method selector present, Cancel closes dialog. 10 tests total; data-resilient (`test.skip()` when no invoices exist or invoice already paid). | P1 | 1h | `e2e/wave6-invoice-payment-guard.spec.ts` (new) | ✅ Done |
| AI-045 | Add E2E safety regression tests covering three AI fixes: (1) Rotables Condemn button uses shadcn AlertDialog not bare window.confirm (AI-031) — verifies no `dialog` event fires, AlertDialog shows "Condemn Component?" + regulatory language + Cancel to abort; (2) Parts inventory cards open PartDetailSheet on click (AI-033) — verifies cursor-pointer cards exist, clicking opens `[role='dialog']` panel with P/N/part info; (3) New WO "Create Work Order" button disabled when WO Number empty (AI-038) — verifies button is disabled by default, WO number label visible. 12 tests total. | P1 | 1h | `e2e/wave6-parts-wo-safety.spec.ts` (new) | ✅ Done |

### Cycle 14 — 2026-02-28 (Error Handling — 2nd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-040 | Fix `settings/locations/page.tsx` `handleDeactivate()` — uses bare `confirm("Deactivate this location?")`. A Part 145 MRO certificate location should never be deactivated with a single-click native browser confirm. If the location has an active Part 145 cert number, deactivating it could disqualify work performed there. Replace with shadcn `AlertDialog` that shows location name and cert number in the warning, with a red destructive confirm button and an `isDeactivating` loading state on the button. | P1 | 20m | `settings/locations/page.tsx` | ✅ Done |
| AI-041 | Fix `parts/inventory-count/page.tsx` — two remaining bare `confirm()` calls documented as known regression risk in AI-021 e2e test: (1) "Complete this count? No more items can be counted after this." on the Complete Count button — replace with an `AlertDialog` (already imported) with `completeConfirmOpen` state; (2) "Delete this draft count?" on the row Trash button — replace with an `AlertDialog` with `deleteConfirmId` state. Completing a count locks the record; deleting removes draft history. Both deserve proper modal confirmation. | P1 | 30m | `parts/inventory-count/page.tsx` | ✅ Done |
| AI-042 | Fix `DocumentAttachmentPanel.tsx` `handleDelete()` — uses bare `confirm("Delete this document? This cannot be undone.")`. Documents attached to work orders under 14 CFR 43.9(a)(4) are part of the legal maintenance record. Accidental deletion of an AMM reference, 8130-3 scan, or SB compliance document cannot be undone. Replace bare `confirm()` with shadcn `AlertDialog` showing document name and type in the warning, with `deleteConfirmId` state to identify which doc is being confirmed. `AlertDialog` component is not imported — add import from `@/components/ui/alert-dialog`. | P0 | 25m | `work-orders/[id]/_components/DocumentAttachmentPanel.tsx` | ✅ Done |

### Cycle 18 — 2026-02-28 (Workflow Fidelity — 3rd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-052 | Fix `tasks/new/page.tsx` silently dropping `aircraftSystem` and `isInspectionItem` — both are captured in form state and shown in `TaskCardForm`, but never passed to `createTaskCard()` in `handleSubmit`. Both fields exist in the Convex mutation schema (confirmed in `convex/taskCards.ts` lines 194–207). Without `aircraftSystem`, task cards always store `undefined` for system, breaking the WorkItemsList system filter. Without `isInspectionItem`, the inspection flag is always false regardless of what the tech marked. | P1 | 10m | `work-orders/[id]/tasks/new/page.tsx` | ✅ Done |
| AI-053 | Fix `WorkItemsList.tsx` "Log Squawk" action button — button renders at the bottom of the work items list but has no `onClick` handler. Same bug as AI-006 (fixed for `DiscrepancyList.tsx`) but this component was missed. Add optional `orgId`, `techId`, `aircraftCurrentHours` props to `WorkItemsList`; import `LogSquawkDialog`; wire button to open it. Update `work-orders/[id]/page.tsx` to pass these props. | P1 | 30m | `work-orders/[id]/_components/WorkItemsList.tsx`, `work-orders/[id]/page.tsx` | ✅ Done |
| AI-054 | Fix `FindingRow` in `WorkItemsList.tsx` — squawk/finding rows are inert `<div>` elements with no click handler or navigation. Task cards link to their detail page; findings do nothing. A tech reviewing work items needs to be able to click a finding to open `DiscrepancyDispositionDialog` and disposition it inline. Add `onDisposition` callback prop to `FindingRow`, add `dispositionState` to `WorkItemsList`, import `DiscrepancyDispositionDialog`, wire everything up. Update `work-orders/[id]/page.tsx` to pass `workOrderId` as `Id<"workOrders">` (already available). | P1 | 30m | `work-orders/[id]/_components/WorkItemsList.tsx` | ✅ Done |

### Cycle 17 — 2026-02-28 (Regulatory Compliance — 3rd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-049 | Fix `work-orders/[id]/certificates/page.tsx` — `handleCreate()` uses `try/finally` with NO `catch`. If Convex throws (auth error, duplicate cert number, validation failure), the dialog sits frozen with the button re-enabled and the 8130-3/EASA Form 1 is silently not created. `handleDownload()` has zero try/catch — PDF failure is completely invisible. These are Part 145 regulatory release certificates. Add `import { toast } from "sonner"`, convert to proper `try/catch/finally`, add `toast.success("Certificate created")` on success and `toast.error(...)` in catch for both operations. Also fix page header typography to match app design system. | P0 | 20m | `work-orders/[id]/certificates/page.tsx` | ✅ Done |
| AI-050 | Fix `records/_components/CreateRecordForm.tsx` — correction record "Record Being Corrected" field is a raw `<Input placeholder="Maintenance record Convex ID…">`. A tech doing a 14 CFR 43.9(b) correction entry must look up and type a cryptic database ID like `k7abc123def456`. Replace with `useQuery(api.maintenanceRecords.listForWorkOrder)` dropdown showing `#seq — description (date)` so techs select the record to correct by human-readable data. The `workOrderId` prop is already available. | P1 | 30m | `work-orders/[id]/records/_components/CreateRecordForm.tsx` | ✅ Done |
| AI-051 | Fix `compliance/audit-trail/page.tsx` — AD compliance only shows one aircraft at a time; FAA ASI walk-in requires fleet-level AD status instantly. Add "Fleet Overview" section above the individual aircraft selector, showing all fleet aircraft in a summary table: tail number, make/model, AD total count, overdue count, due-soon count, overall status badge. Clicking a row sets `selectedAircraftId` to drill into that aircraft. Sorted: non-compliant first, then due-soon, then compliant. Allows FAA inspector to see entire fleet AD status in one view. | P1 | 30m | `compliance/audit-trail/page.tsx` | ✅ Done |

### Cycle 20 — 2026-02-28 (UI/UX Quality — 3rd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-058 | Fix `settings/users/page.tsx` design system violations — header used `text-2xl font-bold tracking-tight` (non-standard; should be `text-lg sm:text-xl md:text-2xl font-semibold text-foreground`), `w-6 h-6` icon (standard: `w-5 h-5`), description `text-muted-foreground mt-1` (standard: `text-sm text-muted-foreground mt-0.5`). Added `max-w-4xl` page width constraint. Changed root `space-y-6` → `space-y-5`. Added `border-border/60` to both cards. Fixed dead "Invite User" button — added `onClick` calling `toast.info("To invite users, go to your Clerk organization dashboard")`. Fixed Role Legend items `bg-muted/30` → `bg-muted/20 border-border/50`. | P1 | 20m | `settings/users/page.tsx` | ✅ Done |
| AI-059 | Fix `fleet/predictions/page.tsx` design system violations — header used `text-2xl font-bold` (non-standard), `text-muted-foreground text-sm` (should be `text-sm text-muted-foreground mt-0.5`). Root `space-y-6` → `space-y-5`. Generate button sized to `size="sm" h-9`. Summary grid `grid-cols-2 md:grid-cols-5` → `grid-cols-2 sm:grid-cols-3 md:grid-cols-5` for graceful mobile→tablet→desktop transition. "Next Due" card `border-primary/20` → `border-border/60`. Fixed hardcoded `acknowledgedBy: "current_user"` — added `const { techId } = useCurrentOrg()` and changed to `acknowledgedBy: techId ?? "unknown"` so audit trail records the actual technician ID. Fixed empty state from plain text to proper two-line `text-sm font-medium` + `text-xs text-muted-foreground/60` pattern. | P1 | 30m | `fleet/predictions/page.tsx` | ✅ Done |
| AI-060 | Fix `settings/shop/page.tsx` timezone field — raw `<Input>` where user typed IANA timezone strings (e.g. `America/Denver`). A typo silently causes scheduling calculation errors with no visual indicator. Replaced with `<Select>` backed by `AVIATION_TIMEZONES` array of 18 common IANA zones covering all US regions + Canada + UTC. Added helper text: "Used for scheduling calculations, shift times, and regulatory record timestamps." Also fixed Quick Links section — `<a href="/billing/tax-config">` and `<a href="/billing/settings">` were native anchor tags (page reload on click); replaced with `import { Link } from "react-router-dom"` and `<Link to="...">` for SPA navigation. | P1 | 20m | `settings/shop/page.tsx` | ✅ Done |

### Cycle 19 — 2026-02-28 (Parts Traceability — 3rd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-055 | Fix `parts/cores/page.tsx` `CoreDetailDialog` — `handleAction()` wraps all four status transitions (markReceived, markInspected, issueCredit, scrap) in `try/finally` with **no catch**. When any action fails (network error, Convex down, auth expiry), the dialog closes silently — user assumes success while backend actually failed. The `Scrap` button fires with **zero confirmation** — scrapping a core is an irreversible financial write-off (cores range $1k–$50k+). Add `import { toast } from "sonner"`, add `catch (err)` with `toast.error()` to `handleAction`, add `toast.success()` per action, replace Scrap button with `AlertDialog` showing core P/N + core value. | P0 | 30m | `parts/cores/page.tsx` | ✅ Done |
| AI-056 | Fix `parts/cores/page.tsx` `CreateCoreDialog` — `handleSubmit()` uses `try/finally` with **no catch**. If `createCoreReturn` throws, the dialog stays open, saving re-enables, and the user sees nothing. Also: `partId` is hardcoded as `"" as Id<"parts">` with a comment "Will be linked properly in production" — this silently passes an empty string to the backend, creating orphaned core records with no part link. Replace with `useQuery(api.parts.listParts)` optional dropdown (mark as optional — not all cores have existing part records). Add `catch`/`toast.error`. | P1 | 30m | `parts/cores/page.tsx` | ✅ Done |
| AI-057 | Fix `parts/shipping/page.tsx` Cancel Shipment — the Trash icon button calls `handleStatusUpdate(s._id, "cancelled")` immediately with no confirmation. Cancelling an outbound shipment could trigger compliance issues if parts were already picked, staged, or shipped. Also, `removeItem` in `ShipmentDetails` fires immediately with no confirmation — deleting a shipment item has no undo. Add `AlertDialog` for Cancel Shipment (shows shipment number + type) and inline confirmation state for Remove Item. `alert-dialog` component already installed from AI-017. | P1 | 30m | `parts/shipping/page.tsx` | ✅ Done |

### Cycle 23 — 2026-02-28 (Testing Gaps — 2nd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-067 | Add E2E tests for `compliance/audit-trail` Fleet Overview panel (AI-051 fix). Verifies: page loads without error, "Fleet Overview" panel is present above aircraft selector, "Click a row to drill in" guidance text present, "Non-compliant first" ordering language present, aircraft rows render or empty state renders cleanly, clicking a row loads AD detail for that aircraft, individual aircraft selector is present below fleet overview, "All N fleet aircraft" count text present, aircraft selection does not crash page. 9 tests total; data-resilient (test.skip() when fleet is empty). | P1 | 1h | `e2e/wave7-audit-trail-fleet.spec.ts` (new) | ✅ Done |
| AI-068 | Add E2E tests for `billing/time-clock` double-clock-in guard and search box fix (AI-061 fix). Verifies: page loads without error, "Time Clock" heading present, "Clock In" button visible, search technician input is present and accepts text (was a ghost UI element before), Clock In dialog opens, dialog has Technician dropdown, dialog has Work Order dropdown, dialog has Notes field, dialog dismisses cleanly via Escape, amber "Already clocked in!" warning appears when an active tech is selected. 10 tests total; data-resilient (double-clock-in warning skipped when no active entries). | P1 | 1h | `e2e/wave7-time-clock-guard.spec.ts` (new) | ✅ Done |
| AI-069 | Add E2E tests for `billing/quotes/[id]` data integrity send guards (AI-063 fix). Verifies: quotes list and detail pages load clean, Send button present on DRAFT quotes, clicking Send on an empty-line-items DRAFT shows "no line items" error, Edit dialog appears on pencil click, qty=0 shows "Quantity must be greater than zero" error, discount=150 shows "Discount must be between 0 and 100%" error, Cancel closes dialog without side effects. 10 tests total; data-resilient (test.skip() when no DRAFT quotes or no line items). | P1 | 1h | `e2e/wave7-quote-send-guards.spec.ts` (new) | ✅ Done |

### Cycle 22 — 2026-02-28 (Error Handling — 3rd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-064 | Fix `billing/otc/page.tsx` — `handleCompleteSale()` uses `try/finally` with no `catch`. If `createSale` throws (duplicate receipt number, inventory conflict, auth expiry), the cashier sees nothing — no error feedback, no indication the transaction failed, but the cart IS cleared (cart reset runs inside `try`). Customer walks away without a record. ALSO: `handleVoid()` has ZERO try/catch — `voidSale` is called bare with `await`. If voiding fails, the void dialog closes (no state reset) and the void is silently dropped. Add `import { toast } from "sonner"`, add `catch (err)` with `toast.error()` to `handleCompleteSale()` (and guard cart reset to after successful sale), add `try/catch` to `handleVoid()` with `toast.success("Sale voided")` on success and `toast.error()` on failure. Add `isVoiding` state to prevent double-void. | P0 | 20m | `billing/otc/page.tsx` | ✅ Done |
| AI-065 | Fix `billing/warranty/page.tsx` — `CreateClaimDialog.handleSubmit()` uses `try/finally` with no `catch`. Silent failure on warranty claim creation. `ClaimDetailDialog.handleAction()` also uses `try/finally` with no `catch` — submit, approve, deny, and close all fail silently. Warranty adjudication actions have financial and legal consequences (approved amounts, resolution records). Add `import { toast } from "sonner"` to both components; add `catch (err)` blocks with `toast.error(err instanceof Error ? err.message : "...")` to both handlers. Add `toast.success()` for each successful action (created, submitted, approved, denied, closed). | P1 | 15m | `billing/warranty/page.tsx` | ✅ Done |
| AI-066 | Fix `billing/settings/page.tsx` — two inline async `onClick` handlers without try/catch: (1) Remove currency button calls `await updateCurrencySettings(...)` with zero error handling — removing a supported currency silently fails, no toast; (2) Delete exchange rate button calls `await deleteRate(...)` followed by `toast.success("Rate deleted.")` with no try/catch — if deletion fails, the success toast is never called but no error toast shows either, user cannot tell if the rate was deleted. Add try/catch with `toast.success/toast.error` to both inline handlers. | P1 | 15m | `billing/settings/page.tsx` | ✅ Done |

### Cycle 24 — 2026-02-28 (Performance — 3rd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-070 | Eliminate redundant `api.aircraft.list` full-fleet subscription in `fleet/[tail]/page.tsx` — page fires the entire fleet list query solely to read `enrichedAircraft.openWorkOrderCount` (used only as a tab badge number). The `workOrders` query for this aircraft is already loaded; `openWoCount` can be derived as `activeWOs.length` (active statuses already filtered). Remove `fleetList = useQuery(api.aircraft.list, ...)` and `enrichedAircraft` lookup entirely. Also wrap `activeWOs`, `plannedWOs`, and `pastWOs` in `useMemo([workOrders])` — they're computed inline on every render (including dialog open/close state changes). Net: removes one full-fleet Convex subscription + O(3n) inline array filters per render for all aircraft detail views. | P2 | 20m | `fleet/[tail]/page.tsx` | ✅ Done |
| AI-071 | Wrap `counts` in `useMemo` in `billing/purchase-orders/page.tsx` — six `.filter()` calls (one per status bucket: all/DRAFT/SUBMITTED/PARTIAL/RECEIVED/CLOSED) are computed inline on every render, including on every search keystroke. The `filtered` array is already `useMemo`-ized but `counts` is not. Memoize with `[pos]` dependency; badge counts only recompute when Convex data changes, not on search input. Also convert `tabs` array constant from inline render allocation to a module-level constant — it never changes. | P2 | 10m | `billing/purchase-orders/page.tsx` | ✅ Done |
| AI-072 | Convert `getTechName()` and `getWONumber()` from O(n) linear `.find()` scans in `billing/time-clock/page.tsx` to O(1) `Map` lookups. These are called once per row for each entry in the active-entries card, the daily-summary card, and the full entries table. With 100 entries × 20 techs each call scans up to 20 items — needlessly. Build `techMap = useMemo(() => new Map(technicians.map(t => [t._id, t.legalName])), [technicians])` and `woMap = useMemo(() => new Map(workOrders.map(w => [w._id, w.workOrderNumber])), [workOrders])`. Also wrap `openWorkOrders` (inline filter computed in the render body after the loading guard, used for the Clock In dialog dropdown) in `useMemo([workOrders])`. | P2 | 15m | `billing/time-clock/page.tsx` | ✅ Done |

### Cycle 21 — 2026-02-28 (Data Integrity — 3rd pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-061 | Fix `billing/time-clock/page.tsx` — double clock-in guard: `handleClockIn()` has no check for whether the selected technician is already clocked into another work order. Under Part 145 labor tracking, a tech cannot log hours against two work orders simultaneously — creates fraudulent labor records and billing discrepancies. Add inline warning in clock-in dialog showing existing active entry (WO number + start time) when selected tech is already active. Require user to explicitly proceed or clock out first. ALSO fix dead `searchTech` filter: `searchTech` state is populated from a Search input but never applied to `filteredEntries` useMemo — the search box is a ghost UI element. Wire it to filter entries by tech name using `getTechName`. | P0 | 30m | `billing/time-clock/page.tsx` | ✅ Done |
| AI-062 | Fix `billing/purchase-orders/[id]/page.tsx` `handleReceive()` — no client-side validation that each entered quantity ≤ remaining quantity (`item.qty - item.receivedQty`). The `max` attribute on the input can be bypassed via dev tools or by typing past browser validation. Receiving 100 units on a PO line for 10 creates phantom inventory — non-existent parts enter the parts system and could be issued to a maintenance task. Add validation loop in `handleReceive()` that checks each receipt quantity against the item's remaining quantity and shows a specific error: "Cannot receive X for [description] — only Y remaining on this PO." | P1 | 15m | `billing/purchase-orders/[id]/page.tsx` | ✅ Done |
| AI-063 | Fix `billing/quotes/[id]/page.tsx` — three data integrity gaps: (1) `handleSend()` allows sending a quote with 0 line items: `canSend` only checks `status === "DRAFT"`, but a user who removes all line items from a draft can still click Send, delivering a blank $0 quote to the customer. Add guard: `if (quote.lineItems.length === 0) { setError("Cannot send a quote with no line items."); return; }`. (2) `handleEditItem()` allows setting qty=0 — zero-qty line items render as $0 on the quote but clutter the line item list and can't be sent as legitimate charges. Add: `if (editQty !== "" && parseFloat(editQty) <= 0) { setError("Quantity must be greater than zero."); return; }`. (3) `handleEditItem()` has no range validation on discount percent — nothing prevents setting discount=150% (line item total goes negative). Add: `if (editDiscountPct !== "" && (parseFloat(editDiscountPct) < 0 \|\| parseFloat(editDiscountPct) > 100)) { setError("Discount must be between 0 and 100%."); return; }`. | P1 | 20m | `billing/quotes/[id]/page.tsx` | ✅ Done |

### Cycle 25 — 2026-02-28 (Regulatory Compliance — 4th pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-073 | Fix `records/_components/CreateRecordForm.tsx` — `handleSubmit()` validates technician, auth event, and approved data fields but **never enforces the 50-character minimum on `workPerformed`**. The amber counter is cosmetic only. A tech can submit with 3 words and create a legally deficient 14 CFR 43.9 record. Add `if (form.workPerformed.trim().length < 50) { setError("Work description must be at least 50 characters per AC 43-9C. Current: X chars."); return; }` before the `setIsSubmitting(true)` call. Also add `if (!form.workPerformed.trim()) { setError("Work performed description is required."); return; }` as a separate leading guard. | P0 | 15m | `work-orders/[id]/records/_components/CreateRecordForm.tsx` | ✅ Done |
| AI-074 | Fix `rts/_components/RtsSignoffForm.tsx` — no real-time warning when entered aircraft hours < current hours on file. User types a lower value, all 9 preconditions visually show PASS/PENDING, they click Authorize, the backend fires, and only then do they get PRE-3 fail ("RTS_AIRCRAFT_TIME_DECREASED"). This is a form UX gap: the frontend knows `currentHoursOnFile` and the entered value at all times. Add inline warning card below the hours input: `if (enteredHours < currentHoursOnFile) → amber warning "Entered hours (X) are less than aircraft time on file (Y.Y). PRE-3 will fail."`. Surface this before the user clicks Authorize, not after. | P1 | 20m | `work-orders/[id]/rts/_components/RtsSignoffForm.tsx` | ✅ Done |
| AI-075 | Remove dead zombie `<RecordsList>` from `records/page.tsx` — bottom of the file renders the legacy `RecordsList` component inside `<div className="hidden">` with the comment "Legacy Records List (keeping for compatibility)". This invisible React subtree renders on every table-view render with no search query active, wasting reconciliation cycles and confusing every future developer who reads the file. The new `filteredFullRecords` table and timeline views replaced it entirely. Remove the zombie `hidden` div and the `RecordsList` import. Also removed unused `hashMap` construction, `ShieldCheck`, `Wrench`, `Eye` icon imports. | P2 | 5m | `work-orders/[id]/records/page.tsx` | ✅ Done |

### Cycle 26 — 2026-02-28 (Workflow Fidelity — 4th pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-076 | Fix `TaskCardList.tsx` — no "Awaiting Sign-Off" visual state. When `completedSteps === totalSteps && totalSteps > 0 && tc.status !== "complete"`, the badge still reads "In Progress." A shift supervisor or IA has no way to triage at a glance which task cards are sitting idle waiting for a certifier vs which still have active step work. Add a distinct amber "Awaiting Sign-Off" badge with a `Lock` icon for this state — immediately visible on the WO detail task tab. | P1 | 15m | `work-orders/[id]/_components/TaskCardList.tsx` | ✅ Done |
| AI-077 | Fix `CustomerStatusControl.tsx` — setting customer-facing status to "completed" has no RTS gate. A single dropdown selection on the WO detail page can mark an aircraft "Completed" for the customer with zero verification the RTS was authorized. Under Part 145, an aircraft is not "completed" unless RTS was signed per 14 CFR 91.407. Add a `toast.warning` with a "Go to RTS →" action link when operator selects "completed", reminding them to complete the RTS workflow before telling the customer the aircraft is done. | P0 | 20m | `work-orders/[id]/_components/CustomerStatusControl.tsx` | ✅ Done |
| AI-078 | Fix `tasks/[cardId]/page.tsx` Sign Card button — enabled as soon as all steps are done, regardless of compliance item status. If any compliance items are `non_compliant` or `pending`, the task card cannot legally be signed off — those items must be resolved first (the AD or SB hasn't been complied with, or the status is unknown). Block `allStepsDone` from enabling the Sign Card button when any compliance items are non-compliant or pending. Show a descriptive message: "X compliance item(s) require resolution before this card can be signed." | P0 | 20m | `work-orders/[id]/tasks/[cardId]/page.tsx` | ✅ Done |

### Cycle 28 — 2026-03-01 (UI/UX Quality — 4th pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-082 | Fix `personnel/training/page.tsx` design system violations — header uses `text-2xl font-bold tracking-tight flex items-center gap-2` (non-standard) with `h-6 w-6` icon (standard: `w-5 h-5`); description `text-muted-foreground text-sm mt-1` (standard: `text-sm text-muted-foreground mt-0.5`); outer wrapper `flex flex-col gap-6 p-4 md:p-6 max-w-7xl mx-auto` adds padding/max-width the AppLayout already handles — replace with `space-y-5`; button icons use `h-4 w-4 mr-2` (standard: `w-4 h-4` with `gap-1.5` on button); stat cards missing `border-border/60`; `Root space-y-4` in tabs → `space-y-3`. Also add `isSubmitting` state to both `handleAddRecord()` and `handleCreateReq()` — buttons currently allow double-submit with no loading indicator. | P1 | 25m | `personnel/training/page.tsx` | ✅ Done |
| AI-083 | Fix `scheduling/bays/page.tsx` design system violations — header uses `text-2xl font-bold tracking-tight` (non-standard; should be `text-lg sm:text-xl md:text-2xl font-semibold text-foreground`) with `text-sm text-muted-foreground mt-1` (standard: `text-sm text-muted-foreground mt-0.5`); Add Bay button missing `size="sm" h-9 gap-1.5` standard action button pattern; bay cards use `border-border/50` (standard: `border-border/60`) and lack hover shadow; `BayDetailPanel` close button uses bare `✕` text character instead of `<X className="w-4 h-4" />` from lucide-react — raw unicode chars don't respect design tokens; `handleRelease()` has zero loading state — a bay release that fails (e.g. active work order still assigned) is silent; add `isReleasing` state with Loader2 spinner. | P1 | 20m | `scheduling/bays/page.tsx` | ✅ Done |
| AI-084 | Fix `App.tsx` git merge conflict — `<<<<<<< HEAD` / `=======` / `>>>>>>> a698b1f2` markers were present, causing 14 TypeScript errors. The conflict was between HEAD (which added `/reports/financials*` sub-routes) and branch `a698b1f2` (which added `/scheduling*` routes). Correct resolution: include both scheduling routes (properly nested) AND the financials sub-routes. Fixed by removing conflict markers and preserving all routes. Pre-existing before this cycle — fixed as prerequisite. | P0 | 5m | `App.tsx` | ✅ Done |

### Cycle 27 — 2026-02-28 (Parts Traceability — 4th pass)

| ID | Description | Priority | Est | Files Affected | Status |
|----|-------------|----------|-----|----------------|--------|
| AI-079 | Fix `parts/tools/page.tsx` design system violations — header uses `text-2xl font-bold tracking-tight` (non-standard; should be `text-lg sm:text-xl md:text-2xl font-semibold text-foreground`), `h-6 w-6` icon (standard: `w-5 h-5`), `text-muted-foreground text-sm mt-1` (standard: `text-sm text-muted-foreground mt-0.5`). Root `gap-6` → `gap-5`. Add Tool button sized to `size="sm" h-9`. Stats grid from `gap-4` → `gap-3 sm:gap-4`. All four stat cards use hardcoded color literals (`text-green-600`, `text-blue-600`, `text-amber-600`, `text-purple-600`) instead of semantic design system tokens. | P1 | 15m | `parts/tools/page.tsx` | ✅ Done |
| AI-080 | Memoize stat card inline `.filter()` calls in `parts/tools/page.tsx` — three `(allTools ?? []).filter(...)` calls inline in the render body recompute on every render event (search keystroke, dialog open/close, tab switch). Replace with a single-pass `useMemo(toolStats, [allTools, calibrationDue])` that computes `available`, `inUse`, and `outForCal` counts in one O(n) pass. `calibrationDue` count already from Convex query; derive from that. | P2 | 10m | `parts/tools/page.tsx` | ✅ Done |
| AI-081 | Add Work Order field to Check Out dialog in `parts/tools/page.tsx` — `checkOutTool` mutation accepts optional `workOrderId` but the UI omits it entirely. Under Part 145 tool control requirements, a calibrated torque wrench or precision test equipment checked out for maintenance must be traceable to the specific work order. If it goes missing or is later found to be out of calibration, you must be able to identify which aircraft/task it was used on. Add optional WO dropdown to the Check Out dialog using `api.workOrders.listActive`. | P1 | 20m | `parts/tools/page.tsx` | ✅ Done |

---

## Bug Hunter Fixes

### Cycle 3 — 2026-03-01 (Persona: Parts Clerk)
Journey traced: Parts Queue → Receiving → POs → Shipping → Rotables → Loaners

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-007 | `handleLoanOut()` fires `loanOut({ customerId: loanForm.customerId as Id<"customers"> })` even when no customer is selected (`loanForm.customerId === ""`). An empty string cast to `Id<"customers">` is sent to the backend — either a confusing Convex schema error or a corrupted loaner record with no customer linkage. | `parts/loaners/page.tsx` | Added `disabled={!loanForm.customerId}` to the Loan Out submit button. Button is now visually disabled until a customer is selected from the dropdown. | Parts clerk clicks Loan Out before selecting a customer and gets a cryptic backend error instead of a clear disabled state. |
| BUG-008 | Parts in `removed_pending_disposition` had **no action button** — the action column rendered `null` for these items. The orange left-border card said "awaiting disposition" but there was nothing to click. A parts clerk had a queue of actionable items with no way to act on them. | `parts/requests/page.tsx` | Added "Disposition via WO →" button (orange-styled link) for removed parts with a linked `receivingWorkOrderId`. When no WO is linked, shows "No WO linked" placeholder. The WO link was already visible in the detail area but wasn't surfaced as the primary CTA. | Clerk sees 3 removed parts with orange warning borders and no way to proceed without navigating manually. Now one-click to the WO where disposition happens. |
| BUG-009 | Shipping page "Create Shipment" button had zero loading state — double-clicking created duplicate shipments silently. Also the page header used non-standard design system tokens: `text-2xl font-bold` (vs `text-lg sm:text-xl md:text-2xl font-semibold text-foreground`), `h-6 w-6` icon (vs `w-5 h-5`), description missing `text-sm` prefix. "New Shipment" button used full default height instead of `size="sm" h-9 gap-1.5`. | `parts/shipping/page.tsx` | Added `isCreating` state, `setIsCreating(true/false)` in `handleCreate()` with `finally` guard, `disabled={isCreating}` + `Loader2` spinner on button. Added `(v) => { if (!isCreating) setShowCreate(v) }` guard to prevent dialog close during submit. Fixed all header design system violations. | Parts clerk double-clicks "Create Shipment" and creates two identical outbound shipments for the same parts — inventory confusion and audit trail issue. |
| BUG-010 | Rotables "Remove" quick-action button fired `recordAction(..., "removed")` immediately with zero confirmation. Removing a component from aircraft is a significant act — a click misfire creates a spurious removal record for a part that's still installed. This is the same category of error as the Condemn button (fixed in AI-031) but was missed on Remove. Also: all quick-action buttons (Install, Remove, Send to Vendor, Receive) had no loading state — could be double-clicked during Convex call. | `parts/rotables/page.tsx` | Added `removeTarget` state + `AlertDialog` with same pattern as the existing Condemn AlertDialog — shows P/N + S/N with "Confirm Removal" button. Added `actioningId` state to `handleAction()` so all quick-action buttons disable with `Loader2` spinner while Convex call is in-flight. Added `Loader2` to lucide-react imports. | Click misfire on "Remove" creates a false removal history entry for an installed $50k turboprop engine. DOM can't tell if the part is actually still installed. |

### Cycle 1 — 2026-03-01 (Persona: Lead Technician)
Journey traced: My Work → Task Cards → Steps → Sign-off → Raise Finding → Handoff Notes

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-003 | `RaiseFindingDialog` Submit button enabled even when required `discrepancyType` and `systemType` fields were empty. Clicking fired handleSubmit which caught the missing fields and showed an error toast — but the button appeared clickable, making the UI feel broken. | `tasks/[cardId]/_components/RaiseFindingDialog.tsx` | Added `!discrepancyType \|\| !systemType` to button's `disabled` prop. Button is now visually grayed until all required classification fields are filled. | Tech fills description, clicks Raise Finding, gets mysterious error instead of clear disabled state. Frustrating mid-inspection. |
| BUG-004 | My Work page showed task cards in arbitrary query order with no urgency sorting. An overdue AOG task card could be card #7 in the list while a pending low-priority card was #1. | `my-work/page.tsx` | Added sort: overdue → at_risk → no_date (schedule risk tier), then in_progress → pending → complete/voided within tier. Also fixed `totalPendingSteps` KPI stat to exclude voided/complete cards — was inflating the "steps remaining" count. | Lead tech opens My Work at shift start and has to hunt for the hot job. With sort, AOG cards are always at top. |
| BUG-005 | `SignStepDialog` had `showPartsForm` state (set to true when parts added, set to true on barcode scan) but it was never **read** — it never conditionally rendered anything. Parts list rendered purely from `partsInstalled.map()`. Dead state from early implementation never cleaned up. | `tasks/[cardId]/_components/SignStepDialog.tsx` | Removed `showPartsForm` useState and all three `setShowPartsForm(true)` call sites. No functional change but removes confusion for future developers and eliminates unnecessary state. | Dev confusion / minor memory waste. Cosmetic fix. |
| BUG-006 | `RaiseFindingDialog` called with `aircraftHours={0}` hardcoded. Every finding raised from a task card was recorded as "found at 0 aircraft hours" — a regulatory data integrity violation. Under 14 CFR Part 145 / AC 43-9C, aircraft hours at time of discrepancy discovery must be accurate. An aircraft with 4,200 hours on the hobbs would log findings as found at 0h. | `tasks/[cardId]/page.tsx` | Added `useQuery(api.workOrders.getWorkOrder, ...)` to task card page. Extracted `currentAircraftHours = workOrderResult?.aircraft?.totalTimeAirframeHours ?? 0`. Passed real value to `aircraftHours` prop. | All discrepancy records from task card execution were logging wrong aircraft hours. Audit trail and AD compliance tracking corrupted. |

### Cycle 4 — 2026-03-01 (Persona: Billing Manager)
Journey traced: Quotes → Invoices → Recurring Billing → Deposits → Analytics

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-011 | Recurring billing templates table showed raw Convex document IDs (`jd7abc123def456...`) in the Customer column. `customerMap` existed only inside `CreateRecurringTemplateDialog`, not in the parent page. Billing manager looking at their 8 active templates had no idea which customer owned which without clicking into each one. | `billing/recurring/page.tsx` | Added `useQuery(api.billingV4.listAllCustomers)` to `RecurringBillingPage`. Built `customerMap` with `useMemo`. Replaced `{tpl.customerId}` with resolved customer name (fallback to truncated ID if customer not found). | Billing manager can now see customer names in the recurring templates table at a glance — no more cryptic IDs. |
| BUG-012 | Analytics page "Recent Conversions" table used native `<a href={...}>` anchor tags. Clicking a quote number caused a full page reload (SPA violation), losing all chart data, filter state, and Convex subscriptions. `Link` from `react-router-dom` was not imported. | `billing/analytics/page.tsx` | Added `import { Link } from "react-router-dom"`. Replaced `<a href={...}>` with `<Link to={...}>`. Navigation now uses SPA client-side routing. | Billing manager reviewing converted quotes clicks a quote number and the entire analytics page reloads from scratch instead of navigating smoothly. |
| BUG-013 | Quotes list page showed no customer name on quote cards, and search only matched `quote.quoteNumber`. A billing manager searching for "Southwest Aero" or "Jones Flying Club" got zero results — they had to know the quote number. Quote cards were entirely anonymous (just quote number + status + date + amount). | `billing/quotes/page.tsx` | Added `useQuery(api.customers.listCustomers)` + `customerMap`. Surfaced customer name as a prominent `text-sm font-medium` line on each quote card. Expanded search `filter()` to also match customer name. Updated placeholder to "Search by quote # or customer...". | Billing manager can identify which customer a quote belongs to without opening it, and can actually search by customer name. |
| BUG-014 | `RecordDepositDialog` used a hardcoded `PLACEHOLDER_TECH_ID = "k97abc000000000000000000"` when `techId` was `undefined`. Deposits recorded during this window created permanent audit trail entries attributing the receipt to a phantom technician ID that doesn't exist in the database. Financial records would show deposits "recorded by" a ghost user with no name. | `billing/deposits/page.tsx` | Removed `PLACEHOLDER_TECH_ID` constant entirely. Added `if (!techId) { setError("Technician profile required..."); return; }` guard in `handleSubmit()`. Users now see a clear actionable error directing them to Personnel setup instead of silently poisoning the financial audit trail. | Deposits were being recorded with a fake technician ID — financial audit trail was corrupted for any user who hadn't completed personnel setup. |

### Cycle 5 — 2026-03-01 (Persona: QCM Inspector)
Journey traced: Compliance → AD/SB → Audit Trail → RTS Review

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-015 | `CloseReadinessPanel` in QCM Review received ALL work orders from `listWorkOrders` (up to 200, all statuses including closed/cancelled/voided). A QCM inspector selecting from a dropdown of 200 items — most already closed — to find an open WO for close-readiness review is impractical. Running `getWorkOrderCloseReadinessV2` on an already-closed WO also returns confusing results. | `compliance/qcm-review/page.tsx` | Added `.filter(wo => !["closed","cancelled","voided"].includes(wo.status ?? ""))` before mapping workOrders to the panel. Panel now only shows open/active WOs that actually need close-readiness evaluation. | QCM inspector trying to check if WO-2026-042 is ready to close had to scroll past 150+ closed WOs to find it. Now only active WOs appear. |
| BUG-016 | `AircraftComplianceCard` built its fleet-to-detail link as `/fleet/${tailNumber}` where `tailNumber = aircraft.currentRegistration ?? "—"`. When `currentRegistration` was undefined, the card linked to `/fleet/—` — a guaranteed 404 route. The fleet detail route is `/fleet/[tail]` and `"—"` matches no aircraft registration. | `compliance/_components/AircraftComplianceCard.tsx` | Changed to build `fleetHref` conditionally: `aircraft.currentRegistration ? /fleet/${encodeURIComponent(registration)} : /fleet/${aircraft._id}`. Aircraft with no registration now link to their Convex ID instead of a broken route. | QCM inspector clicking on an unregistered ferry aircraft from the compliance list got a 404 page instead of its AD status detail. |
| BUG-017 | AD/SB compliance page: `statusFilter` state (`"overdue"`, `"due_soon"`, etc.) persisted when the aircraft selector changed. Inspector viewing "overdue" ADs for N192AK, then switching to N76LS (which has 0 overdue ADs but several due-soon), would see an empty table and assume N76LS was fine — because "overdue" filter was still active, hiding the amber due-soon items. | `compliance/ad-sb/page.tsx` | Added `setStatusFilter("all")` call inside the aircraft `Select`'s `onValueChange` handler. Status filter now resets to "All Statuses" whenever a different aircraft is selected. | Inspector reviews overdue ADs for 3 aircraft in sequence. On aircraft 2 (no overdue, 4 due-soon), they see zero rows and incorrectly close the compliance review without noting the approaching due dates. |
| BUG-018 | QCM Review IA sign-off queue: `IAStep.workOrderId` is typed as `Id<"workOrders"> \| null`. The "IA Sign Off" button Link built its URL as `/work-orders/${step.workOrderId}/tasks/...` — when `workOrderId` was `null`, this navigated to `/work-orders/null/tasks/...` which is a broken 404 route. An IA inspector clicking a sign-off button for an orphaned step would land on a not-found page with no explanation. | `compliance/qcm-review/page.tsx` | Wrapped "IA Sign Off" Button in a conditional: when `step.workOrderId` is truthy, render the Link button; when null, render a `"No WO linked"` italicized placeholder. IA can immediately see that the step has no work order context and escalate appropriately rather than hitting a dead link. | IA inspector working the sign-off queue clicks "IA Sign Off" for a step and lands on a 404 page. Wastes their time and creates confusion about whether the sign-off was triggered or not. |

### Cycle 7 — 2026-03-02 (Persona: Shop Manager)
Journey traced: Dashboard → Work Orders → Scheduling → Reports (all sub-pages)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-023 | `reports/financials/forecast/page.tsx`, `profitability/page.tsx`, and `runway/page.tsx` had NO sub-navigation bar. A shop manager clicking "Forecast" from the Financial Dashboard was stranded — no buttons to reach "Profitability" or "Runway" without hitting the browser back button. The icon imports (`BarChart2`, `Navigation`, `FileBarChart`) were imported on forecast but the sub-nav was never rendered. The profitability and runway pages had no `Link` import at all. | `reports/financials/forecast/page.tsx`, `reports/financials/profitability/page.tsx`, `reports/financials/runway/page.tsx` | Added the full 5-button Reports Sub-Navigation bar to all three pages (matching the pattern on `reports/financials/page.tsx`). Each page highlights its own button with `variant="secondary"`; siblings use `variant="ghost"`. Added `Link`, `Button`, and required icon imports to pages that were missing them. | Shop manager navigating the reports section was trapped in whichever sub-page they opened — a core navigation dead-end that required browser back button awareness. |
| BUG-024 | `reports/financials/forecast/page.tsx` declared `const timeEntries = useQuery(api.timeClock.listTimeEntries, ...)` but never used the variable in any computation. Wasted a Convex subscription slot on every page mount. Also `reports/financials/page.tsx` (Financial Dashboard) declared both `quotes = useQuery(api.billing.listQuotes, ...)` and `timeEntries = useQuery(api.timeClock.listTimeEntries, ...)` — neither variable was used anywhere in the file after the removed `woTypeRevenue` / `aircraftTypeRevenue` computations. | `reports/financials/forecast/page.tsx`, `reports/financials/page.tsx` | Removed `timeEntries` subscription from forecast page. Removed both `quotes` and `timeEntries` subscriptions from the Financial Dashboard (neither was consumed). Each dead subscription was firing a full Convex read on every render with no user benefit. | Unnecessary backend load; Convex subscription quotas consumed for zero user value. |
| BUG-025 | Financial Dashboard (`reports/financials/page.tsx`) rendered two charts — "Revenue by Aircraft Type" and "Top 5 WO Types by Revenue" — using completely hardcoded percentage distributions applied to total invoice revenue. Aircraft type used `[Cessna 30%, Piper 22%, Beechcraft 18%, Cirrus 15%, Other 15%]`; WO types used `[0.35, 0.25, 0.20, 0.12, 0.08]`. A shop manager reviewing these charts was looking at entirely fabricated data presented as real analytics. The only disclosure was a small amber "Connect live data" badge easily mistaken as a minor caveat rather than "none of this is real." `PieChart`, `Pie`, `Cell`, and `CHART_COLORS` were all imported solely for these fake charts. | `reports/financials/page.tsx` | Removed both fake useMemo computations and their chart render blocks entirely. Replaced with honest `border-dashed` empty-state cards explaining that WO-type and aircraft-type revenue breakdowns require server-side invoice→WO→aircraft joins not yet implemented. Removed now-unused recharts imports (`PieChart`, `Pie`, `Cell`, `BarChart`, `Bar`) and the `CHART_COLORS` constant. Logged the real data requirement in the component comment. | Shop manager presenting "WO Type Revenue" breakdown to ownership was showing completely fabricated statistics. This is now a clear "data not available" state instead of a misleading chart. |

### Cycle 6 — 2026-03-02 (Persona: DOM — Director of Maintenance)
Journey traced: Fleet → Aircraft Detail → Logbook → Predictions → Training

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-019 | Aircraft detail "Customer" card displayed the raw Convex document ID (`jd7abc123def456...`) with the label "Customer ID". No `customerMap` was built on this page — unlike the fleet list which correctly resolved names. A DOM clicking into an aircraft's detail to see who owns it saw a cryptic hex string, not the customer name. | `app/(app)/fleet/[tail]/page.tsx` | Added `useQuery(api.billingV4.listAllCustomers)` and `useMemo`-built `customerMap`. Customer card now shows the resolved customer name with a skeleton while loading. Falls back to truncated ID only if customer is not found. | DOM opens N192AK detail to call the customer about a repair estimate and sees "j97a5bc2..." instead of "Southwest Aero Services". Has to navigate to Billing separately to find the name. |
| BUG-020 | Fleet calendar event links navigated to `/work-orders/${ev.woNumber ?? ev.woId}`. Since `ev.woNumber = wo.workOrderNumber` (e.g. `"WO-2026-042"`) it was always truthy, so the link always used the human-readable WO number string — but the work orders router expects the Convex document `_id`. Clicking any calendar event produced a "Work Order Not Found" 404 page. | `app/(app)/fleet/calendar/page.tsx` | Changed `ev.woNumber ?? ev.woId` to `ev.woId` — uses the Convex document ID which is what the WO detail route actually expects. The `woNumber` field is still stored in the event object but no longer used for routing. | DOM clicks on a scheduled maintenance event in the fleet calendar and gets a 404 page. Every calendar event link was broken. |
| BUG-021 | Predictions severity tab labels (Critical / High / Medium / Low) showed counts from `summary`, which counted only `status === "active"` predictions. But the tab content shows ALL predictions by severity regardless of status (acknowledged, resolved, dismissed included). With 5 critical predictions (3 resolved, 2 active), the tab label said "Critical (2)" but showed 5 cards — the badge didn't match reality. | `app/(app)/fleet/predictions/page.tsx` | Added `tabCounts` useMemo that counts all predictions by severity (independent of status). Tab labels now use `tabCounts.critical/high/medium/low`. Summary stat cards continue to use `summary` (active-only) for the operational urgency view. | DOM sees "Critical (0)" on the tab but clicks through and sees 4 critical cards — one acknowledged and three dismissed but still visible. Loses confidence in the numbers. |
| BUG-022 | Aircraft detail page had no navigation path to the per-aircraft logbook (`/fleet/[tail]/logbook`). The logbook was a complete, functional page but was unreachable from the UI — no tab, no link, no button anywhere in the aircraft detail view. A DOM reviewing an aircraft's maintenance history had no way to get there without knowing the URL pattern manually. | `app/(app)/fleet/[tail]/page.tsx` | Added a 5th "Logbook" tab to the aircraft detail `TabsList`. Tab content includes a description of the logbook's regulatory coverage (43.9/43.11/corrections), a prominent "Open Full Logbook" button linking to `/fleet/[tail]/logbook`, and a bullet-list summary of what's captured per entry (approved data, signing tech, cert number, parts replaced). | DOM trying to review the maintenance history of an aircraft had zero in-app navigation to the logbook — it was effectively a hidden page. Now accessible via a dedicated tab alongside Aircraft Info, Times & Cycles, Work Orders, and AD Compliance. |

### Cycle 10 — 2026-03-02 (Persona: Billing Manager — 2nd pass)
Journey traced: Customers → Pricing → Tax Config → Invoices → Quotes → P&L

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-030 | `billing/invoices/[id]/page.tsx` — `canPayment` was `SENT \|\| PARTIAL \|\| PAID`. "Record Payment" button rendered on fully paid ($0 balance) invoices. While AI-039's overpayment guard would block any positive entry, showing a green "Record Payment" button on a paid invoice confuses billing managers — they have to click, enter an amount, and get rejected before realising the invoice is already settled. | `billing/invoices/[id]/page.tsx` | Changed `canPayment` to `invoice.status === "SENT" \|\| invoice.status === "PARTIAL"`. PAID invoices no longer show the Record Payment button. | Billing manager reviewing the invoice list clicks into a PAID invoice and sees "Record Payment" — spends time entering payment details before realising the balance is $0. |
| BUG-031 | `billing/pricing/page.tsx` — New Pricing Profile dialog used a raw HTML `<input type="checkbox">` for "Set as Default Profile". No `Checkbox` import existed on this page. Raw HTML checkboxes are invisible in dark mode (white-on-white background) — same class of bug as BUG-028 (shipping hazmat, fixed). | `billing/pricing/page.tsx` | Added `import { Checkbox } from "@/components/ui/checkbox"`. Replaced raw `<input type="checkbox">` + bare `<label>` with shadcn `<Checkbox id="pricing-is-default">` + `<Label htmlFor=...>`. | In dark mode, the "Set as Default" checkbox is invisible — billing manager cannot mark a pricing profile as the org default without knowing the invisible checkbox exists. |
| BUG-032 | `billing/customers/page.tsx` — `filtered` was computed as an inline IIFE (`const filtered = (() => {...})()`) — not wrapped in `useMemo`. Runs on every render including dialog open/close, badge re-renders, and parent state updates irrelevant to the search. | `billing/customers/page.tsx` | Added `useMemo` import. Replaced inline IIFE with `useMemo(() => {...}, [customers, search])`. Filter only recomputes when the customer list or search text changes. | Minor perf fix — search keystrokes no longer trigger full re-filter on unrelated renders. Noticeable on orgs with 100+ customers. |
| BUG-033 | `billing/tax-config/page.tsx` — (1) `AddTaxRateDialog.handleSubmit()` closed the dialog silently on success — the billing manager had no confirmation toast and had to scan the table to verify the rate appeared. (2) The only "Add Tax Rate" button was in the page header — when a billing manager scrolled past a long list of rates and wanted to add another, they had to scroll all the way back to the top. | `billing/tax-config/page.tsx` | Added `import { toast } from "sonner"`. Added `toast.success("Tax rate added")` before `onClose()` in the success path. Added a secondary "Add Rate" button inside the Tax Rates card header (visible only when rates exist), so the action is always reachable without scrolling. | Billing manager adds a tax rate, dialog closes, nothing confirms success. They scroll down to find the rate — or add it a second time creating a duplicate. Also note: no backend `updateTaxRate`/`deleteTaxRate` exists — **BACKEND-NEEDED** to add deactivate/edit actions on existing rates. |

### Cycle 9 — 2026-03-02 (Persona: Parts Clerk — 2nd pass)
Journey traced: Inventory → Receiving → POs → Shipping → Rotables → Loaners

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-026 | `loaners/page.tsx` — all three mutation handlers (`handleCreate`, `handleLoanOut`, `handleReturn`) had zero `isSubmitting` state. Double-clicking "Create", "Loan Out", or "Return" during the in-flight Convex call would fire the mutation a second time, creating duplicate loaner item records and duplicate loan/return history events. Dialogs also didn't prevent closure mid-submit. Errors were swallowed with no message detail (`catch { toast.error("...") }` — discarded the actual error). | `parts/loaners/page.tsx` | Added `isCreatingItem`, `isLoaning`, `isReturning` state. Each handler now sets its flag at start and clears in `finally`. Dialog `onOpenChange` guards prevent closing during submission. Submit buttons disabled + show `Loader2` spinner while in-flight. Error catch blocks now pass `err.message` to `toast.error`. | Parts clerk double-clicks "Loan Out" and two loan records are created for the same part — billing and history are corrupted. Now one click, clear feedback. |
| BUG-027 | `shipping/page.tsx` — "Mark Delivered" button called `handleStatusUpdate(s._id, "delivered")` immediately on click with zero confirmation. Marking a shipment as "Delivered" is the definitive record that parts were physically received — there is no reverse transition from "delivered" back to "in_transit". A misclick on this button (e.g., intending "Mark Shipped") permanently changes the shipment record and cannot be corrected from the UI. | `parts/shipping/page.tsx` | Added `deliverConfirm` state (same shape as existing `cancelConfirm`). "Mark Delivered" button now sets `deliverConfirm` state instead of calling `handleStatusUpdate` directly. Added `AlertDialog` with: "Confirm Delivery?" title, description emphasizing physical receipt verification and irreversibility, green "Confirm Delivery" action button, and "Not Yet" cancel. | Parts clerk accidentally clicks "Mark Delivered" on an in-transit shipment that hasn't arrived yet. Inventory records show parts received; physically they're still in transit. Now requires explicit confirmation. |
| BUG-028 | `shipping/page.tsx` — Hazmat checkbox in the Create Shipment dialog was a raw HTML `<input type="checkbox">` instead of the shadcn `Checkbox` component. Raw HTML checkboxes do not respond to Tailwind dark-mode theming or CSS variable tokens — the checkbox is invisible (white on white) in dark mode, and the interaction area doesn't match the design system's touch target size. | `parts/shipping/page.tsx` | Added `import { Checkbox } from "@/components/ui/checkbox"`. Replaced raw `<input type="checkbox" id="hazmat" checked={...} onChange={e => setFormHazmat(e.target.checked)}>` with `<Checkbox id="hazmat" checked={formHazmat} onCheckedChange={(checked) => setFormHazmat(checked === true)}>`. Added `cursor-pointer` to the associated `<Label>`. | Hazmat checkbox is invisible in dark mode — parts clerk cannot check "Hazmat" on an outbound shipment, which is a DOT regulatory marking requirement (49 CFR Part 175). |
| BUG-029 | `billing/purchase-orders/new/page.tsx` — "Create PO" submit button was enabled even when the vendor dropdown showed no options (zero approved vendors). The page correctly shows an amber warning "No approved vendors found. Approve a vendor before creating a PO." but the button remained enabled. Clicking it triggered client-side validation and showed a toast error. The disabled state and the warning already communicated the blocker — the button just needed to reflect it. | `billing/purchase-orders/new/page.tsx` | Added `(vendors ?? []).length === 0` to the submit button's `disabled` expression. Button is now visually grayed and non-clickable when the vendor list is empty, consistent with what the amber warning already communicates. | Parts clerk creates a PO, the vendor dropdown is empty (new org or all vendors unapproved), the amber warning is there but they try clicking "Create PO" anyway and get a confusing error toast. The button being active suggested it would work. |


## Bug Hunter Fixes

### Cycle 1 — 2026-03-02 (Persona: Lead Technician)
Journey traced: My Work → Task Cards → Steps → Sign-off → Handoff Notes → RTS

| ID | What Was Broken | File | What Was Changed | Why It Matters |
|----|----------------|------|-----------------|----------------|
| BH-001 | **RTS signature page: `?returnTo=` param missing from link.** `RtsSignoffForm.tsx` linked to `/work-orders/${id}/signature` with no `?returnTo=` or `?intendedTable=` params. The signature page shows "Continue to Sign-Off" button **only when `returnTo` is present** — without it, the user enters their PIN, sees the token generate, and the "Continue" button is nowhere. They're stuck on the signature page with a 5-minute expiring token and no way back. Every RTS sign-off attempt was broken. | `app/(app)/work-orders/[id]/rts/_components/RtsSignoffForm.tsx` | Updated signature page `Link` to include `?returnTo=/work-orders/${workOrderId}/rts&intendedTable=returnToService` in the `to` prop. The signature page will now display "Continue to Sign-Off" and redirect back correctly. | Lead tech / IA doing end-of-WO RTS authorization enters their PIN on the signature page and cannot proceed — "Continue to Sign-Off" button is missing. Must manually navigate back, the token expires before they can use it, and they have to start over. |
| BH-002 | **RTS page ignores `?authEventId=` URL param.** The signature page appends `?authEventId=<id>` to the `returnTo` URL when redirecting back, but `rts/page.tsx` never called `useSearchParams()` so the auth event ID was silently discarded. The `signatureAuthEventId` state stayed empty and the user was required to **manually type a 25+ character Convex ID** into the text box — practically impossible. | `app/(app)/work-orders/[id]/rts/page.tsx` | Added `useSearchParams` to import. Extracted `authEventIdFromUrl = searchParams.get("authEventId") ?? ""` before state init. Initialized `signatureAuthEventId` state with `authEventIdFromUrl` so the field is pre-filled when the user returns from the signature page. | Even after BH-001 is fixed, the RTS page would still require the user to manually type the Convex auth event ID. With both fixes, the full flow works: PIN on signature page → redirect back → field pre-populated → Authorize button enabled immediately. |
| BH-003 | **Compliance "Save" button enabled when `aircraftId` is null** (silent failure). The compliance item add form's Save button was `disabled={!addReference.trim()}` — but the `onClick` handler also early-returns on `!taskCard.aircraftId`. On a task card with no aircraft linked, the button appeared enabled, the user clicked it, and nothing happened with zero feedback — no error, no toast, no loading state. | `app/(app)/work-orders/[id]/tasks/[cardId]/page.tsx` | Added `|| !orgId || !taskCard.workOrderId || !taskCard.aircraftId` to the Save button's `disabled` expression. The button's enabled state now correctly reflects all prerequisites for a successful save, not just the reference field. | Lead tech on a non-aircraft WO (ground equipment, component repair) adds a compliance item, clicks Save, nothing happens. No error message. They click again, still nothing. Compliance items silently fail to persist. |

### BACKEND-NEEDED items from Cycle 1:
| ID | Description |
|----|-------------|
| BACKEND-NEEDED-BH-01 | `tasks/new/page.tsx` captures `estimatedHours` in form state but cannot pass it to `createTaskCard` — the mutation schema doesn't include this field. A tech estimating 8 hours on a task card has no way to record it at card creation time. Add `estimatedHours` to the `createTaskCard` mutation schema. |

### Cycle 12 — 2026-03-02 (Persona: DOM — Director of Maintenance, 2nd pass)
Journey traced: Fleet list → Aircraft detail → Logbook → Calendar → Predictions

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-DOM-1 | `fleet/page.tsx` — `customerMap` was built as a bare `new Map()` in the render body (not wrapped in `useMemo`), rebuilding on every keystroke in the search box and every other render trigger. Fleet list was also unsorted — aircraft appeared in database insertion order (the order they were added to the org). A DOM with 20 aircraft searching for "N192AK" had to visually scan all 20 cards in random order. | `fleet/page.tsx` | Added `import { useMemo }` (was missing). Wrapped `customerMap` in `useMemo([customers])`. Replaced `fleet?.filter(...)` with a `useMemo([fleet, filterActiveWo, searchTerm, customerMap])` that filters AND sorts the result: `result.sort((a,b) => aKey.localeCompare(bKey))` using `currentRegistration ?? serialNumber`. Fleet now displays A–Z by registration at all times. | DOM searching for a specific aircraft no longer scans an unordered list. Customer map no longer rebuilds on every search keystroke. |
| BUG-DOM-2 | `fleet/calendar/page.tsx` — Calendar event tiles displayed only the tail number (`ev.tail`). The `ev.woNumber` field was populated and available but never rendered. A DOM looking at March 15 with "N192AK" on the calendar had no idea whether it was the annual inspection, an AOG repair, or a routine oil change — they had to click every event to find out. | `fleet/calendar/page.tsx` | Added `{ev.woNumber && <span className="opacity-70 ml-0.5">·{ev.woNumber}</span>}` inside the event tile Link. Made tail bold (`font-semibold`). Added `title={ev.woNumber ? \`${ev.tail} — ${ev.woNumber}\` : ev.tail}` for hover tooltip. DOM can now see both aircraft and WO number in a single tile without clicking. | DOM reviewing the weekly maintenance schedule can identify WO type at a glance. Clicking is no longer required just to know what's scheduled. |
| BUG-DOM-3 | `fleet/calendar/page.tsx` — No "Go to Today" button. After navigating to a future or past month to review scheduled maintenance, the DOM had no way to jump back to the current month except clicking the prev/next arrow one month at a time. | `fleet/calendar/page.tsx` | Added a "Today" `Button` in the month navigation row that only renders when the displayed month/year differs from the current month/year. Clicking it calls `setMonth(now.getMonth()); setYear(now.getFullYear())`. Standard calendar UX — omitting it was an obvious gap. | DOM navigates to April to review upcoming scheduled WOs, then cannot quickly return to the current week's view without clicking back repeatedly. |
| BUG-DOM-4 | `fleet/[tail]/page.tsx` — `pastWOs` was computed as `workOrders.filter(closed).slice(0, 20)` with no UI disclosure. An aircraft with 47 closed WOs showed exactly 20 rows with no message indicating older records existed. A DOM auditing an airframe's complete maintenance history would think they had the full picture when 27 WOs were silently hidden. | `fleet/[tail]/page.tsx` | Separated into `allPastWOs` (full filtered list) and `pastWOs` (sliced to 20). Added `pastWOsTruncated = allPastWOs.length > 20`. Added `truncated` prop to `WoSection`. When `truncated` is true, renders: "Showing most recent 20 of N closed work orders" notice at the bottom of the list. Count in the "Past" section badge now reflects the actual total (e.g., "Past (47)"), not just the displayed 20. | DOM reviewing an aircraft that's been in service since 2010 thinks they see all maintenance history. Now they see the true count and a clear truncation notice. |

### Cycle 11 — 2026-03-02 (Persona: QCM Inspector — 2nd pass)
Journey traced: Compliance → AD/SB → Audit Trail → QCM Review → RTS Authorization

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BH-004 | **RTS page "Download RTS PDF" button generates an incomplete document.** The button was in the pre-authorization header and called `RtsPDF()` without the RTS statement, aircraft hours at RTS, or limitations — all of which are entered in the form BELOW the button. The generated PDF had blank signature/statement fields. After authorization (success state), no download button existed at all — so the QCM inspector who just signed an RTS had no way to download the authorized certificate without navigating elsewhere. | `work-orders/[id]/rts/page.tsx` | Removed the pre-auth download button (generates legally incomplete document). Added "Download RTS Certificate" button to the success state, passing `rtsStatement`, `aircraftHoursAtRts`, and all form values — the complete authorized document. | IA downloads RTS PDF before authorization, gets a blank certificate, submits it to records. OR: IA authorizes RTS, looks for the download, can't find it, navigates away and loses the moment. Now the download is only available with the complete authorized data. |
| BH-005 | **Compliance page fleet list shows aircraft in database insertion order**, not alphabetically. The comment in the code said "sorted alphabetically by registration" but no `.sort()` was applied — `(fleet ?? []) as AircraftRow[]` was used directly. A QCM inspector scrolling through 15 aircraft to find N192AK would see them in whatever order they were added to the system months ago. | `compliance/page.tsx` | Changed to `[...(fleet ?? [])].sort((a, b) => (a.currentRegistration ?? "").localeCompare(b.currentRegistration ?? ""))`. Fleet list now consistently shows A–Z by registration. | QCM inspector reviewing the compliance dashboard can now find a specific aircraft by scanning alphabetically, not by remembering when it was added to the system. |
| BH-006 | **QCM Review IA sign-off queue: steps within each work order group were unsorted (query insertion order), and work order groups themselves were unsorted** (V8 object key insertion order). An IA inspector reviewing the queue for WO-2026-042 might see Step 5, Step 2, Step 8, Step 1. Out-of-sequence steps are confusing during task card review. WO groups in random order makes the queue hard to scan. | `compliance/qcm-review/page.tsx` | Steps within each group are now `.sort((a, b) => a.stepNumber - b.stepNumber)`. WO groups themselves are now `.sort(([a], [b]) => a.localeCompare(b))`. IA queue is now deterministic: WOs in WO-number order, steps in step-number order. | IA inspector reviewing a 12-step task card in the sign-off queue sees the steps out of order and loses confidence in the data. Now displays Step 1 → 2 → 3 consistently. |
| BH-007 | **Compliance page "Audit Trail" tool card said "Review all signed maintenance events"** — an incorrect description. The Audit Trail page (`/compliance/audit-trail`) shows per-aircraft AD compliance detail with overdue/due-soon drill-down, not a chronological log of signed maintenance actions. A QCM inspector or FAA auditor navigating there expecting a 14 CFR 91.417 maintenance event history would find AD compliance dashboards instead, creating confusion about what records exist. | `compliance/page.tsx` | Updated tool card description to "Per-aircraft AD compliance detail and due-date drill-down" — accurately describes what the page shows. | FAA inspector clicks "Audit Trail" expecting signed maintenance record log, gets AD compliance data. Could lead to a false conclusion that no maintenance event audit trail exists in the system. |

### Cycle 13 — 2026-03-02 (Persona: Shop Manager)
Journey traced: Dashboard → Work Orders (Kanban) → Scheduling → Reports (Financial Dashboard)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-SM-001 | **Financial Dashboard revenue KPIs included unpaid invoices** — `validInvoices = invoices.filter(i => i.status !== "VOID")` included DRAFT, SENT, and PARTIAL invoices in YTD/QTD/MTD revenue totals. `revenueMTD/QTD/YTD` also used `i.createdAt` (invoice creation date) for period attribution instead of `i.paidAt` (when cash was actually received). Monthly revenue trend and Gross Margin trend had the same flaw. Top 5 Customers table counted customers with $0 collected as "top customers." | `reports/financials/page.tsx` | Changed `validInvoices` filter for all revenue metrics to `status === "PAID" \|\| status === "PARTIAL"`. Updated period attribution in MTD/QTD/YTD filters from `i.createdAt` to `i.paidAt ?? i.createdAt`. Updated monthly revenue trend and margin trend filters to match. Updated topCustomers filter to exclude uncollected invoices. | Shop manager reviews "Revenue YTD" for a budget meeting and sees an inflated figure that includes $80k in unpaid drafts. Reports data to the bank or ownership group — number is wrong and potentially fraudulent misrepresentation of cash position. |
| BUG-SM-002 | **Scheduling sub-nav never showed "Gantt Board" as the active page.** Every other scheduling sub-page (Capacity, Financial Planning, Bays) correctly rendered their own button as `variant="secondary"` to indicate the current location. But the main scheduling/page.tsx sub-nav rendered all buttons as `variant="ghost"` — the "Gantt Board" button was indistinguishable from the others even when it was the active page. | `scheduling/page.tsx` | Changed "Gantt Board" `Button` from `variant="ghost"` to `variant="secondary"` in the scheduling toolbar sub-nav. Active page indication is now consistent across all 4 scheduling sub-pages. | Shop manager navigating the scheduling section can't tell which sub-page they're on without reading the page heading — the nav gives no active state signal. Inconsistent UI erodes trust in the product. |
| BUG-SM-003 | **Financial Planning save handler silently passed NaN to Convex backend** when inputs contained non-numeric text. `Number("100k")` = `NaN`, `Number("$125")` = `NaN`. The `|| 0` guard in `parsed` only caught empty strings — not text with letters or symbols. NaN in `defaultShopRate` corrupts the entire Gantt financial model: the P&L tracker, Capacity Forecaster, and Analytics panel all use this value for daily income calculations. All would show `NaN` or `$0` revenue. | `scheduling/financial-planning/page.tsx` | Added a validation loop in `handleSave()` before the Convex mutation call: iterates all 5 parsed numeric values, checks `Number.isNaN(value)` with a clear user-facing error message (e.g. "Default Shop Rate: enter a valid number (e.g. 125, not '125/hr')"), and checks `value < 0` for negative values. Returns early with `toast.error()` on first failure — no bad data reaches the backend. | Shop manager types "125/hr" or "38,000" (comma-separated) in a financial planning field, hits Save, and corrupts the scheduling model's entire P&L projection. All financial charts in the Gantt show $NaN until manually corrected. |
| BUG-SM-004 | **Kanban board `h1` used non-standard design system typography** — `text-xl font-bold` instead of the app-standard `text-lg sm:text-xl md:text-2xl font-semibold text-foreground`. The header was visually heavier and inconsistent with every other page header in the application. | `work-orders/kanban/page.tsx` | Changed `h1` className from `text-xl font-bold` to `text-lg sm:text-xl md:text-2xl font-semibold text-foreground`. Consistent with all other page headers in the codebase. | Minor visual inconsistency — header text weight and size doesn't match rest of the app, slightly off-putting to visual-detail-oriented users and product reviewers. |

### BACKEND-NEEDED items from Cycle 13:
| ID | Description |
|----|-------------|
| BACKEND-NEEDED-SM-01 | Financial Dashboard Gross Margin uses `i.laborTotal` from invoices as labor COGS — this is the labor *revenue charged to the customer*, not the shop's labor *cost*. True labor cost requires time-clock entries × technician wage rates. The gross margin calculation is structurally incorrect until a server-side COGS query is available that joins time entries to wage rates per WO. |

### Cycle 2 (Bug Hunter) — 2026-03-02 (Persona: Parts Clerk)
Journey traced: Inventory → Receiving → POs → Shipping → Rotables → Loaners

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BH-008 | **Dead `Filter` button in Parts Inventory** — The toolbar in `parts/page.tsx` had a `<Button>` with a `Filter` icon and no `onClick` handler, no popover, no functionality. A parts clerk clicking it expected a condition/location/supplier filter dropdown and got nothing. The `Filter` icon was imported from lucide-react solely for this dead button. | `app/(app)/parts/page.tsx` | Removed the non-functional `Filter` button and the now-unused `Filter` import from lucide-react. The Scan button remains as the only toolbar action. | Parts clerk clicks the Filter button trying to narrow 200-part inventory to a specific condition or location — nothing happens, zero feedback, wastes their time. |
| BH-009 | **"Loaned Out" rotables tab missing from the UI** — `RotableFilter` type included `"loaned_out"`, `STATUS_STYLES` and `STATUS_LABELS` maps included `"loaned_out"`, the backend `rotables.list` query accepts `status: "loaned_out"` — but the `TabsList` in `rotables/page.tsx` only rendered tabs for All, Installed, Serviceable, In Shop, At Vendor, Condemned. If a rotable was set to `loaned_out` status, it was visible only in "All" with no dedicated filter tab. The parts clerk managing 8 loaned-out props/starters had no way to see them at a glance. | `app/(app)/parts/rotables/page.tsx` | Added `["loaned_out", "Loaned Out"]` to the `TabsList` array, between "At Vendor" and "Condemned". The tab correctly drives the `status` filter on the backend query. Count badge appears when loaned-out rotables exist. | Parts clerk needs to follow up on an overdue loaner rotable but has no filter to isolate loaned-out components. Had to scan the "All" tab and identify them manually by badge color. |
| BH-010 | **Purchase Order list showed no vendor name** — `billing/purchase-orders/page.tsx` displayed only `poNumber`, status badge, creation date, and dollar amount. A parts clerk looking at "PO-0042, SUBMITTED, $2,840.00" had no idea which vendor it was for without clicking into the detail page. With 30+ open POs, this required clicking through each PO to find the right one. The search also only matched `poNumber`, not vendor name. | `app/(app)/billing/purchase-orders/page.tsx` | Added `useQuery(api.vendors.listVendors)` with a `useMemo`-built lookup map `vendorMap: Record<vendorId, name>`. Each PO card now shows the vendor name with a `Building2` icon below the PO number. Added `Building2` to lucide imports. Updated search filter to also match vendor name. Updated search placeholder to "Search PO # or vendor...". Added `Id` type import. | Parts clerk searching for the AVStar PO to check delivery status had to click through 12 POs because nothing showed vendor names in the list. Now all POs show the vendor name. |
| BH-011 | **New PO form allowed deleting the last line item** — In `billing/purchase-orders/new/page.tsx`, every line item had a `Trash2` delete button with no minimum-item guard. A clerk who accidentally clicked delete on the only remaining line item ended up with an empty form (no line items), the submit button still showed "Create PO" but validation would fail silently (the error only appeared if they clicked submit). The UX left them stranded — they had to re-add a line item or reload. | `app/(app)/billing/purchase-orders/new/page.tsx` | Added `disabled={lineItems.length === 1}` and `title="Cannot remove the only line item"` to the delete `Button`. Added `disabled:opacity-30 disabled:cursor-not-allowed` to className. The delete button is visually dimmed and non-interactive when only one line item exists. | Parts clerk creating a PO for a single part accidentally deletes the line item. The form is now empty, no error is shown, and the "Create PO" button gives a confusing validation error. Now the button is grayed when there's only 1 item. |

### BACKEND-NEEDED items from Cycle 2 (Bug Hunter):
| ID | Description |
|----|-------------|
| BACKEND-NEEDED-BH-P01 | PO line items have no `partNumber` field in the `poLineItems` schema — only a freeform `description`. A parts clerk ordering 3 part numbers from the same vendor needs to record the P/N on each line for receiving. Add `partNumber: v.optional(v.string())` to `poLineItems` schema and `addPOLineItem` mutation args, then add a "Part Number" column to the new PO form. |

### Cycle 14 — 2026-03-02 (Persona: Lead Technician)
Journey traced: My Work → Task Cards → Steps → Sign-off → Handoff Notes

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-LT-001 | **`RaiseFindingDialog` could be dismissed while in-flight.** `onOpenChange={(v) => !v && onClose()}` had no `isSubmitting` guard. When the tech clicked "Raise Finding" and then pressed Escape or clicked outside while the Convex `openDiscrepancy` mutation was still awaiting auth, the dialog closed. The finding may or may not have been created — the tech assumed it was saved (they clicked the button), but if the mutation was mid-flight the finding was silently dropped. Unlike `SignStepDialog` and `SignCardDialog` which had this fixed as BUG-LT2-007, `RaiseFindingDialog` still had the unguarded handler. | `work-orders/[id]/tasks/[cardId]/_components/RaiseFindingDialog.tsx` | Changed `onOpenChange={(v) => !v && onClose()}` to `onOpenChange={(v) => { if (!v && !isSubmitting) onClose(); }}`. Finding dialog now stays open (non-dismissable) while submission is in-flight, consistent with sign-off dialogs. | Tech reports a cracked spar finding, presses Escape a half-second too early, finds no finding in the discrepancy list, reports the issue again on the next shift — double-entry or missed finding. Safety-critical data loss path. |
| BUG-LT-002 | **`MarkNaDialog` could be dismissed while in-flight.** Same missing `isSubmitting` guard as BUG-LT-001. If the tech clicked "Mark N/A" and the dialog closed mid-flight, the backend step might be marked N/A but `onSuccess()` never fired, `naTarget` never cleared, the UI still showed the step as pending. Re-opening the dialog attempted a second N/A mutation which the backend rejected with "step already completed" — no explanation why the button seemed broken. | `work-orders/[id]/tasks/[cardId]/_components/MarkNaDialog.tsx` | Changed `onOpenChange={(v) => !v && onClose()}` to `onOpenChange={(v) => { if (!v && !isSubmitting) onClose(); }}`. Consistent with all other sign-off dialogs. | Tech marks step N/A, dialog closes mid-flight on accidental Escape press. Step appears still-pending. Tech tries again, gets "step already completed" error with no explanation. Wastes time and erodes trust in the step-tracking system. |
| BUG-LT-003 | **`StepBuilder` used raw `<input type="checkbox">` for "Requires special tool", "Sign-off required", and "IA sign-off required".** Raw HTML checkboxes render as native browser checkboxes — white in most dark-mode themes, making them invisible against the card background. "IA sign-off required" in particular: a supervisor creating a task card in dark mode cannot see whether the IA checkbox is checked, and may accidentally create IA-required steps or miss flagging one. This is the same design system violation fixed across the rest of the app. | `work-orders/[id]/tasks/new/_components/StepBuilder.tsx` | Added `import { Checkbox } from "@/components/ui/checkbox"`. Replaced all three `<input type="checkbox">` elements with shadcn `<Checkbox>` components using `onCheckedChange={(checked) => onChange(id, field, checked === true)}`. Checkbox state is now visible in all themes. | Supervisor in dark mode creates a 12-step task card. "IA sign-off required" checkbox was already checked by default (invisible). An airframe tech gets to the step at 2AM, sees the IA badge, and can't proceed — the IA inspector isn't on shift. Work stops. |
| BUG-LT-004 | **`TaskStepRow` showed the N/A button for `pending` steps only.** A tech who accidentally clicked "Start" on the wrong step (marking it `in_progress`) had zero path to un-start it or mark it N/A. They were stuck: they could sign the step (creating a false maintenance record for work that was never performed), void the entire card (discarding all completed steps), or leave the step permanently `in_progress` (card can never be signed). The `MarkNaDialog` backend call (`completeStep({ action: "mark_na" })`) accepts in_progress steps — the restriction was UI-only. | `work-orders/[id]/tasks/[cardId]/_components/TaskStepRow.tsx` | Changed N/A button guard from `isPending &&` to `(isPending \|\| isInProgress) &&`. A tech who accidentally started the wrong step can now immediately mark it N/A with a reason, clearing the in_progress state and keeping the card on track. | Tech clicks "Start" on Step 5 instead of Step 3 at 2AM. Has no undo. The only legal exit was voiding the entire card (losing all signed steps) or creating a false maintenance record. Now they can mark it N/A ("started in error — step does not apply to this airframe config") and continue. |


### Cycle 15 — 2026-03-02 (Persona: Parts Clerk)
Journey traced: Inventory → Receiving → Parts Queue → New Part → Low Stock

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-PC-001 | **`counts` object computed inline on every render in `PartsPage`.** Seven `.filter()` passes ran on every keystroke in the search box, every dialog open/close, and every tab switch — none wrapped in `useMemo`. With a 500-part inventory (not unusual for a busy shop), the clerk typing a 5-character P/N would trigger 35 filter iterations just for the tab badge counts, in addition to the `filtered` memo. | `app/(app)/parts/page.tsx` | Replaced the inline `counts` object with a `useMemo([parts])` that computes all six location counts in a single `for...of` pass (from 7 filter calls to 1 loop). Zero re-computation unless the `parts` array reference changes. | Inventory page sluggish when typing part numbers with a large inventory. Tabs flicker on each keystroke as badge counts recomputed. Perceptible lag on lower-end shop tablets used at the parts counter. |
| BUG-PC-002 | **`ReceivingInspectionDialog` and `ReservePartDialog` in `PartsPage` could be dismissed mid-flight.** Both dialogs used `onOpenChange={(v) => !v && handleClose()}` with no `isSubmitting` guard. A parts clerk who clicked "Approve" or "Reserve Part" and then accidentally hit Escape or clicked outside the modal while the Convex mutation was in-flight would see the dialog disappear. The mutation might succeed, partially succeed, or fail — the clerk had no way to know. | `app/(app)/parts/page.tsx` | Changed both dialog `onOpenChange` handlers to `(v) => { if (!v && !isSubmitting) handleClose(); }`. Both dialogs are now non-dismissable while a submission is in-flight, consistent with the pattern established for all other confirmation dialogs in the app. | Parts clerk approves a $4,000 LLP during receiving inspection. Hits Escape by accident just after clicking Approve. Dialog closes. Clerk re-opens the receiving queue expecting to see the part gone — it's still there. Tries to inspect again, gets a confusing backend error or double-approves. Part ends up with two inspection records. |
| BUG-PC-003 | **`InspectDialog` in `parts/requests/page.tsx` and `parts/receiving/page.tsx` also lacked `isSubmitting` guard on `onOpenChange`.** Same class of bug as BUG-PC-002. Three separate parts-receiving dialogs had this vulnerability. | `app/(app)/parts/requests/page.tsx`, `app/(app)/parts/receiving/page.tsx` | Changed both `onOpenChange={(v) => !v && handleClose()}` to `(v) => { if (!v && !submitting) handleClose(); }`. All three receiving inspection dialogs across the parts module now consistently prevent mid-flight dismissal. | Same impact as BUG-PC-002. Particularly bad in the Parts Queue page, which is the primary workflow for processing incoming shipments. |
| BUG-PC-004 | **"Low Stock" tab and card badge triggered for ANY part with a `reorderPoint` set, regardless of actual quantity.** The filter was `p.reorderPoint != null && p.location === "inventory"` — meaning a part with `reorderPoint: 2` and 500 units on hand showed `⚠ Low Stock` on its card AND counted toward the Low Stock tab badge. A shop that diligently sets reorder points on all consumables (O-rings, filters, safeties) would see every inventory part badged as low-stock, making the alert meaningless. | `app/(app)/parts/page.tsx` | Added `quantityOnHand?: number; quantity?: number;` to the `PartDoc` type (backend already returns these fields, evidenced by the CSV export's `p.quantityOnHand ?? p.quantity` cast). Updated both the `counts` memo and the `filtered` memo low_stock branch to `p.reorderPoint != null && (p.quantityOnHand ?? p.quantity ?? 0) <= p.reorderPoint`. Card badge updated to match. | Shop configures reorder points for all 200 consumable parts. "Low Stock" tab shows "200". Clerk stops trusting the alert. An oil filter actually hits zero — it's buried in 200 false positives. Aircraft sits AOG waiting for a $12 filter nobody noticed was out. |
| BUG-PC-005 | **`parts/new/page.tsx` used raw `<input type="checkbox">` for four critical receiving fields**: Serialized Part, Owner-Supplied Part (OSP), Life-Limited Part (LLP), and Has Shelf-Life Expiry. Raw browser checkboxes are nearly invisible in dark mode against the card background (white checkbox on off-white surface). The LLP checkbox is particularly critical — checking it wrong means life-limited tracking is either silently enabled or disabled for a newly received part. | `app/(app)/parts/new/page.tsx` | Added `import { Checkbox } from "@/components/ui/checkbox"`. Replaced all four `<input type="checkbox">` elements with shadcn `<Checkbox>` + `<Label>` pairs using `onCheckedChange`. Checkbox state is now clearly visible in all themes with the standard design system indicator. | Parts clerk receiving a new alternator in dark mode can't see whether "Life-Limited Part" is checked. Ships the alternator to stock without the LLP flag. The part accumulates hours with no life-limit tracking. Nobody notices until the overhaul comes due and the paperwork doesn't match. |

### Cycle 16 — 2026-03-02 (Persona: Billing Manager)
Journey traced: Quotes → Analytics → Recurring Billing → AR Dashboard

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-BM-001 | **`billing/analytics/page.tsx` — `totalInvoiced`, `count`, and `avgInvoiceValue` KPIs included VOID invoices.** `invoices.reduce(sum + i.total)` and `invoices.length` operated on the entire invoice array, including voided records. A billing manager reviewing the "Total Invoiced" KPI would see inflated figures — every voided invoice (cancelled job, billing error, test record) added to the headline number even though no money changed hands. | `app/(app)/billing/analytics/page.tsx` | Derived a `nonVoid = invoices.filter(i.status !== "VOID")` array in `invoiceMetrics` useMemo. All three affected values (`totalInvoiced`, `count`, `avgInvoiceValue`) now operate on `nonVoid`. | Billing manager presents "Total Invoiced YTD: $312,000" to ownership, but $48,000 of that is voided invoices. The real number is $264,000 — a material misstatement. |
| BUG-BM-002 | **`billing/analytics/page.tsx` — Monthly Revenue "Collected" bar used invoice creation date (`createdAt`) for month attribution instead of payment date (`paidAt`).** An invoice created in December and paid by the customer in January appeared in December's "Collected" bar, not January's. For a cash-basis shop, this is incorrect revenue recognition — December looks better than it was, January worse. | `app/(app)/billing/analytics/page.tsx` | Split the monthly data loop into two attribution paths: `invoiced` continues to use `inv.createdAt` (when the obligation was incurred), `collected` now uses `(inv.paidAt ?? inv.createdAt)` — cash received month, not invoice creation month. VOID invoices are skipped entirely. | Billing manager reviewing January's collected revenue sees it understated by $18,000 — that cash arrived in January but appears in December's bar. Bank reconciliation fails to match the chart. |
| BUG-BM-003 | **`billing/analytics/page.tsx` — "Top Customers by Revenue" bar chart included DRAFT, SENT, and PARTIAL invoices as "revenue."** The filter was `if (inv.status === "VOID") continue` — meaning a customer with $50,000 in sent-but-unpaid invoices ranked as a top revenue customer. The chart also accumulated `inv.total` (billed amount) instead of `inv.amountPaid` (cash received). | `app/(app)/billing/analytics/page.tsx` | Changed filter to `if (inv.amountPaid <= 0) continue` — only invoices with actual cash received enter the chart. Changed accumulator from `inv.total` to `inv.amountPaid`. Updated chart card title from "Top Customers by Revenue" to "Top Customers by Collected Revenue" for clarity. | Billing manager identifies "top revenue customers" to prioritize relationship management — but the list is actually "customers with the most outstanding AR." Real top payers are buried under chronic late-payers with large outstanding balances. |
| BUG-BM-004 | **`billing/recurring/page.tsx` — "Generate Now" button fired the invoice generation mutation immediately with no confirmation dialog and no per-row loading state.** Clicking the button once created an invoice immediately; double-clicking (or clicking while navigating away) would fire the mutation twice, generating duplicate invoices for the customer. There was no visual feedback that the mutation was in-flight — the button remained clickable throughout. | `app/(app)/billing/recurring/page.tsx` | Added `generateConfirm` state (tracks which template was clicked) and `generatingId` state (tracks in-flight mutation). "Generate Now" button now sets `generateConfirm` instead of calling the mutation directly. Added `AlertDialog` with clear description (warns about creating outside the scheduled cycle). Confirmation button is disabled and shows `Loader2` spinner while generating. `handleGenerateNow` sets/clears `generatingId` in try/finally. | Billing manager accidentally double-clicks "Generate Now" for a $3,200 monthly maintenance contract. Two invoices are created. Customer receives two bills. Billing team must manually void one invoice, contact customer, issue corrected statement. Now requires deliberate confirmation. |

### Cycle 17 — 2026-03-02 (Persona: QCM Inspector — 3rd pass)
Journey traced: Compliance → AD/SB Tracking → Audit Trail → RTS Review

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-QCM-F1 | **AD/SB per-aircraft "Overdue" filter missed all due-date overruns on recurring ADs.** `filteredRecords` for `statusFilter === "overdue"` only matched `r.complianceStatus === "not_complied"` (ADs that were never performed). But the "Overdue" badge also renders for records where `nextDueDate < Date.now() && applicable` — a recurring AD that was performed years ago but whose interval has now passed has `complianceStatus: "complied_recurring"`, not `"not_complied"`. Selecting "Overdue" in the filter showed these as absent, while the table badges (before filtering) correctly showed them as Overdue. QCM inspector filtering for overdue ADs missed an entire category of non-compliant records. | `compliance/ad-sb/page.tsx` | Extended the `statusFilter === "overdue"` case to include both: (1) `r.complianceStatus === "not_complied"` (never performed) AND (2) records where `nextDueDate < now && applicable && complianceStatus !== "not_complied"` (due-date overruns). Filter now matches exactly what the status badge renders. | QCM inspector filters for "Overdue" before an FAA audit. The filter shows 2 records (both not_complied). Inspector marks them resolved, closes the filter. Audit reveals 4 additional ADs past their recurring due date — caught by the badge but missed by the filter. FAA inspector cites non-compliance. |
| BUG-QCM-F2 | **AD/SB fleet "Compliant" filter showed aircraft with never-performed ADs as "Compliant."** `filteredFleetSummaries` for `statusFilter === "compliant"` checked `overdueCount === 0 && dueSoonCount === 0 && pendingCount === 0 && total > 0` — but omitted `notCompliedCount`. An aircraft with 3 ADs that were never performed (`notCompliedCount: 3`) but had no *date*-overruns (`overdueCount: 0`) passed the Compliant filter and appeared in the "Compliant" aircraft list. These aircraft are actively non-compliant and block RTS. | `compliance/ad-sb/page.tsx` | Added `notCompliedCount === 0` to the Compliant filter check. Also updated the Overdue fleet-view filter to include `notCompliedCount > 0`, and the Due Soon filter to exclude aircraft with either `overdueCount > 0` or `notCompliedCount > 0`. `notCompliedCount` is cast via `(s as unknown as Record<string, number>)["notCompliedCount"] ?? 0` since it's not yet in the TypeScript surface. Fixed the Link import (was missing, causing a pre-existing TS error on the Manage button). | QCM showing ownership "look, all aircraft in the Compliant filter" — but two aircraft have ADs that were never performed. Creates false regulatory assurance. |
| BUG-QCM-F3 | **RTS "Authorize Return to Service" button was enabled when the Aircraft Hours field was empty.** PRE-3 ("Aircraft Total Time Consistent") derives its `PASS` status from `report.aircraftCurrentHours != null` — it checks whether hours are *on file*, not whether the IA has *entered* a value in the form. With an auth event provided and RTS statement filled in, all 9 preconditions could show green while the hours field was blank. The IA clicked Authorize and received a text error below the button instead of a disabled button state. | `work-orders/[id]/rts/page.tsx` | Added `|| !aircraftHoursAtRts.trim()` to the Authorize button's `disabled` expression. The button is now gray and non-clickable until the hours field contains a value, even when all 9 precondition cards display PASS. | IA doing an RTS sign-off fills the statement, sees 9 green checks, clicks Authorize — gets "Please enter valid aircraft hours." text error below the button. Confusing: everything appeared ready but the button fired an error. Now the button stays gray until hours are entered. |
| BUG-QCM-F4 | **Audit Trail page had no sub-navigation** — a QCM inspector who navigated there from the Compliance dashboard or AD/SB page had no buttons to reach the main Compliance overview, AD/SB tracking, or QCM Review without using the browser back button or sidebar. All three other compliance pages (compliance/page.tsx, ad-sb/page.tsx, qcm-review/page.tsx) had at least one cross-link, but Audit Trail was a dead-end. Also fixed a pre-existing missing `Link` import in `ad-sb/page.tsx` that would have caused a TS error when the Manage button was clicked (though the button was rendered, it would have had no href resolution). | `compliance/audit-trail/page.tsx`, `compliance/ad-sb/page.tsx` | Added a 3-button sub-nav header row to the Audit Trail page: "← Compliance" ghost button, "AD/SB Tracking" outline button, "QCM Review" outline button — matching the cross-link pattern on `qcm-review/page.tsx`. Added `Link`, `Button`, `ClipboardList`, `ShieldAlert`, `ArrowLeft` imports. Fixed missing `Link` import in `ad-sb/page.tsx` (changed `useSearchParams` import to `useSearchParams, Link`). | QCM inspector reviews the fleet overview on Audit Trail, wants to check a specific aircraft's AD detail on AD/SB — had to navigate via sidebar or browser back. Now one click. |

### Cycle 18 — 2026-03-02 (Persona: DOM — Director of Maintenance)
Journey traced: Fleet → Aircraft Detail → Logbook → Predictions → Training

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-DOM-5 | **Training "Expiring Within 30 Days" and "Expired Training" sections showed course names with no technician attribution.** A DOM scanning these alert cards saw entries like "King Air Recurrent — Expires Apr 15, 2026" with zero indication of WHO needed renewal. To find out, the DOM had to click through each technician row individually. With a 12-person shop, that's 12 clicks to answer "who has expiring HazMat certs?" — a question that should be answerable in one glance. The `technicianId` field was present on every record but never rendered. | `app/(app)/personnel/training/page.tsx` | Added `techMap` useMemo (maps `tech._id → tech.legalName` from the already-loaded `technicians` query). In both the "Expiring Within 30 Days" and "Expired Training" sections, rendered `— {techMap.get(rec.technicianId) ?? "Unknown"}` inline after each course name. DOM can now read "King Air Recurrent — J. Martinez — Expires Apr 15" at a glance. | DOM preparing for an FAA inspection reviews the expiring training section. Can't tell which 3 out of 12 techs need recurrent certs. Has to click through each technician row. Misses one. FAA inspector asks for J. Martinez's King Air recurrent cert — it expired last week. |
| BUG-DOM-6 | **Technicians with zero training records showed a green "Compliant" badge and a green left-border.** The badge logic was `if (expired === 0 && expiringSoon === 0) → "Compliant"` — which is vacuously true for a brand-new hire whose records have never been entered. A DOM reviewing training compliance for a recently onboarded technician would see green "Compliant" — false assurance that the technician is qualified. The condition failed to differentiate "has records and all are current" (genuinely compliant) from "has no records at all" (unknown compliance state). | `app/(app)/personnel/training/page.tsx` | Added `totalRecords = current + expiringSoon + expired` computation per technician. Changed badge guard to `expired === 0 && expiringSoon === 0 && totalRecords > 0` for green "Compliant". Added new case: `totalRecords === 0` → gray "No Records" badge (outline variant) and `border-slate-500/40` left-border. Techs with zero records are now visually distinct from compliant techs. | DOM looks at training compliance for a tech hired last month. Green "Compliant" badge. Assumes all training entered. Tech gets assigned to a HazMat job. Records were never entered — the tech hasn't completed the course. Compliance gap was invisible. |
| BUG-DOM-7 | **"Add Training Record" and "Add Qualification Requirement" dialogs were dismissable mid-flight via Escape or outside-click.** `onOpenChange={setShowAddDialog}` and `onOpenChange={setShowReqDialog}` had no `isSubmitting` guard. The DOM clicks "Add Record," the Convex mutation fires, they accidentally hit Escape while it's in-flight — dialog closes. The record may or may not have been created. Re-opening the dialog and submitting again risks a duplicate training record (a regulatory compliance audit trail issue) or a gap if it silently failed. | `app/(app)/personnel/training/page.tsx` | Changed both dialog `onOpenChange` handlers to guard on the respective in-flight flag: `(v) => { if (!isAddingRecord) setShowAddDialog(v); }` and `(v) => { if (!isCreatingReq) setShowReqDialog(v); }`. Dialogs are now non-dismissable while a mutation is in-flight. | DOM adds a technician's annual recurrent training. Presses Escape a half-second after clicking Add. Dialog closes. DOM unsure if it saved. Checks the list — not there. Adds it again. Now the technician has two identical training records in their audit trail. Looks like the record was fabricated after-the-fact. |
| BUG-DOM-8 | **"Dismiss" button on Predictive Maintenance fired immediately for ALL prediction severities, including Critical.** Clicking Dismiss on a "CRITICAL — Engine bearing wear trending toward TBO limit" prediction removed it from the active list in one click with no confirmation, no friction, and no undo. A DOM who meant to click "Acknowledge" (the button directly above) could accidentally dismiss a safety-critical maintenance prediction with no recovery path. | `app/(app)/fleet/predictions/page.tsx` | For `severity === "critical"` or `severity === "high"` predictions: Dismiss button now sets `dismissTarget` state (prediction ID + severity + description text) instead of calling `handleDismiss` directly. An `AlertDialog` renders the prediction description and a severity-colored warning ("Dismissing a **critical** prediction should only be done when..."), requiring explicit "Dismiss Anyway" confirmation. Medium/Low severity predictions continue to dismiss immediately (appropriate low-friction path). Added `AlertDialog` import from `@/components/ui/alert-dialog`. | DOM reviewing fleet predictions sees "CRITICAL — Engine #1 bearing temp trending +12°F above baseline over 40 hours." Meant to click Acknowledge. Clicks Dismiss. Critical prediction gone. Engine bearing continues to degrade. The aircraft is next scheduled for a transatlantic ferry flight. Now Critical/High require a two-step confirm. |

### Cycle 20 — 2026-03-02 (Persona: Lead Technician — 3rd pass)
Journey traced: My Work → Task Cards → Maintenance Records → RTS Authorization → Signature flow

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BH-LT3-001 | **RTS PRE-3 ("Aircraft Total Time Consistent") showed PASS with an empty hours field.** `derivePreconditions` evaluated PRE-3 as `PASS` when `report.aircraftCurrentHours != null` — it checked whether hours were *on file*, not whether the IA had *entered* a value. With auth event + RTS statement filled in, all 9 preconditions showed green while the hours field was blank. Authorize stayed grayed out with no explanation (it had `!aircraftHoursAtRts.trim()` as an extra guard, but the precondition cards showed no PENDING). | `work-orders/[id]/rts/_components/derivePreconditions.ts`, `work-orders/[id]/rts/page.tsx` | Added `aircraftHoursAtRts?: string` as a 4th param to `derivePreconditions`. PRE-3 logic: if hours field empty → PENDING; if entered → check `aircraftCurrentHours != null` for PASS. Updated PRE-3 `failureMessage` to surface "Enter aircraft total time at RTS before authorizing." when pending. `rts/page.tsx` updated to pass `aircraftHoursAtRts` as 4th arg. | IA doing RTS authorization sees 9 green checkmarks but Authorize button is gray with no explanation. They click the button (it has a hit-box even when disabled) and nothing happens. No feedback. They assume a bug, escalate to DOM, waste 20 minutes. Now PRE-3 stays PENDING until the hours field is filled. |
| BH-LT3-002 | **`CreateRecordForm.tsx` "Return to Service" checkbox was a raw HTML `<input type="checkbox">`.** Invisible in dark mode (white checkbox on near-white background). Lead tech creating a maintenance record in a dark-mode browser or tablet cannot see or interact with the RTS checkbox. Any record that should carry an RTS certification is silently submitted without it — regulatory gap. This is the same class of bug fixed in BUG-028 (shipping hazmat), BUG-031 (pricing default), and BUG-LT-003 (StepBuilder). | `work-orders/[id]/records/_components/CreateRecordForm.tsx` | Added `import { Checkbox } from "@/components/ui/checkbox"`. Replaced `<input type="checkbox" id="rts-toggle">` with shadcn `<Checkbox id="rts-toggle" checked={...} onCheckedChange={(checked) => setField("returnedToService", checked === true)}>`. Changed bare `<label>` to `<Label>` (already imported). Checkbox is now themed, visible, and has correct dark-mode styling. | Lead tech on night shift (dark mode) creates the closing maintenance record. Cannot see the RTS checkbox. Submits without checking it. The record doesn't carry RTS certification. The WO closes, the PRE-8 maintenance records check shows records as unsigned/incomplete, and the QCM can't proceed to RTS authorization. |
| BH-LT3-003 | **`CreateRecordForm.tsx` signature link had no `?returnTo=` or `?intendedTable=` params.** Link to the signature page was `/work-orders/${workOrderId}/signature` — no returnTo, no intendedTable. The signature page only shows "Continue to Sign-Off" when `returnTo` is present. Without it, the tech got their auth event, the "Continue" button never appeared, and they had to manually copy a 25+ char Convex ID from the page UI back into the form field — practically impossible on a tablet. Also: the records page didn't read `?authEventId=` from the URL when the signature page redirected back. | `work-orders/[id]/records/_components/CreateRecordForm.tsx`, `work-orders/[id]/records/page.tsx` | **Form:** Signature link updated to `/work-orders/${workOrderId}/signature?returnTo=/work-orders/${workOrderId}/records&intendedTable=maintenanceRecords`. **Records page:** Added `useSearchParams`, read `authEventIdFromUrl = searchParams.get("authEventId") ?? ""`. `useEffect` auto-opens the create form when `authEventId` is in URL. `authEventIdFromUrl` passed as `initialState.signatureAuthEventId` to `CreateRecordForm`. Full flow now: link → signature page → PIN → "Continue to Sign-Off" → back to records page with form auto-open and field pre-filled. | Lead tech tries to sign a maintenance record at end of job. Clicks "the signature page" link. Gets their token. "Continue to Sign-Off" doesn't appear — signature page is a dead end. Token expires. They navigate back manually, stare at an empty auth event ID field, have no idea what to type. The whole maintenance record signing workflow was broken for this path. |
| BH-LT3-004 | **`CreateRecordForm.tsx` `handleSubmit()` had no validation for the RTS certification statement.** When `returnedToService` was `true`, the char counter showed "15 / 50 min" in amber — but nothing prevented submission with a 15-char statement. The 50-char minimum (required per 14 CFR 43.9 for certification statements) was only shown as UI feedback, not enforced. The backend might reject the short statement, but the error would arrive as a generic Convex schema message — confusing to a tech in the middle of a record creation workflow. | `work-orders/[id]/records/_components/CreateRecordForm.tsx` | Added two validation checks in `handleSubmit()` before `setIsSubmitting(true)`: (1) if `returnedToService && !returnToServiceStatement.trim()` → "An RTS certification statement is required…"; (2) if `returnedToService && statement.trim().length < 50` → "RTS certification statement must be at least 50 characters (14 CFR 43.9). Current: N chars." Matches the same validation pattern as `SignCardDialog` and `rts/page.tsx` PRE-9. | Lead tech writes a 30-char RTS statement, sees the amber "30 / 50 min" counter, thinks it's just a suggestion, clicks "Create & Sign Record." Gets a generic backend error. Confused — the form accepted the submission, why did the backend reject it? Now gets clear proactive error before the mutation fires. |

### Cycle 21 — 2026-03-02 (Persona: Parts Clerk)
Journey traced: Inventory → Receiving Inspection → Shipping → Rotables → Loaners

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-PC-001 | **Sidebar "Parts" section linked to `/parts/requests` instead of `/parts` (inventory).** A Parts Clerk clicking "Parts" in the left nav landed on the Part Requests page, not the inventory list. The main parts inventory page (`/parts`) had no sidebar entry at all, making it effectively unreachable without a direct URL or a button on another page. | `components/AppSidebar.tsx` | Changed `href: "/parts/requests"` to `href: "/parts"` on the Parts nav item so clicking the top-level entry goes to inventory. | Parts Clerk opens the app, clicks "Parts" in the sidebar, ends up on Part Requests instead of inventory. Searches for a part. Doesn't find it. Doesn't realize they're on the wrong page. |
| BUG-PC-002 | **`/parts/receiving` and `/parts/requests` had no sidebar child links.** The sidebar children only listed Tool Crib, Inventory Count, Core Tracking, Shipping, Rotables, and Loaners — the two most-used sub-pages (Receiving Inspection and Part Requests) were missing. Parts Clerk had to navigate there indirectly via the button on the inventory page. | `components/AppSidebar.tsx` | Added `{ title: "Receiving Inspection", href: "/parts/receiving" }` and `{ title: "Part Requests", href: "/parts/requests" }` as first two children under the Parts nav section. | Parts Clerk needs to approve an incoming part. Can't find "Receiving Inspection" in the sidebar. Clicks every sub-menu. Never finds it. Eventually lands there from the inventory page. Wastes 2 minutes every time. |
| BUG-PC-003 | **"Receiving Queue" button on Parts Inventory page linked to `/parts/requests` instead of `/parts/receiving`.** The button label says "Receiving Queue" and shows pending inspection count, but it routed to the Part Requests page — a completely different page. Clicking the amber "3 pending" badge took the user to the wrong destination. | `app/(app)/parts/page.tsx` | Changed `<Link to="/parts/requests">` to `<Link to="/parts/receiving">` on the Receiving Queue button. | Parts Clerk sees "3 pending" on the Receiving Queue button, clicks it expecting to inspect parts, lands on Part Requests instead. Confused. Has to find receiving another way. |
| BUG-PC-004 | **Loan-Out dialog showed "Loan Out Item" with no identification of which item was being loaned.** With multiple similar items (e.g., 5 King Air ELT covers on the shelf), clicking "Loan Out" opened a form with no indication of the part number or serial number being loaned. On a busy day, a parts clerk could easily select the wrong item. | `app/(app)/parts/loaners/page.tsx` | Added a part identification block below the dialog header: looks up the item from `all` using `loanDialogId`, renders a muted card showing `partNumber`, `serialNumber` (if present), and `description`. | Parts Clerk has 3 loaner generators on the shelf. Clicks "Loan Out" on one and can't confirm which one the dialog is for. Accidentally records the loan against the wrong S/N. Creates an audit trail error. |
| BUG-BM-005 | **`billing/invoices/page.tsx` — "Send (N)" batch button had no error handling, no loading state, and no toast feedback.** `handleBatchSend` was a bare `await batchSendInvoices(...)` with no try/catch. If the Convex mutation failed (e.g. network drop, org mismatch), the billing manager saw nothing — no error, no toast, no visual state change. The button also remained clickable during the in-flight mutation, allowing double-sends that could mark invoices as SENT twice. | `app/(app)/billing/invoices/page.tsx` | Added `batchSending` state. Wrapped `batchSendInvoices` call in try/catch/finally: success shows `toast.success("N invoice(s) sent.")`, failure shows `toast.error(err.message)`. Button gets `disabled={batchSending}` and label changes to "Sending…" during in-flight. | Billing manager selects 8 draft invoices, clicks "Send (8)" before a network blip. No feedback. Clicks again. 8 invoices are now sent twice (duplicate SENT records) or the manager gives up thinking the feature is broken. |
| BUG-BM-006 | **`billing/time-approval/page.tsx` — Duration column showed raw minutes (e.g. "485") making 8-hour entries completely unreadable.** `fmtDuration` returned `String(Math.round(ms / 60000))`, so a 485-minute (8h 5m) entry rendered as "485" with column header "Duration (min)". A billing manager reviewing time entries to approve had to mentally divide every value by 60 to determine hours — error-prone when approving for payroll purposes. | `app/(app)/billing/time-approval/page.tsx` | Updated `fmtDuration` to format as `Xh Ym` (e.g. "8h 5m") or `Ym` for under-60-minute entries (matching the pattern already used in `time-clock/page.tsx`). Updated all three table column headers from "Duration (min)" to "Duration". | Billing manager approving time entries sees "485" for a tech entry. Not sure if that's minutes or something else. Approves it. Bookkeeper downloads the CSV and sees the same "485" — now it's a billing discrepancy because the hours aren't clear. |
| BUG-BM-007 | **`billing/invoices/[id]/page.tsx` — Line item edit dialog sent `NaN` to the backend when non-numeric input was entered in Qty or Unit Price fields.** `parseFloat("abc") !== ""` is true, so `qty: NaN` was passed to `updateInvoiceLineItem`. The backend might silently accept NaN (resulting in a $0 or broken line item) or throw an unformatted error. Either way, the billing manager got no clear validation message. Also: qty of 0 would pass through (0 !== undefined), creating a $0 line item with no warning. | `app/(app)/billing/invoices/[id]/page.tsx` | Added pre-mutation validation: `isNaN(parsedQty) \|\| parsedQty <= 0` blocks with "Quantity must be a number greater than zero."; `isNaN(parsedPrice) \|\| parsedPrice < 0` blocks with "Unit price must be a valid non-negative number."; discount outside 0–100 blocks with range message. Validation errors set `error` state (displayed in the dialog). Mutation only fires after all checks pass. | Billing manager editing a line item accidentally types "2.5k" (shorthand) in the qty field and clicks Save. Dialog closes with no error. The invoice shows qty NaN, total = $0 or NaN. Customer receives a $0 invoice. Now validation catches it immediately. |
| BUG-BM-008 | **`billing/ar-dashboard/page.tsx` — Customer Balance table fell back to the raw Convex document ID when a customer lookup failed.** `customerMap.get(cb.customerId) ?? cb.customerId` displayed values like `"k57abc123def456"` if the customer record was inactive, deleted, or if `listAllCustomers` had not yet resolved. A billing manager reviewing the AR aging table would see cryptic IDs for customers that couldn't be resolved — impossible to reconcile. | `app/(app)/billing/ar-dashboard/page.tsx` | Changed fallback from `cb.customerId` to `"Unknown Customer"`. The overdue invoices table already used `"—"` as its fallback (consistent); now the customer balance table uses a readable label instead of leaking internal IDs. | Billing manager shows the AR aging report to ownership. One row shows "k57abc…" with $12,400 outstanding. Owner asks "who is that?" Billing manager doesn't know. The raw ID is useless without a database lookup. Now reads "Unknown Customer." |
| BUG-QCM-C1 | **`work-orders/[id]/AdComplianceTab.tsx` — `pending_determination` ADs displayed with the same red "Not Complied" badge as `not_complied` ADs.** The `AdStatusBadge` function grouped both statuses: `if (complianceStatus === "not_complied" || complianceStatus === "pending_determination")` → red "Not Complied". `pending_determination` = applicability not yet confirmed; `not_complied` = confirmed applicable, work never done (hard RTS block). Showing pending ADs as "Not Complied" on the WO's Compliance tab inflated the apparent non-compliance signal and contradicted the amber "Pending" badge shown for the same records on the AD/SB and Audit Trail pages (fixed for those files in BUG-QCM-AT-001, but this file was missed). | `app/(app)/work-orders/[id]/AdComplianceTab.tsx` | Split the `if` into two branches: `pending_determination` → amber "Pending" badge (Clock icon); `not_complied` → red "Not Complied" badge (AlertTriangle). Consistent with all other compliance views. | QCM opens a WO's AD Compliance tab during review. Sees 2 red "Not Complied" badges — one is genuinely non-compliant, one is just pending applicability review. Both look identical. QCM rejects the WO as non-compliant. The pending AD turns out to be not applicable to this aircraft. QCM wasted 30 minutes on a false alarm. |
| BUG-QCM-C2 | **`work-orders/[id]/AdComplianceTab.tsx` — Summary cards showed "Overdue: 0" in neutral color while the blocking banner was red.** The 4-card summary grid only tracked `overdueCount` (due-date overruns). An aircraft with 0 date-overruns but 3 never-performed ADs (`notCompliedCount: 3`) showed "Overdue: 0" in neutral text while `summary.hasBlockingItems` fired the red blocking banner below — contradictory signals on the same screen. | `app/(app)/work-orders/[id]/AdComplianceTab.tsx` | Added a 5th "Not Complied" card to the summary grid. Lights red when `notCompliedCount > 0`. Grid changed from `grid-cols-4` to `grid-cols-5`. `notCompliedCount` accessed via cast since it may not yet be in the TypeScript surface. | QCM checks a WO's AD Compliance summary: "Overdue: 0, Due Soon: 0" — looks okay. Scrolls down and sees a red blocking banner. Confused. Summary cards said OK but something is blocking. Now the "Not Complied: 3" card in red makes the contradiction impossible. |
| BUG-QCM-C3 | **`compliance/audit-trail/page.tsx` — Same "Overdue: 0" / red banner contradiction as BUG-QCM-C2.** The `AdCompliancePanel` summary row in the Audit Trail page also lacked a `notCompliedCount` card, so aircraft with never-performed ADs showed the neutral "Overdue: 0" card while the blocking banner was red. | `app/(app)/compliance/audit-trail/page.tsx` | Added a 5th "Not Complied" card to the summary grid in `AdCompliancePanel`. Same cast pattern as BUG-QCM-C2. Grid changed to 5 columns. | QCM drilling into fleet compliance pre-FAA audit sees "Overdue: 0" for N776AB but red blocking banner. Checks AD list — finds 2 ADs never performed. Now "Not Complied: 2" card is immediately visible at the top. |
| BUG-QCM-C4 | **`work-orders/[id]/AdComplianceTab.tsx` — Blocking banner said "Aircraft may not return to service until resolved" for ALL blocking item types including `pending_determination`.** The banner text was a generic string with no breakdown of what was blocking. A `pending_determination` AD hasn't been confirmed as applicable yet — it may not actually block RTS — but the message implied a hard airworthiness hold. QCM could escalate unnecessarily or lose trust in the compliance tool. Also: the banner gave no count breakdown, so the QCM couldn't tell at a glance whether 1 or 10 ADs were involved. | `app/(app)/work-orders/[id]/AdComplianceTab.tsx` | Replaced the static string with a dynamic breakdown: renders overdue / not-complied / pending-determination counts separately (same pattern as the Audit Trail `BUG-QCM-AT-002` fix). Example: "N776AB has 1 overdue and 2 pending-determination ADs." The QCM immediately sees severity and scale. | QCM reviewing WO sees red blocking banner: "has blocking AD items. Aircraft may not return to service." Escalates to DOM. DOM grounds the aircraft. Turns out all 3 ADs are in `pending_determination` (not even confirmed applicable). Aircraft was grounded for a false alarm. Banner now says "1 pending-determination AD" — the QCM investigates before escalating. |

### Cycle 24 — 2026-03-02 (Persona: DOM — Director of Maintenance)
Journey traced: Fleet → Aircraft → Logbook → Predictions → Training

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-DOM-9 | **`Filter` icon imported but never used in training/page.tsx.** `Filter` was imported from lucide-react but never referenced in JSX. Dead import adds noise to the bundle and confuses any developer reading the imports. | `app/(app)/personnel/training/page.tsx` | Removed `Filter` from the lucide-react import block. | Minor: dead code, potential build analyzer noise. |
| BUG-DOM-10 | **Predictions tab counts included resolved/dismissed predictions.** The severity tab badges (e.g. "Critical (5)") counted ALL predictions for that severity — including already-resolved or dismissed ones — not just active/actionable ones. A DOM reviewing the fleet health summary saw "Critical (5)" but only 2 were actually active and needing attention. The summary cards (top-level stat cards) correctly counted only active ones, creating contradictory numbers on the same page. | `app/(app)/fleet/predictions/page.tsx` | `tabCounts` now filters `p.status === "active"` before incrementing per-severity counts. Tab badges now match the stat cards: both show only active prediction counts. | DOM checks Predictions page, sees "Critical (5)" tab and "Critical: 2" stat card on the same screen. Which is correct? Neither is trusted. Now both show the same active-only count. |
| BUG-DOM-11 | **Fleet Calendar showed no empty state when a month had zero scheduled work orders.** When `workOrders` resolved to `[]` for a given month, the DOM saw a complete calendar grid with 28–31 empty day cells and no message explaining why. Looked like a loading failure or a data display bug rather than "no WOs scheduled this month." | `app/(app)/fleet/calendar/page.tsx` | Added a `workOrders.length === 0` branch before the full calendar grid render: shows a centered empty state card with a `CalendarDays` icon, "No scheduled work orders this month," and a helper text explaining what triggers entries to appear. | DOM checks the March calendar, sees a blank grid, assumes the feature is broken. Files a bug report. Wastes 10 minutes. Now sees an explicit "No scheduled work orders this month" message. |
| BUG-DOM-12 | **Personnel page had no path from technician cards to the Training page.** A DOM reviewing their personnel roster who wanted to check a specific technician's training compliance had to navigate to the sidebar's Training link separately — there was no "Training" shortcut on the personnel card itself. With 15+ technicians, finding the right tech in the Training list after jumping to a different page was an extra step on every review. | `app/(app)/personnel/page.tsx` | Added `GraduationCap` icon to imports. Added a `<Button asChild variant="ghost">` with a `<Link to="/personnel/training">` shortcut alongside the "Edit Shift" button on each technician workload row. | DOM reviewing personnel roster wants to see if Tech A's HazMat certification has expired. Previously had to close the tab mentally, click Training in the sidebar, scroll to find Tech A. Now clicks "Training" link directly from the personnel card. |

### Cycle 25 — 2026-03-02 (Persona: Shop Manager)
Journey traced: Dashboard → Work Orders → Scheduling → Reports

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-SM-001 | **Reports page charts showed duplicate "Jan" (and other month) labels when the date range spanned multiple calendar years.** `revenueData` and `throughputData` both built their X-axis label as the short month name only (e.g., "Jan"). If the selected date range covered Jan 2025 through Mar 2026, the chart rendered two "Jan" and two "Feb" columns — indistinguishable. A shop manager comparing year-over-year revenue trends would see overlapping bars with no way to tell which year each represented. | `app/(app)/reports/page.tsx` | Added multi-year detection: compute the set of unique calendar years in the sorted entries. If `years.size > 1`, use `"MMM 'YY"` format (e.g., "Jan '25", "Jan '26"); if single year, keep the short name as before. Applied to both `revenueData` and `throughputData`. | Shop manager sets date range to Jan 2025–Mar 2026 to do a year-over-year review. Monthly Revenue chart shows two "Jan" columns side-by-side with no year. The $45k bar is for January 2025 or 2026? Impossible to tell. Decision made on wrong month's data. |
| BUG-SM-002 | **Dashboard "Certs Expiring (30d)" secondary KPI card linked to `/compliance/audit-trail` — the AD/SB compliance page — instead of `/personnel` where technician certificates are actually managed.** A shop manager who sees "2 certs expiring" and clicks the card lands on the AD compliance audit trail showing aircraft airworthiness directives, not technician IA/AMT certificate records. They'd then have to navigate back and find the personnel page manually. | `app/(app)/dashboard/page.tsx` | Changed `href` for the "Certs Expiring (30d)" card from `"/compliance/audit-trail"` to `"/personnel"`. The Personnel page is where technician cert records and expiry dates are displayed. | Shop manager gets a Monday morning alert: "2 certs expiring." Clicks the card. Lands on AD compliance. Confused — this shows aircraft ADs, not tech certs. Wastes 5 minutes navigating to Personnel. |
| BUG-SM-003 | **"Awaiting Parts" tab in Work Orders included closed, cancelled, and voided WOs that still had historic pending-part records.** Both `filterWorkOrders("awaiting_parts")` and the tab badge count used `partsOnOrder > 0` without excluding terminal-status WOs. A shop manager scanning the "Awaiting Parts: 7" count would find entries for WOs already closed — creating false urgency and making it impossible to know how many ACTIVE jobs were truly blocked on parts. | `app/(app)/work-orders/page.tsx` | Added status exclusion to both the `filterWorkOrders("awaiting_parts")` case and the `counts` accumulator: `!["closed", "cancelled", "voided"].includes(wo.status)`. Only active/non-terminal WOs contribute to the count and tab list. | Shop manager sees "Awaiting Parts: 7." Opens the tab. Three of the entries are closed WOs from last month. The real blocked-jobs count is 4, not 7. Shop manager escalates the wrong POs to procurement. |
| BUG-SM-004 | **Reports page CSV export buttons for "Monthly Revenue" and "WO Throughput" had no disabled state when data was empty.** Clicking either button with no data silently did nothing — no toast, no visual feedback, no error. The button looked fully interactive (same styling as when data exists) but clicking it produced zero result. A shop manager who set a future date range or a range with no closed invoices would click the button repeatedly thinking it was broken. | `app/(app)/reports/page.tsx` | Added `disabled={revenueData.length === 0}` and `disabled={throughputData.length === 0}` to the respective CSV buttons. Removed the now-redundant `if (data.length)` guard inside the handler (the button being disabled makes it unreachable). shadcn Button's disabled styling automatically dims and disables pointer events. | Shop manager sets a date range that has no paid invoices, clicks "CSV," nothing happens. Clicks again. Assumes the feature is broken. Asks IT. Answer: no data in range. Now the button is visibly grayed out. |

### Cycle 26 — 2026-03-02 (Persona: Lead Technician)
Journey traced: My Work → Task Cards → Steps → Sign-off → Handoff Notes

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-LT3-001 | **`SignStepDialog` did not reset `partsInstalled` or `approvedDataRef` after a successful sign-off.** `handleSign` cleared `pin`, `notes`, and `photoStorageIds`, but never cleared `partsInstalled` or `approvedDataRef`. The component stays mounted (dialog open state changes, component doesn't unmount), so all parts from Step 1's sign remain in state when the tech opens the dialog for Step 2. Those parts are then submitted with Step 2's maintenance record — permanently linking the wrong parts to the wrong step under 14 CFR 43.9(a)(4). | `app/(app)/work-orders/[id]/tasks/[cardId]/_components/SignStepDialog.tsx` | Added `setApprovedDataRef("")`, `setPartsInstalled([])`, and `setRating(requiresIa ? "ia" : "airframe")` to the success reset block in `handleSign`. | Tech signs Step 1 (replaced filter P/N AB-123). Immediately signs Step 2 (tightened fasteners — no parts). Step 2's maintenance record now shows P/N AB-123 was installed. Permanent data integrity error, uncorrectable after sign. |
| BUG-LT3-002 | **`SignStepDialog` `rating` state did not reset between steps when a step requiring IA followed one that didn't (or vice versa).** `useState(requiresIa ? "ia" : "airframe")` only computes the initial value once at mount. As the dialog component stays mounted and `requiresIa` changes between openings (different step targets), the `rating` field stayed on whatever was last selected. A tech signing a non-IA step after an IA step would see "IA" pre-selected as the rating, and could sign the card with the wrong rating if they didn't notice. | `app/(app)/work-orders/[id]/tasks/[cardId]/_components/SignStepDialog.tsx` | Added `useEffect` that resets `rating` to `requiresIa ? "ia" : "airframe"` whenever `open` becomes `true`. Also imported `useEffect`. | Tech signs an IA-required step for engine run-up, then opens sign dialog for a routine airframe step. Rating shows "IA" — if not caught, the record says IA was exercised for a step that didn't require it (regulatory misrepresentation). |
| BUG-LT3-003 | **Task cards with 0 itemized steps could never be signed off — `allStepsDone` was always `false` for empty step lists due to `totalSteps > 0` guard.** A valid task card (e.g. "Perform functional check per POH section 4-4" — the card IS the record) with no sub-steps would show "0/0 steps done" and "Complete all 0 remaining steps before signing" with the Sign button permanently disabled. The tech had no path to certify this card without creating a dummy step. | `app/(app)/work-orders/[id]/tasks/[cardId]/page.tsx` | Changed `allStepsDone` from `totalSteps > 0 && taskCard.steps.every(...)` to `totalSteps === 0 \|\| taskCard.steps.every(...)`. Updated the sign-off card description text to say "No steps required — ready for card-level sign-off." for the 0-step case. | Lead tech creates a simple ops-check task card with no steps. Task complete. Clicks "Sign Card." Button is grayed out, message says "Complete all 0 remaining steps." No way forward — task card can never be certified without hacking in a fake step. |
| BUG-LT3-004 | **`TaskCardList.tsx` on the WO detail page had the same `totalSteps > 0` bug: 0-step cards were never highlighted amber ("Awaiting Sign-Off").** The WO detail card list uses `awaitingSignOff = totalSteps > 0 && completedSteps === totalSteps && ...`. A 0-step ready-to-sign card appeared with a plain white border — identical to an in-progress card — giving no visual cue to the shop manager or QCM that it was ready for cert. | `app/(app)/work-orders/[id]/_components/TaskCardList.tsx` | Changed the `awaitingSignOff` check to `(totalSteps === 0 \|\| completedSteps === totalSteps) && tc.status !== "complete" && tc.status !== "voided"`. | Shop manager reviewing WO detail sees 3 task cards. Two have amber highlight (awaiting sign). Third (0 steps) looks plain. Manager assumes it's not ready and skips it. Card never gets signed. WO can't close. Customer waits an extra day while manager hunts for the blocking card. |

### Cycle 27 — 2026-03-02 (Persona: Parts Clerk)
Journey traced: Inventory → Receiving → Shipping → Rotables → Loaners

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-PC-005 | **Loaner Return dialog showed no item identification.** The "Loan Out" dialog showed the part number, S/N, and description of the loaner before accepting input. The "Return" dialog was blank — just two text areas with no indication of which item was being returned. With multiple parts on loan, a parts clerk who accidentally clicked "Return" on the wrong row had zero information to catch the mistake before submitting. | `app/(app)/parts/loaners/page.tsx` | Added an item-info block to the Return dialog (same pattern as the Loan Out dialog) that displays the loaner's `partNumber`, `serialNumber`, and `description` as a read-only header before the form fields. | Parts clerk processes 4 loaners returned at once. Accidentally opens Return on the wrong item. Dialog shows nothing identifying the item — submits. Wrong return logged, loaner inventory corrupt. Now the dialog leads with clear part identification. |
| BUG-PC-006 | **Rotable "Install" action fired immediately with a single click — no confirmation dialog.** "Remove" and "Condemn" both had `AlertDialog` confirmation dialogs with explanatory text. "Install" (a similarly significant aircraft maintenance action) called `handleAction(id, "installed")` directly on button click. One mis-tap during a busy receiving shift installs the wrong S/N against the wrong aircraft with no recourse. | `app/(app)/parts/rotables/page.tsx` | Added `installTarget` state and `AlertDialog` confirmation identical in pattern to the existing Remove confirmation. The dialog explains the action creates a permanent installation record. "Install" button now sets `installTarget` instead of firing the action immediately. | Parts clerk selects a replacement alternator to install. Mis-taps the wrong S/N row. Install record created for the wrong unit — wrong S/N now recorded as installed on the aircraft. No undo. Now requires a confirmation click naming the specific P/N and S/N. |
| BUG-PC-007 | **Purchase Orders page (`/billing/purchase-orders`) had no sidebar navigation entry.** POs exist and are fully implemented at `/billing/purchase-orders`, but the Billing section of the sidebar listed Counter Sales, Labor Kits, Warranty Claims, and Customer Portal — no PO link. A parts clerk tracking back-ordered parts against open POs had no way to reach the PO list without typing the URL directly. | `components/AppSidebar.tsx` | Added `{ title: "Purchase Orders", href: "/billing/purchase-orders" }` as the first child of the Billing section. | Parts clerk receives a shipment and needs to match it against an open PO. Looks in the Billing sidebar — no POs. Looks in Parts sidebar — no POs. Has to ask the billing manager for the URL or navigate by memory. |
| BUG-PC-008 | **ShipmentDetails add-item form silently replaced invalid or zero quantity with `1`.** `Number(qty) \|\| 1` treats any non-numeric input, empty string, or "0" as valid — silently substituting 1. A parts clerk entering "abc" or "0" in the Qty field sees no error, and the item is saved with quantity 1 without any indication that the input was modified. | `app/(app)/parts/shipping/page.tsx` | `handleAdd` now validates: empty/non-numeric/non-integer/≤0 values in `qty` trigger a `toast.error(...)` and return early before submission. Parts are trimmed before saving. | Parts clerk receives 12 units, types "1z" by mistake in qty field. Form submits, 1 unit is recorded. Shipment manifest shows 1, physical stock is 12. Discrepancy discovered at next inventory count — hours of reconciliation. Now shows immediate error: "Quantity must be a whole number greater than zero." |

## Bug Hunter Fixes — Billing Manager Cycle 2 (2026-03-02)

| ID | Bug | File | Fix | User Impact |
|----|-----|------|-----|-------------|
| BUG-BM-009 | **OTC Counter Sales: inventory parts added with $0 unit price and no warning.** `addPartToCart` hardcoded `unitPrice: 0`. Parts in the inventory table have no stored selling price field. A billing manager or parts clerk could add a $100 avionics component to the cart, tab past the price field, and process a $0 sale with no alert. | `app/(app)/billing/otc/page.tsx` | Added `toast.warning(...)` immediately after calling `setCart` in `addPartToCart`, informing the user the unit price needs to be set before completing the sale. | Clerk adds 3 parts from inventory to a walk-in counter sale. All show $0. Clerk completes the sale. Receipt says $0.00. Customer is gone. Revenue is $0 in the books. Now shows an immediate toast: "Set the unit price before completing sale." |
| BUG-BM-010 | **OTC Counter Sales: manual item entry allowed $0 price through validation.** `addManualItem` checked `!manualPrice` (falsy on empty string) but "0" passes that check — `!"0"` is `false`. Items with `unitPrice: 0` could be added manually with no error, then `parseFloat("0") \|\| 0 = 0` persisted the $0. Quantity "0" or non-integer also silently passed through as `parseInt("abc") \|\| 1 = 1`. | `app/(app)/billing/otc/page.tsx` | Added explicit numeric validation: `parsedPrice <= 0` triggers `toast.error("Unit price must be greater than $0.00.")`. Also validates qty with `parsedQty < 1` guard. Returns early before adding to cart. | Billing manager manually adds a service line item at $0 (maybe placeholder-typing). Item added. Sale goes through as $0 revenue. Now shows clear error before the item enters the cart. |
| BUG-BM-011 | **OTC Counter Sales: "Complete Sale" button had no zero-price line item guard.** Even with the cart containing $0-priced items, the sale could be finalized through `handleCompleteSale`. A billing manager distracted by a phone call might miss the cart total of $0.00 and click "Complete Sale". | `app/(app)/billing/otc/page.tsx` | Added pre-submit validation in `handleCompleteSale`: collects all items where `unitPrice <= 0`, names them in the error toast, and returns early before firing the Convex mutation. | Last line of defense: cart has 5 parts, 2 at $0. "Complete Sale" is clicked. Toast lists the zero-priced items by name and blocks the transaction. Billing manager must price all items first. |
| BUG-BM-012 | **Time Approval page: Approve/Reject buttons silently did nothing for users without a technician record.** `handleApprove` and `handleReject` had `if (!orgId \|\| !techId) return` with no user feedback. A billing manager or supervisor without a technician record in Personnel would click "Approve" and nothing would happen — no toast, no error, no indication that a technician profile is required. | `app/(app)/billing/time-approval/page.tsx` | Separated the `orgId` and `techId` guards. If `techId` is missing, `toast.error(...)` explains that a Personnel technician record is required and how to create one, then returns early. | Finance manager reviews 12 pending time entries. Clicks "Approve" on each. Nothing happens. Clicks all 12 again. Still nothing. Calls IT support. Root cause: no technician record. Now: instant error toast explaining exactly what's missing. |

## Bug Hunter Fixes — QCM Inspector Cycle (2026-03-02)

| ID | Bug | File | Fix | User Impact |
|----|-----|------|-----|-------------|
| BUG-030 | **QCM Review: Close Readiness panel never auto-selected when exactly one `pending_signoff` WO existed.** `CloseReadinessPanel` always initialized `selectedWoId` to `null`, so the readiness panel was blank even when there was only one possible candidate. The QCM had to open the dropdown, recognize the WO, and click it — even though there was no meaningful choice to make. | `compliance/qcm-review/page.tsx` | Added a `useEffect` in `CloseReadinessPanel` that auto-sets `selectedWoId` to the first `pending_signoff` WO when exactly one exists and no selection has been made yet. Also imports `useEffect` from React. `pendingSignoffWos` is memoized to avoid stale closures. | QCM lands on the review page. One WO is ready for sign-off. Panel is blank. QCM opens dropdown, clicks the only entry. Now the readiness checklist loads immediately — zero extra clicks. |
| BUG-031 | **RTS PRE-8 ("Maintenance Records Signed") stuck at PENDING forever when WO had zero maintenance records and no backend blocker fired.** `pre8Status` showed PENDING when `maintenanceRecords.length === 0` — giving the inspector no indication of whether records were required or simply not applicable. The backend fires `RTS_NO_MAINTENANCE_RECORDS` when records are a hard requirement; absence of that blocker means records are not required for this WO type. PENDING in this state is misleading — inspectors often created dummy maintenance records just to make the card green. | `work-orders/[id]/rts/_components/derivePreconditions.ts` | Changed logic: if `maintenanceRecords.length === 0` and no blocker is active → PASS (records not required). Also updated PRE-8 description to show "No maintenance records on file — not required for this work order type." when in that state. | IA doing a post-inspection RTS with no maintenance records (WO type doesn't require them) sees PRE-8 stuck at PENDING. Can't tell if they need to create records. Creates a blank record just to advance. Now PRE-8 shows PASS with an explanatory description. |
| BUG-032 | **`FleetComplianceStats` AD Issues count excluded never-performed ADs (`notCompliedAds`).** `adIssueCount = overdueAds + pendingAds` only counted date-overrun and pending-applicability ADs. ADs that were never performed (`complianceStatus: "not_complied"`) didn't appear in the fleet dashboard count. A fleet with 4 never-performed ADs showed "AD Issues: 0 · all compliant" in the header stats. BACKEND-NEEDED: `getFleetAdSummary` needs to add `notCompliedAds` to `fleetTotals`. Frontend now reads it defensively via cast; falls back to 0 until backend is updated. | `compliance/_components/FleetComplianceStats.tsx` | Added `notCompliedAds` via defensive cast. Updated `adIssueCount = overdueAds + notCompliedAds + pendingAds`. Updated card border, number color, and subtitle to go red on `notCompliedAds > 0` (same severity as `overdueAds`). Subtitle now shows e.g. "2 overdue · 4 not complied · 1 pending". | DOM reviews the compliance dashboard. Four aircraft have ADs that were never performed (no baseline compliance established). Dashboard shows "AD Issues: 0 — all compliant." DOM reports fleet as compliant to the FAA. Audit reveals 4 not-complied ADs. |

## Bug Hunter Fixes — DOM Cycle (2026-03-02 Cycle 30)

| ID | Bug | File | Fix | User Impact |
|----|-----|------|-----|-------------|
| BUG-DOM-001 | **Logbook page flashed empty state while maintenance records were loading.** `isLoading` only waited for org context and aircraft to load (`!orgLoaded \|\| aircraft === undefined`). Once the aircraft record arrived, `isLoading` became `false` even though `rawRecords` was still `undefined`. The timeline rendered with `(records ?? [])` = `[]`, showing "No maintenance records for this aircraft" for 1–3 seconds before records arrived. Particularly confusing for a DOM with a full logbook. | `app/(app)/fleet/[tail]/logbook/page.tsx` | Changed `isLoading` to include records: `!orgLoaded \|\| aircraft === undefined \|\| (aircraft !== null && rawRecords === undefined)`. The `aircraft !== null` guard prevents indefinite loading when aircraft is genuinely not found. | DOM opens an aircraft's logbook. Sees "No maintenance records" for 2 seconds, then 87 entries appear. DOM thinks the logbook is empty and starts calling the shop to ask who forgot to log maintenance. Now shows skeleton until records arrive. |
| BUG-DOM-002 | **Predictions page showed all-status predictions (active + acknowledged + resolved + dismissed) with no way to filter by status.** Tab badges counted only `active` predictions (e.g. "Critical (2)"), but clicking a tab revealed all critical predictions including dismissed and resolved ones. A DOM would click "Critical (2)" expecting 2 actionable items and see 8 rows — 6 of which were already handled. No way to filter to just actionable items. | `app/(app)/fleet/predictions/page.tsx` | Added `statusFilter` state defaulting to `"active"`. Wired into the `filtered` useMemo alongside other filters. Added a Status `<Select>` to the filter bar with options: All Statuses / Active / Acknowledged / Resolved / Dismissed. Default shows only active predictions matching what the tab badges advertise. | DOM opens Predictions. Critical tab shows "(2)". Clicks it. Sees 9 prediction cards — 7 are resolved from last month. Has to mentally parse which are new. Now defaults to active-only, matching what the tab badges communicate. |
| BUG-DOM-003 | **"Add Engine" and "Add Propeller" buttons silently disabled.** Aircraft detail page had `<Button disabled>Add Engine</Button>` and `<Button disabled>Add Propeller</Button>` — visually enabled (no `cursor-not-allowed` styling), producing zero feedback when clicked. A DOM trying to add a newly-overhauled engine to a freshly-acquired aircraft would click the button and nothing would happen. Zero indication of what to do instead. | `app/(app)/fleet/[tail]/page.tsx` | Removed `disabled` prop from both buttons. Added `onClick` handlers that call `toast.info(...)` explaining the feature is coming soon with a contact-support fallback. DOM now gets actionable feedback instead of a silent dead button. | New aircraft added to fleet. DOM navigates to Engines tab to add 2 turboprop engines. Clicks "Add Engine." Nothing. Clicks again. Nothing. Gives up, assumes the feature is broken. Now shows: "Engine management coming soon. Contact support to add or update engine records." |
| BUG-DOM-004 | **`"use client"` directives in logbook and predictions pages.** Both `fleet/[tail]/logbook/page.tsx` and `fleet/predictions/page.tsx` had `"use client";` as their first line — a Next.js App Router directive with no meaning in this Vite/React Router project. While harmless at runtime, it signals to future developers that these files were ported from a Next.js codebase without cleanup, and could cause confusion when debugging rendering behavior. | Both files above | Removed the `"use client";` directive from both page files. | Code hygiene / developer confusion. Consistent with all other pages in the codebase which do not use this directive. |

## Bug Hunter Fixes — Shop Manager Cycle (2026-03-02 Cycle 31)

| ID | Bug | File | Fix | User Impact |
|----|-----|------|-----|-------------|
| BUG-SM-001 | **WO list page subtitle said "X in progress" but the "Active" tab included `open` status WOs too.** The subtitle computed count using `["in_progress", "open_discrepancies"]` — but the "Active" filter tab includes all three: `open`, `in_progress`, `open_discrepancies`. A shop manager would see "10 active" in the KPI card, land on the WO list, and read "15 total · 3 in progress" — not realizing 7 `open` WOs were already in the Active tab. | `app/(app)/work-orders/page.tsx` | Changed subtitle filter to include `open` status and changed label from "in progress" to "active": `["open", "in_progress", "open_discrepancies"]`. Now matches what the Active tab shows. | Shop manager looks at the WO list expecting to understand workload at a glance. Subtitle says "3 in progress" but Active tab shows 10. Assumes 7 WOs are miscategorized. Now reads "10 active" — consistent with the tab count. |
| BUG-SM-002 | **WO detail "Squawks" tab was labeled "Squawks" but rendered all work items (task cards + discrepancies combined).** `workItems` is built by concatenating `taskCards` + `discrepancies`. The tab badge showed `workItems.length` — e.g. "Squawks 8" when 6 were routine task cards and only 2 were squawks. A shop manager or QCM scanning for squawk count would see an inflated number. | `app/(app)/work-orders/[id]/page.tsx` | Renamed the tab label from "Squawks" to "Tasks & Findings" — accurately reflecting that it lists all work items. | Shop manager opens a WO. Sees "Squawks: 8" in the tab badge. Gets concerned — calls the lead tech asking about 8 open squawks. Lead tech explains only 2 are squawks; 6 are planned task cards. Now labeled "Tasks & Findings: 8" — no confusion. |
| BUG-SM-003 | **New WO form passed negative or NaN labor hour values to the backend.** The labor hours field had `min="0"` on the HTML `<input>` (which browsers can bypass) but `handleSubmit` used raw `parseFloat(estimatedLaborHours)` — so "abc" → `NaN`, "-5" → `-5` could be submitted. The backend would receive invalid values. | `app/(app)/work-orders/new/page.tsx` | Changed the value to `Math.max(0, parseFloat(estimatedLaborHours) \|\| 0) \|\| undefined` — coerces NaN to 0, clamps negatives to 0, then converts 0 back to `undefined` (treated as "no override"). | Shop manager accidentally types "-10" or "1hr" in the labor hours field. Backend receives invalid value. Now safely sanitized client-side before submission. |
| BUG-SM-004 | **Reports page silently showed no data when either date input was cleared, with no error message.** If `dateFrom` or `dateTo` was cleared to an empty string, `new Date("").getTime()` returns `NaN`. All timestamp comparisons with NaN return `false`, so all data was filtered out with no charts, no table rows, and no indication of why. The existing `dateRangeInvalid` error only triggered when both dates were present but inverted. | `app/(app)/reports/page.tsx` | Added `dateInputMissing` check (`!dateFrom \|\| !dateTo`). Added an amber warning banner when either date is missing. Fixed `fromTs`/`toTs` to only call `new Date()` when the date string is non-empty. | Shop manager clears the "From" date field to type a new one, tabs to something else before finishing. Charts go blank. No error. Manager assumes no revenue data for the period and escalates to billing. Now shows: "Both a From and To date are required to display report data." |

## Bug Hunter Fixes — Lead Technician Cycle (2026-03-02 Cycle 32)

| ID | Bug | File | Fix | User Impact |
|----|-----|------|-----|-------------|
| BUG-LT-HUNT-001 | **`SignStepDialog`: PIN field and error banner not cleared on re-open.** The existing `useEffect([open, requiresIa])` only reset the `rating` dropdown. `pin` and `error` state persisted across dialog open/close cycles. If a tech entered a wrong PIN, saw the "Invalid PIN" error, closed the dialog, and re-opened it for another step, the old error banner was still visible and the PIN field still showed the previous (wrong) entry. `SignCardDialog` had this exact bug fixed already (BUG-LT4-002) but the fix was never ported to `SignStepDialog`. | `app/(app)/work-orders/[id]/tasks/[cardId]/_components/SignStepDialog.tsx` | Added `setPin(""); setError(null);` to the existing `useEffect` that fires on `open`. Both fields now reset to clean state each time the dialog opens. | Tech fails a sign attempt (wrong PIN), closes dialog, opens it again for a different step. Old "Invalid PIN" error is still visible before they've typed anything. They think the step is already locked or something is broken. They call the shop manager. Now: clean dialog every time. |
| BUG-LT-HUNT-002 | **`MarkNaDialog`: `reason` text and `error` banner not cleared on re-open.** No `useEffect` existed to clear state when the dialog opened. If a tech opened the N/A dialog for Step 3, typed a reason, then closed without submitting, then opened it for Step 5, Step 5's dialog pre-filled with Step 3's reason text. Similarly, a stale error from a failed N/A attempt persisted on re-open. This is a maintenance records integrity risk: the N/A reason is a permanent record under 14 CFR 43.9(a)(2) — a tech might not notice the pre-filled wrong reason and submit it for the wrong step. | `app/(app)/work-orders/[id]/tasks/[cardId]/_components/MarkNaDialog.tsx` | Added `import { useState, useEffect }` and a `useEffect(() => { if (open) { setReason(""); setError(null); } }, [open])`. Also added `useEffect` import. Both fields now reset each time the dialog opens. | Tech opens N/A dialog for Step 3 ("Aircraft does not have oxygen system"), types reason, decides to cancel. Then opens N/A dialog for Step 7. Step 7's dialog shows Step 3's reason pre-filled. Tech doesn't notice, clicks "Mark N/A". Step 7 now has the wrong reason in the permanent maintenance record. Now: fresh dialog every time. |
| BUG-PC-009 | **Loaners return dialog did not reset `returnForm` state on close.** The `onOpenChange` handler only called `setReturnDialogId(null)` — it never cleared `returnForm`. `returnForm` was only reset inside `handleReturn` on successful submission (line 195). If a clerk opened the return dialog, typed condition notes, then dismissed via X or "Cancel", those notes persisted. Opening the return dialog for the next item would show the previous item's condition notes pre-filled. A clerk returning multiple items in sequence could easily submit wrong condition-in notes for the second item. | `app/(app)/parts/loaners/page.tsx` | Updated `onOpenChange` handler to call `setReturnForm({ conditionIn: "", notes: "" })` when closing. Updated inline Cancel button `onClick` to also reset form. Both paths now clean state identically. | Parts clerk returns a loaner prop governor with notes "shaft seal seeping". Decides to cancel — there's an issue. Opens return dialog for the next item (a different governor). Condition field still shows "shaft seal seeping" from the first item. Clerk doesn't notice, submits. Wrong condition note is permanently recorded for the second unit. Now: fresh form every time. |
| BUG-PC-010 | **"Add Part" page back button and Cancel link both routed to `/parts/requests` (Parts Queue) instead of `/parts` (Inventory).** The "Receive Part Into Inventory" form has an `<ArrowLeft> Parts Queue` back button at the top and a Cancel button at the bottom — both linked to `/parts/requests`. A parts clerk who lands on this page from the inventory list (`/parts → + Add Part`) has no way to cancel back to inventory. Clicking "Cancel" drops them on the parts receiving queue instead of where they came from. | `app/(app)/parts/new/page.tsx` | Changed both Link `to` props from `/parts/requests` to `/parts`. Relabeled back button from "Parts Queue" to "Parts Inventory". | Parts clerk is on the inventory list, accidentally clicks "Add Part" instead of clicking a part row. Clicks Cancel to go back. Lands on the receiving queue instead of the inventory list. Confused, navigates back using the browser button. Now: Cancel returns to inventory as expected. |
| BUG-PC-011 | **Loaners "Loan Out" dialog allowed setting expected return date in the past.** The `<Input type="date">` for expected return date had no `min` attribute. A parts clerk could accidentally (or via autofill) select yesterday's date. The item would be loaned out and immediately flagged as overdue — adding noise to the overdue counter and overdue dashboard highlights before the item ever left the shelf. | `app/(app)/parts/loaners/page.tsx` | Added `min={new Date().toISOString().slice(0, 10)}` to the expected return date input. Browser date picker now prevents selecting any date before today. | Parts clerk loans out a landing gear strut for a 2-week external repair. Date picker autocompletes to yesterday. Item instantly appears as overdue. DOM sees "1 overdue loaner" on the dashboard and calls the clerk to ask what happened. Now: minimum date is today — can't accidentally backdate a new loan. |
| BUG-PC-012 | **Inventory Count "Start Count" button silently failed when user lacked a technician record.** `handleStart` called `startCount({ countId, countedBy: techId })` with no guard on `techId`. If `techId` was `undefined` (user has no Personnel/technician record), the call would fire with `countedBy: undefined`, either silently failing or producing a confusing backend error with no user-visible explanation. Identical pattern to BUG-BM-012 (Time Approval) but in a different workflow. | `app/(app)/parts/inventory-count/page.tsx` | Added early-return guard: `if (!techId) { toast.error("A Personnel technician record is required to start a count. Create your technician profile in Personnel first."); return; }` — fires before `setActionLoading` so the button is not locked. | Parts admin clicks "Start Count" to kick off a cycle count. Nothing happens (or generic error appears). They retry three times. Call the IT desk. Root cause: no technician record. Now: instant toast explaining exactly what's needed and where to fix it. |

## Bug Hunter Fixes — Billing Manager Cycle 34 (2026-03-02)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-BM-005 | **Quotes page tab badge counts dropped to 0 when switching off "All" tab.** The `listQuotes` query passed `status: activeTab === "all" ? undefined : activeTab` — so when on the "SENT" tab, the API returned only SENT quotes. The counts object (`DRAFT: all.filter(...)`) was computed from that filtered result, so DRAFT=0, APPROVED=0, etc. All tab badges except the active one silently hid. A billing manager reviewing sent quotes couldn't see at a glance how many quotes were still in DRAFT or waiting on approval. | `app/(app)/billing/quotes/page.tsx` | Removed server-side status filter from the query (always loads all quotes). Added client-side status filter in the `filtered` useMemo (`activeTab === "all" ? quotes : quotes.filter(q => q.status === activeTab)`). Counts now always reflect the full dataset. | Billing manager clicks "Sent" tab. Sees "Sent (4)" but all other tabs show blank badges. Doesn't know if there are pending approvals or drafts to follow up on. Now all badges stay live as she moves between tabs. |
| BUG-BM-006 | **Invoices page had the exact same tab badge count bug.** Same server-side status filter pattern meant switching to "DRAFT" tab caused SENT, PARTIAL, PAID, VOID badges to all go to 0 and disappear. The billing manager viewing draft invoices had no way to see outstanding or partial counts without navigating back to "All". | `app/(app)/billing/invoices/page.tsx` | Same fix: removed server-side status filter, added client-side status filter in the `filtered` useMemo. All tab counts now always reflect the full invoice list. | Billing manager reviews DRAFT invoices to clean up before month-end. While there, wants to check how many are still in PARTIAL. All tabs show "(0)" except Draft. Has to click back to "All" to see the counts, then click "Partial". Extra navigation per review session. Now all badge counts visible at all times. |
| BUG-BM-007 | **AR Dashboard customer names briefly showed "—" after the overdue invoices table appeared.** `isLoading` only guarded on `aging` and `overdueInvoices`. The `customers` query was excluded. So the overdue table rendered with customer IDs unresolved, showing "—" in the Customer column until `customers` resolved (~100-300ms later). On a page centered on "who owes us money," showing blank customer names even briefly undermines the entire page. | `app/(app)/billing/ar-dashboard/page.tsx` | Added `customers === undefined` to the `isLoading` check. The skeleton loading state now persists until all three datasets (aging, overdueInvoices, customers) are ready. | Billing manager opens AR Dashboard. Table renders immediately but Customer column shows "—" for all rows. Manager thinks customer names are missing from invoices. Clicks into an invoice to verify. Now skeleton shows until all data is ready; table appears complete on first render. |
| BUG-BM-008 | **Time Clock WO filter dropdown capped at 20 work orders.** The filter `<Select>` used `(workOrders ?? []).slice(0, 20)` — so any shop with more than 20 work orders couldn't filter time entries by WO number past the 20th. The `workOrders` paginated query already loads `initialNumItems: 100`, so items 21–100 were available in memory but not shown in the dropdown. | `app/(app)/billing/time-clock/page.tsx` | Changed `.slice(0, 20)` to `.slice(0, 100)`. | Billing manager wants to filter time entries to WO-047. Shop has 45 open WOs. Opens the WO filter dropdown — WO-047 isn't there. Thinks the entry is against a different WO or there's a data error. Now shows up to 100 WOs in the filter dropdown, matching what the query already loads. |

## Bug Hunter Fixes — DOM Cycle 36 (2026-03-02)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-DOM-005 | **Fleet Calendar replaced the entire grid with an empty-state card when no work orders were scheduled for that month.** When `workOrders.length === 0`, the conditional rendered a full-width empty-state placeholder instead of the calendar grid. A DOM clicking through months to check upcoming scheduling or verify nothing was booked would see a blank card instead of the calendar — no day numbers, no grid, no visual date context at all. | `app/(app)/fleet/calendar/page.tsx` | Removed the `workOrders.length === 0` branch that replaced the calendar. The grid now always renders when `workOrders` is loaded. When the month has no events, a subtle info banner appears above the grid ("No scheduled work orders in March 2026 — …"). Calendar days are always visible. | DOM navigates to Fleet Calendar for a month with no scheduled WOs. Sees a blank card saying "No work orders." Has no way to visually confirm whether the month is truly clear or if the data didn't load. Now sees the full calendar grid with an inline note — can verify at a glance that no events exist on specific days. |
| BUG-DOM-006 | **`"use client"` Next.js directives in three frontend files with no effect in Vite/React Router.** `fleet/calendar/page.tsx`, `personnel/training/page.tsx`, and `fleet/[tail]/_components/AircraftAdComplianceTab.tsx` all had `"use client";` as their first line — a directive only meaningful in Next.js App Router. In this Vite project it is a no-op string expression. These files appear to have been authored or copy-pasted from a Next.js codebase. While harmless at runtime, the directives confuse developers debugging rendering behavior, create inconsistency across the codebase, and could mislead a future migration. | All three files above | Removed `"use client";` from all three files. Consistent with every other page in the codebase. | Developer adds a new page, copies one of these files as a template, and adds `"use client"` to all new pages unnecessarily. Now: consistent, clean entrypoints throughout. |
| BUG-DOM-007 | **Training page showed a perpetual `<Skeleton>` placeholder when the user had no organization context (`!orgId`).** When `orgId` was undefined (user hasn't completed onboarding or has no linked technician record), the page rendered a skeleton bar with no text, no error message, and no action. The DOM would stare at a loading animation that never resolved — with no explanation or next step. Identical issue was already fixed in the Fleet page (which shows a proper empty state with "Complete Setup" link). | `app/(app)/personnel/training/page.tsx` | Replaced the `<Skeleton>` fallback with a proper empty state: GraduationCap icon, "No organization context available" heading, and "Ask your administrator to link your account to a technician record." sub-text. Matches the pattern used on Fleet and other pages. | DOM opens Training page. Loading spinner never clears. DOM refreshes, waits, calls support. Root cause: account isn't linked to an org yet. Now: immediately shows a clear explanation with the action to take. |

## Bug Hunter Fixes — QCM Inspector Cycle 35 (2026-03-02)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-QCM-AT-003 | **AD/SB fleet table was missing a "Not Complied" column.** The fleet table showed Tracked \| Compliant \| Due Soon \| Overdue \| Pending — but no "Not Complied" column. An aircraft with `notCompliedCount=3, overdueCount=0` showed all dashes across every severity column. These 3 ADs (never performed, confirmed applicable) block RTS just as hard as overdue ones. The filter logic already correctly included these aircraft under the "Overdue" filter, but the table cells showed "—" in Overdue — a direct contradiction. A QCM inspector filtering for "Overdue" aircraft would see aircraft in the list with "—" in every red column, and couldn't understand why they appeared. | `app/(app)/compliance/ad-sb/page.tsx` | Added a "Not Complied" column between Overdue and Pending. Shows `notCompliedCount` in red when > 0, "—" otherwise. Updated `colSpan` on the empty-state row from 7 to 8. `notCompliedCount` accessed via cast since it's not yet in the TypeScript surface. | QCM filters AD/SB table for "Overdue". Sees N776AB in the list. Clicks "View" to drill in. Overdue column for N776AB shows "—". QCM is confused — why is this aircraft in the Overdue filter? Now: "Not Complied: 3" is immediately visible in the fleet table row. |
| BUG-QCM-DATE-001 | **AD/SB per-aircraft table used `toLocaleDateString()` without a timezone for AD due dates.** All other compliance pages (audit-trail, qcm-review) format dates with `timeZone: "UTC"`. AD due dates are stored as UTC midnight timestamps. In a non-UTC timezone (e.g., UTC-5), `new Date(utcMidnight).toLocaleDateString()` renders the previous calendar day — so a "January 1" due date shows as "December 31" of the prior year. An AD that is due today would appear overdue by one day for users in western timezones. | `app/(app)/compliance/ad-sb/page.tsx` | Changed `new Date(r.nextDueDate).toLocaleDateString()` to `new Date(r.nextDueDate).toLocaleDateString("en-US", { year: "numeric", month: "short", day: "numeric", timeZone: "UTC" })`. Now consistent with audit-trail/page.tsx and qcm-review/page.tsx. | QCM inspector in US Eastern time opens an aircraft's AD records. An AD due January 1 shows as "December 31" — appears overdue by one day. QCM flags it, calls the tech, starts an investigation. AD is actually current. False escalation caused by a timezone display bug. |
| BUG-QCM-C4 | **`AircraftComplianceCard` showed "Non-Compliant" badge but the metrics row said nothing about why.** When `notCompliedCount > 0, overdueCount = 0`, the card correctly showed the red "Non-Compliant" badge (because both conditions map to the "overdue" tier). But the metrics line below only rendered `{summary.overdueCount} overdue` — `notCompliedCount` was never shown. A QCM inspector would see the red badge with "0 overdue · 2 due soon" and have no idea why the aircraft was non-compliant. They would have to click through to the AD/SB page to discover the not-complied ADs. | `app/(app)/compliance/_components/AircraftComplianceCard.tsx` | Added a `{summary.notCompliedCount > 0 && (<span>N not complied</span>)}` metric line alongside the existing overdueCount line. The badge and the metric row now agree: if the card says "Non-Compliant" due to not-complied ADs, the count is visible without clicking through. | QCM reviewing the compliance dashboard sees N334TB with a red "Non-Compliant" badge but the metric shows "0 overdue · 3 due soon." QCM assumes the badge is wrong and ignores it. Three ADs (never performed) are missed before the FAA ramp check. Now: "2 not complied" is visible immediately on the card. |

## Bug Hunter Fixes — Shop Manager Cycle 37 (2026-03-02)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-SM-010 | **Reports Revenue Summary table showed redundant year label in multi-year mode** (e.g. "Jan '25 2025"). The chart X-axis correctly abbreviated to "Jan '25" for multi-year spans, but the table cell appended `row.key.split("-")[0]` (e.g. "2025") unconditionally regardless of whether `row.month` already embedded a 2-digit year. In single-year mode: "Jan 2025" ✓. Multi-year: "Jan '25 2025" ✗. | `app/(app)/reports/page.tsx` | Changed table cell to always derive the full month name and full year from `row.key` (`MONTH_NAMES[month-1] + year`). This gives a clean "Jan 2025" in all cases — no double-year, no abbreviated year. | Shop manager exports the monthly revenue table for accountant review. Multi-year data shows "Jan '25 2025" in every January row. Accountant asks why the formatting looks broken. Fix: consistent "Jan 2025" format in the table regardless of date range span. |
| BUG-SM-011 | **WO Throughput chart bucketed closed WOs by creation date instead of close date.** `throughputData` iterated closed WOs and used `wo.completedAt ?? wo._creationTime`. The field `completedAt` does not exist on work orders — only `closedAt` does. Because `completedAt` is always `undefined`, every closed WO fell back to `_creationTime` (when the WO was opened). A WO opened in November and closed in March would appear in November's throughput bar. The chart misrepresented when work was actually completed — making a busy March look empty and a quiet November look inflated. | `app/(app)/reports/page.tsx` | Changed `wo.completedAt ?? wo._creationTime` to `wo.closedAt ?? wo._creationTime`. `closedAt` is set (via `api.workOrders.closeWorkOrder`) when the WO transitions to "closed" status. | Shop manager reviews March throughput to justify adding a technician headcount. Chart shows 2 WOs in March. He has 11 WOs he personally signed off in March. The data looks obviously wrong but he can't tell why. Actual issue: all those WOs were opened in prior months and closed in March — but the chart showed them in their open month. Fix: throughput now reflects when WOs were actually closed. |
| BUG-SM-012 | **Dashboard "Active Work Orders" KPI count used a broader status filter than the WO list "Active" tab**, causing a confusing number mismatch. Dashboard counted all WOs not in `[closed, voided, cancelled, draft]` — including on_hold, pending_inspection, and pending_signoff. WO list "Active" tab only showed `[open, in_progress, open_discrepancies]`. A shop manager seeing "14 Active Work Orders" on the dashboard would click through and see only 9 WOs in the Active tab. | `app/(app)/dashboard/page.tsx` | Changed `active` filter in `LiveKPICards` to match the WO list: `["open", "in_progress", "open_discrepancies"].includes(wo.status)`. Added a comment linking the two. | Shop manager glances at dashboard: "14 Active Work Orders." Clicks through to manage them. WO list shows 9 in the Active tab. He wonders if 5 WOs are missing or the dashboard is double-counting. Spends 10 minutes auditing. Fix: dashboard KPI and WO list active count now always agree. |

## Bug Hunter Fixes — Lead Technician Cycle (2026-03-02T23:03Z)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-LT-HUNT-003 | **`SignStepDialog` did not clear `photoStorageIds`, `partsInstalled`, `approvedDataRef`, or `notes` on Cancel/re-open — only on successful submit.** A tech who adds photos for Step 2, clicks Cancel (decides not to sign yet), then opens the dialog for Step 3 would see Step 2's photos and parts pre-populated. Under 14 CFR 43.9 every sign-off must reference the actual work performed — attaching Step 2 photos to Step 3 creates a false maintenance record. The existing `useEffect` that runs on dialog open only cleared `pin`, `rating`, and `error`; the larger state fields were silently carried over. | `app/(app)/work-orders/[id]/tasks/[cardId]/_components/SignStepDialog.tsx` | Added `setPhotoStorageIds([]); setPartsInstalled([]); setNotes(""); setApprovedDataRef("");` to the existing open-triggered `useEffect`. All ephemeral form state is now cleared each time the dialog opens. | Tech signs Step 1 with a photo of the oil filter. Closes dialog. Opens dialog for Step 2 (landing gear inspection). Doesn't notice Step 1's photo is still there. Signs Step 2 — the oil filter photo is now permanently attached to the landing gear step. QCM finds the discrepancy during audit. Now: fresh state every time. |
| BUG-LT-HUNT-004 | **`HandoffNotesPanel` crashed with a TypeError when a note's `technicianName` was null/undefined** (e.g. technician record deleted after posting a note). `initials(n.technicianName)` called `.split()` on undefined, throwing `TypeError: Cannot read properties of undefined (reading 'split')`. This crashed the entire panel — the WO detail page became unusable for any task card with at least one note from a deleted tech. The task card detail page had `hn.technicianName ?? "Unknown Technician"` guards but HandoffNotesPanel did not. | `components/HandoffNotesPanel.tsx` | Changed `initials(name: string)` signature to `initials(name: string \| null \| undefined)` with an early return of `"??"` for falsy values. Added `n.technicianName ?? "Unknown Technician"` to the display span. | WO has 12 handoff notes. One technician was removed from the system. Shop manager opens the WO detail page to review shift notes before the morning debrief. Page crashes to a blank white area (React error boundary). All 12 notes are inaccessible. Fix: deleted-tech notes now show "Unknown Technician" instead of crashing. |
| BUG-LT-HUNT-005 | **`HandoffNotesPanel` textarea had no character limit**, unlike the inline textarea on the task card detail page (500-char cap). A tech writing a long shift note would hit the Convex backend size limit and receive a cryptic schema validation error with no indication of how many characters are allowed or how close they were. The task card detail page implemented the 500-char cap with a live character counter in a prior cycle (BUG-LT4-004) but the fix was never ported to `HandoffNotesPanel` (used on the WO detail page). | `components/HandoffNotesPanel.tsx` | Added `maxLength={500}` and `.slice(0, 500)` to the textarea onChange. Added a character counter (`{noteText.length}/500`) that turns amber at 480 chars. Restructured the input area to flex-1 with the counter below, matching the task card page pattern. | Tech types a detailed handoff note for the incoming shift (>500 chars). Clicks Send. Gets a cryptic "schema validation error" with no actionable message. The note is lost. Tech gives up and passes info verbally. Fix: counter warns at 480, enforcement at 500 — same cap as the task card page. |

## Bug Hunter Fixes — Parts Clerk Cycle 39 (2026-03-02)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-PC-001 | **PO Receive dialog always opened with blank quantity fields.** When a Parts Clerk clicked "Receive Items," `setReceiveQtys({})` reset all entries to empty. For a PO with 8 line items, the clerk had to manually type in the remaining quantity for every single item — including for full receipts. The `max` attribute on the input already had the right value (`item.qty - item.receivedQty`) but the default value was always `""`. This slowed down every receiving event and was error-prone on a busy receiving dock. | `app/(app)/billing/purchase-orders/[id]/page.tsx` | Pre-populated `receiveQtys` with each item's remaining quantity (`item.qty - item.receivedQty`) when the dialog opens. Clerk now sees the expected receipt quantity pre-filled; they only need to adjust items that were short-shipped. | Clerk receives a 12-line PO from Aviall. Opens dialog. All fields are blank. Must type "4", "2", "1", etc. for every line. Miskeys one field, receives 10 instead of 1, creates an inventory discrepancy. Fix: dialog pre-fills the full remaining quantity for every pending line item. |
| BUG-PC-002 | **Parts page had a `qrScannerOpen` state and `QRScannerDialog` component that was never reachable from the UI.** The page renders QR code badges for each part and has a `QRScannerDialog` that navigates to a part when a QR code is scanned — but the "QR" button to trigger this dialog was missing. The existing "Scan" button only opened the barcode scanner. The entire QR scan workflow (generate QR on label → scan QR at workbench to pull up part) was broken at the last step. | `app/(app)/parts/page.tsx` | Added a "QR" button next to the existing "Scan" button. Clicking it sets `qrScannerOpen=true`, which opens the `QRScannerDialog`. The dialog handles `PART:{partNumber}:{serialNumber}` formatted QR codes and navigates to `/parts?search={partNumber}`. | Clerk prints QR labels for all incoming parts (QR button per-row). Scans them at the bench to pull up the part record. Clicks "Scan" button on parts page but it opens barcode scanner mode — doesn't recognize the QR code format. Clerk manually searches by P/N every time. Fix: dedicated "QR" button now opens the QR scanner. |
| BUG-PC-003 | **Loaners `handleLoanOut` submitted to the backend even when no customer was selected.** The form required a customer (`*` label on the Select), but `handleLoanOut` had no client-side guard on `loanForm.customerId`. An empty string was passed as `customerId` to `api.loaners.loanOut`, which would either produce a Convex schema validation error (cryptic message) or create a loaner record with no customer association — making it impossible to track who the item was loaned to or invoice for it. | `app/(app)/parts/loaners/page.tsx` | Added a `if (!loanForm.customerId)` guard before `setIsLoaning(true)`. Shows a `toast.error("Please select a customer before loaning out this item.")` and returns early. The backend is never called with an empty `customerId`. | Clerk is on the phone with a pilot and quickly loans out a tow bar. Hits "Submit" before selecting the customer from the dropdown. Item is marked "Loaned Out" with no customer. No record of who took it. Cannot invoice for the rental. Loaner goes missing with no audit trail. Fix: hard stop with a clear error message before submission. |

## Bug Hunter Fixes — Billing Manager Cycle 40 (2026-03-02T23:33Z)

| ID | What Was Broken | File | What Was Changed | User Impact |
|----|----------------|------|-----------------|-------------|
| BUG-BM-HUNT-001 | **Quotes list hid the customer name (the primary identifier) while the `customers` query was loading.** The entire customer `<p>` was wrapped in `{customerMap.size > 0 && ...}` — so until customers resolved, all quote cards showed only a quote number and status badge with a blank space where the customer name should be. `isLoading` also did not include `customers === undefined`, meaning the skeleton phase ended before customer data was ready. On any slow connection, the billing manager would see a list of quotes with no customer context. | `app/(app)/billing/quotes/page.tsx` | Added `customers === undefined` to `isLoading` check. Removed `customerMap.size > 0 &&` guard — customer name is now always rendered (shows "Unknown Customer" if missing from map). Page stays in skeleton state until both `quotes` and `customers` are ready, then renders complete cards immediately. | Billing manager opens Quotes. Skeleton disappears — but quote cards show no customer names. She clicks into one to figure out who it's for. Three more are blank. She thinks the customer association is broken. Now: cards are complete on first render. |
| BUG-BM-HUNT-002 | **Quotes list showed no visual indicator for expired SENT quotes.** A quote with `expiresAt < Date.now()` and status `SENT` looked identical to an active SENT quote on the list — same blue "SENT" badge, no expired state, expires date shown in gray text. The detail page already had `isExpired` logic with a red badge, but the list view was blind to it. A billing manager reviewing her pipeline couldn't spot expired quotes without opening each one individually. | `app/(app)/billing/quotes/page.tsx` | Added `isExpired` check per quote (same logic as detail page: `expiresAt != null && expiresAt < Date.now() && status === "SENT"`). Expired quotes now show a red "EXPIRED" badge alongside the SENT badge, and the expiry date text turns red and reads "Expired Dec 31" instead of "Expires Dec 31". | Billing manager has 8 SENT quotes in her pipeline. Two expired 3 weeks ago and customers never responded. She doesn't follow up because they look active. Aged quotes sit forever. Now: EXPIRED badge is immediately visible on the list; she can prioritize follow-up or archive. |
| BUG-BM-HUNT-003 | **Time Clock clock-in dialog did not reset `error`, `clockInWO`, or `clockInNotes` on cancel/dismiss.** `onOpenChange` was set to the raw `setClockInDialog` setter — so closing via Cancel button, Escape key, or overlay click only toggled the boolean. The error message from a failed validation ("Select a work order") persisted on re-open. Any WO selection or notes typed before canceling would be pre-filled next time the dialog opened. A billing manager trying to clock in a tech for a different WO would see yesterday's WO pre-selected. | `app/(app)/billing/time-clock/page.tsx` | Changed `onOpenChange={setClockInDialog}` to a proper handler: on close (`!v`), clears `error`, `clockInWO`, `clockInNotes`, then calls the setter. All three paths (Cancel button, Escape, overlay) now clean state. | Manager opens clock-in dialog, selects WO-023, realizes she needs WO-031, cancels. Re-opens: WO-023 is still selected. Clocks in the tech to the wrong WO. Now: fresh dialog every time, tech selection is re-initialized from logged-in user's ID. |
| BUG-BM-HUNT-004 | **`VoidBatchDialog` in Invoices did not reset `reason` or `error` state on Cancel or overlay dismiss.** `onOpenChange` only called `onClose()`. If a billing manager typed a void reason, changed her mind, hit Cancel, then selected different invoices to void, the prior reason was pre-filled and the prior error (if any) was still visible. On a destructive action with permanent consequences ("this cannot be undone"), stale pre-filled data is a real risk. | `app/(app)/billing/invoices/page.tsx` | Updated `onOpenChange` handler to call `setReason(""); setError(null);` before `onClose()`. Updated the Cancel button's `onClick` to do the same before `onClose()`. Both exit paths now produce a clean dialog. | Billing manager selects 5 invoices to void, types "Duplicate billing run", changes mind, cancels. Selects 2 different invoices 10 minutes later. Void dialog pre-fills "Duplicate billing run." She doesn't notice. Submits. Two unrelated invoices voided with the wrong reason in the audit trail. Now: clean dialog on every open. |

## Bug Hunter Fixes — QCM Inspector Cycle 41 (2026-03-03T00:33Z)

Persona: **QCM Inspector** — Release Certificates + Maintenance Records workflow

| Bug ID | What Was Broken | File | Fix Applied | Why It Matters to the User |
|--------|-----------------|------|-------------|----------------------------|
| BUG-QCM-CERT-001 | **Certificates dialog did not reset form fields on re-open.** `openDialog()` only set `formType` and `approvalNumber` from org cert — it never cleared `partDescription`, `partNumber`, `serialNumber`, `batchNumber`, `quantity`, `workPerformed`, `condition`, or `remarks`. A QCM who typed an 8130-3 entry for Part A, cancelled, then clicked "New FAA 8130-3" for Part B would see Part A's full data pre-filled. Submitting without noticing would create a regulatory release certificate with the wrong part information. | `app/(app)/work-orders/[id]/certificates/page.tsx` | Added explicit reset of all form fields inside `openDialog()` before `setDialogOpen(true)`. Now every dialog open starts from a clean slate. | QCM issues an 8130-3 for a Fuel Control Unit. Cancels mid-way. Clicks "New 8130-3" for the Magneto. Description still says "Fuel Control Unit". Submits. FAA receives a Magneto cert with a FCU description. Regulatory risk. Fix: fresh form every time. |
| BUG-QCM-CERT-002 | **Certificates dialog had no client-side validation for required regulatory fields.** `handleCreate` called the backend immediately with no checks. `partDescription`, `partNumber`, `workPerformed`, `condition`, and `approvalNumber` are required on FAA 8130-3 and EASA Form 1 — blank submissions either produced a cryptic schema error or, worse, created an invalid release certificate document if the backend schema was lenient. | `app/(app)/work-orders/[id]/certificates/page.tsx` | Added sequential guard checks before `setSaving(true)`: empty partDescription → toast error + return; empty partNumber → toast error + return; no workPerformed selected → toast error + return; no condition selected → toast error + return; empty approvalNumber → toast error + return. Backend is never called with invalid data. | QCM opens dialog, skips Part Number (field is optional-looking), hits Create. Cert is issued with no P/N. DER reviewing the 8130-3 rejects it. Now: clear "Part number is required" message before any backend call. |
| BUG-QCM-REC-001 | **Maintenance record completion date default used UTC, not local date.** `defaultFormState.completionDate = new Date().toISOString().slice(0, 10)` extracts the UTC date. A technician in UTC-5 opening the form at 8pm gets yesterday's date pre-filled (e.g., form opened March 3 at 20:00 local = March 4 02:00 UTC → pre-fills "2026-03-04"). A maintenance record with the wrong completion date is a 14 CFR 43.9(a)(1) error. | `app/(app)/work-orders/[id]/records/_components/CreateRecordForm.tsx` | Replaced `new Date().toISOString().slice(0, 10)` with a `todayLocalISO()` helper that reads `d.getFullYear()`, `d.getMonth()`, and `d.getDate()` (local time) and pads them to YYYY-MM-DD. Added explanatory comment. | Tech in Chicago creates a maintenance record at 9pm on March 3. Date field pre-fills March 4 (UTC). Tech doesn't notice. Record is signed as "March 4" when the work was done on March 3. Inspector flags it as a date discrepancy. Fix: completion date always shows the technician's local calendar date. |
| BUG-QCM-REC-002 | **Maintenance records table and export used `toLocaleDateString()` without timezone for `completionDate`.** `completionDate` is stored as a YYYY-MM-DD string (the technician's local date). `new Date("2026-03-03")` parses as UTC midnight, so `toLocaleDateString()` in UTC-5 renders "Mar 2" instead of "Mar 3". This affected both the table view (line 359), the timeline view (line 438), and the plain-text export function. A QCM reviewing records would see dates shifted one day back — and an exported report submitted to the FAA would contain wrong dates. | `app/(app)/work-orders/[id]/records/page.tsx` | Changed all three instances to `toLocaleDateString("en-US", { timeZone: "UTC", year: "numeric", month: "short", day: "numeric" })`. UTC parse + UTC display = original YYYY-MM-DD value displayed correctly. Matches the same fix applied to audit-trail and qcm-review pages in prior cycles. | QCM exports maintenance records for a ramp inspection. All dates are one day earlier than the actual work dates. FAA asks why records are dated March 2 when the logbook entry says March 3. MRO must explain the discrepancy. Fix: displayed and exported dates match what the technician entered. |

## Bug Hunter Fixes — DOM (Director of Maintenance) Cycle 42 (2026-03-03T00:48Z)

Persona: **DOM** — Fleet → Aircraft Detail → Logbook → Predictions → Training

| Bug ID | What Was Broken | File | Fix Applied | Why It Matters to the User |
|--------|-----------------|------|-------------|----------------------------|
| BUG-DOM-HUNT-001 | **Predictions page: "Resolve" button had no confirmation for critical/high severity.** "Dismiss" correctly prompted a confirmation AlertDialog for critical/high predictions (safety gate), but "Resolve" fired immediately on click — no confirmation, no warning, no nudge to verify the maintenance action was logged. Resolving a CRITICAL prediction is at least as consequential as dismissing one: it removes the item from the active list and implies the underlying condition was corrected. | `app/(app)/fleet/predictions/page.tsx` | Added `resolveTarget` state (parallel to `dismissTarget`). Resolve button now checks severity: critical/high opens a confirmation AlertDialog; medium/low resolve immediately. Dialog warns DOM to confirm logbook entry before resolving. | DOM clicks "Resolve" on a critical engine oil pressure prediction. One-click — it's gone. No log entry was made. Three weeks later another tech sees the prediction is marked resolved, assumes it was handled. Oil pressure issue recurs. Fix: DOM must confirm with a dialog that the condition was addressed. |
| BUG-DOM-HUNT-002 | **Predictions tab severity badge counts didn't update when Status filter changed.** The tab badges (Critical, High, Medium, Low) always showed active-only counts regardless of the Status dropdown. If a DOM changed statusFilter to "All" to audit resolved/dismissed predictions, the tab said "Critical (2)" but the content showed 7 critical items. The counts and the visible list were contradictory, making the DOM think the tab was broken. | `app/(app)/fleet/predictions/page.tsx` | `tabCounts` memo now factors in `aircraftFilter`, `typeFilter`, and `statusFilter` (same logic as the `filtered` memo). Tab badge counts always match the number of items visible in that tab's content for the current filter state. | DOM switches to "All Statuses" to audit dismissed items. Critical tab shows "(2)" but clicking it shows 8 critical entries. She thinks 6 items disappeared. Changes filters to debug. Fix: counts and content always agree. |
| BUG-DOM-HUNT-003 | **Training dialog form state persisted on Cancel, causing pre-filled data on next open.** `resetAddForm()` was only called after a successful submission. Closing the "Add Training Record" dialog via Cancel or the X button left all fields (technician, course name, type, provider, dates, cert number, notes) populated. A DOM adding two training records would open the dialog a second time to find the previous record's fields pre-filled — especially dangerous when the technician dropdown still shows the previous tech, leading to misattributed training records. | `app/(app)/personnel/training/page.tsx` | Added `resetAddForm()` call to the Cancel button's `onClick` handler and to the `onOpenChange` handler's close branch. Both close paths now reset all form fields. | DOM adds HazMat training for Technician A, submits. Opens dialog for Technician B's HazMat renewal. Tech A is still selected in the dropdown. DOM doesn't notice. Submits. Technician B's completion never gets logged. DOM discovers it at the next audit. Fix: clean form on every open. |
| BUG-DOM-HUNT-004 | **Fleet CSV export silently did nothing when filters returned 0 aircraft.** The export handler was guarded by `if (filtered?.length)` — when `filtered` was empty (e.g., a status filter yielding no results), clicking Export produced no feedback whatsoever: no toast, no error, nothing. A DOM thinking the export ran silently, or trying multiple times without understanding why the file wasn't downloading, wastes time debugging a silent no-op. | `app/(app)/fleet/page.tsx` | Replaced `if (filtered?.length)` guard with an early-return that fires `toast.error("No aircraft to export. Clear your filters and try again.")` when the list is empty. On success, the toast now includes the count: "Exported 14 aircraft to fleet.csv". | DOM has status filter set to "In Maintenance", gets 0 results, clicks Export. Nothing happens. She clicks again. Checks her downloads folder — nothing. Calls IT. Fix: clear error toast explains the issue immediately. |
