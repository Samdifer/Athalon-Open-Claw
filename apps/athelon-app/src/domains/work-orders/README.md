# work-orders

Domain module scaffold for work-orders.

Current compatibility mapping:
- Route components currently live under `app/` and are being migrated incrementally.
- Shared UI/hooks/lib are available under `src/shared/` with legacy path compatibility.
